# 逐无人机功率、观测集合、融合节点与 C2F 的融合实验

## 设计与适用范围

本轮延续 600 m 水平部署区域、15 架无人机、10 个目标、RCS 0.05/0.1/0.2 m²、最多 8 个远程报告、60 条总观测、每目标最多 6 条观测。每架无人机总分配功率仍为 1 W，搜索感知占比 rho_i ∈ {0.2, 0.5, 0.8, 0.95}，通信占比为 1-rho_i；所有方法均采用两轮功率坐标搜索。没有额外提高天线增益或改变高度。

功率同时影响目标回波、其他接收机上的感知泄漏，以及报告链路的信号和可靠性。因此，联合变量是功率比例向量、逐目标观测集合及逐目标融合节点。优化仍然使用已有的预测检测效用与报告时延代价，且仅访问带误差的目标先验；没有使用真实检测结果挑选动作。

### 保守融合：保留搜索动作，按需精化

1. 缓存同一场景的几何、直接链路增益和 DD 数据，向量化更新功率变化后的通信及感知表。
2. 功率坐标搜索保留全部四档候选，只精化当前观测集合对应的软统计量。
3. 更新观测和融合节点时，根据当前功率形成候选池：当前观测、强观测，以及候选融合节点上的本地观测。
4. 仅精化池内链路，再执行原有增加、删除、交换和融合节点迁移；以完整精细模型的预测目标值决定是否接受。
5. 功率变化后重新形成候选池，避免把初始功率下的 C2F 筛选结果永久冻结。

候选池覆盖的是原有有界局部搜索可访问的动作。它不是全体观测组合的无损筛选证明，也没有联合全局最优保证。原有搜索的每个目标至多三个强接收机候选，加上当前融合节点；每个目的地采用六条强全局观测和六条强本地观测。

覆盖依据：在一次固定功率的局部搜索中，粗模型排序和通信可行性固定。后续融合节点只能来自初始节点与三个强接收机的并集；后续新增观测只能来自这些目的地的全局/本地前六名。候选池包含上述并集及初始观测，因此增、删、交换形成的后续集合仍位于池内。该结论依赖当前搜索规则；扩大目的地或观测前沿时，必须同步扩大候选池。物理表向量化存在浮点运算顺序差异，故实际实现仍通过逐场景结果一致性检查验证。

返回状态中的 `fine` 表只保证已查询候选和最终选中观测具有精细值；对池外候选开展新的搜索时必须重新精化。真实检测评估使用最终功率重建真实链路表，不能复用初始功率下的真值表。

### 激进融合：功率粗筛与更频繁的拓扑更新

另实现了探索性分支：每个功率坐标按粗模型排序，只精查前两个候选；每处理五架无人机就更新观测和融合节点，且在 C2F 候选池中保留可行的当前观测。该分支改变了搜索路径，粗筛可能错过好的功率动作。精细目标值在自身轨迹上单调，并不保证优于原算法，更不保证真实检测率提高。

## 实验与统计口径

- `results_v1_power_c2f`：新种子 10923，每个 RCS 30 个场景，共 90 个配对场景。比较采用相同缓存后端的原联合搜索与激进融合版；交替执行顺序，6 个工作进程。检测差异使用场景级配对区间，另报告三组 RCS 的 Bonferroni 校正区间。属于探索性实验。
- `results_v1_power_c2f_benchmark`：新种子 10929，单进程、轮换执行顺序；分别计时原实现、仅缓存后端、保守融合，逐场景检查最终观测集合、融合节点、功率向量和预测目标值是否一致。
- 计时仅包括联合搜索，不包含各方法共享的初始选取、几何/DD 预处理和真实检测评估；不应解释为端到端实时调度延迟。
- `fine_entry_updates` 是软统计量精细更新次数；`unique_refined_entries` 是被查询的不同链路—目标条目数。当前 `BaseGains` 仍预计算全量 DD 数据，不能把这些计数的减少说成已执行 DD 波形计算或整个系统运行时间等比例下降。
- 相同分配功率预算不代表不同调度方案消耗相同能量。

## 文件与复现

- `isac_sim/power_tables.py`：有界缓存及精确物理公式的向量化后端。仅支持当前正交、串行报告、共享频谱、LLR 的标准模型，遇到不支持的配置直接拒绝。
- `isac_sim/power_c2f_conservative.py`：保留原搜索动作的按需 C2F 精化。
- `isac_sim/power_c2f.py`：探索性的功率粗筛与频繁联合更新。
- `tests/test_power_c2f.py`：物理表一致性、输入不变、预算、预测目标值及原搜索结果一致性。

推荐的实验入口如下，输入为原联合搜索使用的同一组先验、初始观测与报告方案。

```python
from isac_sim.power_c2f_conservative import optimize_power_c2f_conservative

result = optimize_power_c2f_conservative(cfg, belief_base, selected, plan)
optimized_cfg = result.joint.cfg
optimized_observations = result.joint.selected
optimized_fusion = result.joint.plan
```

```powershell
E:/anaconda/3_11_python/python.exe tools/audit_v1_power_c2f.py --mc 30 --workers 6 --out results_v1_power_c2f_new
E:/anaconda/3_11_python/python.exe tools/benchmark_power_c2f.py --mc 4 --out results_v1_power_c2f_benchmark_new
E:/anaconda/3_11_python/python.exe -m pytest -q
```

## 激进融合结果：暂不采用

每组 30 个配对场景，以下时间为 6 个工作进程环境中的平均单场景搜索时间。

| RCS (m²) | 原联合搜索检测率 | 激进融合检测率 | 检测率差值的三组校正区间 | 原搜索时间 (s) | 激进融合时间 (s) |
|---|---:|---:|---|---:|---:|
| 0.05 | 0.580 | 0.557 | [-0.0716, 0.0249] | 21.39 | 37.21 |
| 0.10 | 0.643 | 0.660 | [-0.0257, 0.0590] | 18.82 | 32.42 |
| 0.20 | 0.780 | 0.777 | [-0.0528, 0.0461] | 15.79 | 27.01 |

三组都没有检出显著的检测率差异，也不能据此宣称等效或非劣。该版本的平均搜索时间增加约 71%–74%，三组预测目标值的均值也都略低于原联合搜索，因此不推荐作为默认方法。

其每场景不同精细链路条目从 2100 降至约 452–467，反复精细更新数从约 19.8 万降至 6273–7378，但这没有转化为本实现的实际加速：粗模型功率评分和更频繁的 C2F/融合节点搜索增加了成本。这是本轮需要保留的反例，而不是可用于宣传加速的结果。

原始逐场景结果及配置、源码哈希保存在对应结果目录。
