> **归档提示（2026-09-18）**：本文引用的部分 `results_*` 产物已移入 `_archive/2026-09-18/`；正文中的路径引用已同步更新为归档位置，命令行示例里的 `--out` 目录仍写作历史原名（重跑时依旧输出到该名）。

# RCS 影响下的系统性能

> ⚠️ **数字口径**：本文引用的是 2026-09-16 晚重跑前的 V1 主结果（`0.9762` 等）。
> 重跑后主比较 $P_D$ = **0.9764**（漂移 +0.0002）。RCS 扫描本身跑在 K=0/无远程报告档，
> 与主比较不同源；**本文的定性结论（P_D≥0.95 需 RCS∈(20,50] m²、协同增益呈倒 U 形）不受影响**。
> 新数字见 `SUBMISSION_TRACEABILITY.md` §3/§7。
>
> 📌 **要按 RCS 值直接查性能，看 §10**（小目标区 0.05–0.2 m² 的五套口径全枚举，2026-09-18 追加）。
>
> 🧭 **主场景已于 2026-09-18 重新定义**：小目标带取 **500–800 m × RCS 0.05–0.2 m²**
> （唯一同时覆盖"与已发布工作点同级"和"更难 2 dB"的带，见 `LOW_RCS_SCENARIO_500_800.md` §1）。
> 本文 §0–§9 的 4 km 结论仍然成立，但**不再是小目标带的主口径**。

日期：2026-09-16。本文回答"RCS 对系统性能的影响"，数据全部来自实跑，并标注每条结论的来源与边界。

## 0. 一句话结论

RCS 是当前系统**最强的主导变量**：在 4 km 部署下，RCS 从 0.05 升到 50 m²（+30 dB）
会把平均目标 P_D 从 **0.083 拉到 0.981**；而通信协同（远程报告）的增益全程不超过
**+0.037**，且呈倒 U 形——低 RCS 时它救不了场，高 RCS 时又用不上。系统在 4 km 下要
达到 P_D≥0.95，需要目标 RCS 落在 **(20, 50] m²** 区间。

## 1. RCS 如何进入模型

固定几何、功率、干扰与波形损伤时，RCS 与期望回波功率成正比，因此与感知 SINR 成正比：

- 双站增益 `g^s_ijq = λ² σ_q / [(4π)³ d_iq² d_jq²]`
- `rescale_sensing_tables_for_rcs` 把 `raw_gamma_sense`、`gamma_sense` 按 `factor` 线性缩放，
  **通信字段 `gamma_comm` / `rate` / `chi_comm` 完全不变**，软统计量矩从缩放后的 SINR 重算
  （保持 LLR 非线性）。

由此得到一个关键的结构性推论：**RCS 变差只削弱感知证据本身，不会让通信链路变差**。
低 RCS 下协同失效不是因为"传不动"，而是因为"可传的东西本来就不够"。

`target-local-v1` preset 与论文表 1 声明 `RCS = 50 m²`（大型目标，约 +17 dBsm），
`rcs_model = "mean"`，未启用 Swerling/起伏与 RCS 估计偏差。

## 2. 统一 RCS 曲线（4 km、C2F 自适应、MC=100、seed 10917）

低 RCS 段来自 `results_v1_rcs_joint`（0.05/0.1/0.2），高 RCS 段为本次新增
`_archive/2026-09-18/results_v1_rcs_wide`（1/5/20/50），两者协议完全相同，可拼接为一条自洽曲线。
方法为 `v1`（即论文的 `proposed_c2f_adaptive_pd` 选择结果）。

| RCS (m²) | dBsm | P_D (K=0) | P_D (K=8) | Δ(K8−K0) | 报告数(K=8) | 观测数(K=8) | P_FA |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 0.05 | −13.0 | 0.083 | 0.093 | +0.010 | 7.00 | 48.38 | 0.047 |
| 0.1 | −10.0 | 0.113 | 0.136 | +0.023 | 7.56 | 51.03 | 0.047 |
| 0.2 | −7.0 | 0.167 | 0.202 | +0.035 | 7.71 | 52.85 | 0.048 |
| 1 | 0.0 | 0.434 | 0.471 | +0.037 | 7.91 | 49.79 | 0.049 |
| 5 | +7.0 | 0.769 | 0.801 | +0.032 | 6.43 | 33.12 | 0.050 |
| 20 | +13.0 | 0.940 | 0.947 | +0.007 | 2.49 | 16.78 | 0.048 |
| 50 | +17.0 | 0.981 | 0.980 | −0.001 | 0.85 | 12.42 | 0.048 |

三条读数：

1. **检测门限效应**：曲线是 S 形。−13 dBsm 处 P_D≈0.09（几乎不可用），+17 dBsm 处
   已饱和到 0.98。**论文的高检测率结论建立在饱和区**，不能外推到小目标。
2. **通信协同价值是倒 U 形**：峰值在 1 m²（+0.037），两端都趋零。原因是两种不同的
   失效：低 RCS 时每条证据都太弱，多一条也跨不过门限；高 RCS 时本地观测已够，
   调度器**主动放弃**远程报告（报告数 7.91 → 0.85），也就没有可增益的空间。
3. **调度器会随 RCS 自动节流**：K=8 实际报告数从 7.91 降到 0.85，观测数从 52.9 降到
   12.4，串行时延从 8.44 ms 降到 0.91 ms。高 RCS 下系统是"省着用"而非"用不上"。

K=8 相对 K=0 的算法侧对照（`summary.json`）：`balanced` 在中高 RCS 稳定领先
（+0.022 @20，+0.014 @50，CI 不含 0）；`robust` 在低中 RCS 领先（+0.024 @1，
+0.023 @5）。`joint` 全程可分辨增益很小。

## 3. 部署区域与 RCS 的兑换率

`V1_RCS_600M_REPORT.md` 的 600 m 紧凑部署（斜距均值 418 m，4 km 为 2109 m）：

| RCS (m²) | 4 km P_D (K=8) | 600 m P_D (K=8) | 配对差 |
|---:|---:|---:|---:|
| 0.05 | 0.093 | 0.475 | +0.382 |
| 0.1 | 0.136 | 0.580 | +0.444 |
| 0.2 | 0.202 | 0.702 | +0.500 |

**实测兑换率约 12–13 dB**：600 m @ 0.05 m²（0.475）≈ 4 km @ 1 m²（0.471）。

注意这与纯雷达路径损耗的预期（斜距缩小 5.04 倍 → 双程 ≈ 28 dB）相差甚远。
原因是部署收缩同时改变了感知路径、无人机间通信路径与**干扰几何**——干扰机同样变近。
该目录自述"属于完整紧凑部署对照，不能归因为只改变雷达路径损耗"，实测支持这一保守说法。

结论不是"收缩就能补偿 RCS"，而是**几何只能买回约 12 dB，30 dB 的 RCS 差距补不平**。

## 4. 低 RCS 的救援手段（V15 主动系统屏幕，RCS 0.05 m²，worst-target P_D）

来自 `results_low_rcs_rescue_*` 与 `WORST_TARGET_REMEDIATION.md`。
以下全部是**筛选结果，不是可发表的估计**（几何种子数少、P_FA 校准分辨率低）。

| 配置 | 几何种子 | worst P_D | mean P_D |
|---|---:|---:|---:|
| 零增益、48 目标能量、标准模式 | 1 | 0.2112 | 0.7880 |
| 零增益、自适应 128 能量、最多 5 观测 | 1 | 0.4617 | 0.8727 |
| 零增益、64-look 救援模式 | 1 | 0.7705 | 0.9335 |
| 零增益、144-look、设计目标 0.97 | 3 | 0.4568 | 0.8271 |
| **15 dB 净增益**、标准模式 | 3 | 0.8837 | 0.9807 |
| **17.5 dB 净增益**、标准模式 | 3 | 0.9749 | 0.9917 |
| **17.5 dB 净增益**、纯 max–min | 3 | **0.9857** | **0.9981** |

两条硬信息：

1. **算法与融合救不回 0.05 m²**。零增益下 144-look 的三种子 worst P_D 为
   0.9703 / 0.0873 / 0.3127——极度不稳定，不能宣称普遍救援。
2. **需要约 17.5 dB 的硬件净增益**才能稳定跨过 0.95。该点尚未映射到真实天线/EIRP/系统损耗设计，
   且最大场景 P_FA 达 0.0808，**不满足严格 P_FA≤0.05，不可提升为结论**。

另有一条重要的阴性诊断：**lossless-report oracle 的 worst-target headroom 恒为 0**
（`lossless_heldout_worst_pd == heldout_worst_pd`）。即把上报当成无差错也提不上去，
说明当前瓶颈是**证据形成（formation-limited）**，不是传输或融合。这直接否定了
"靠 fusion/中继模块补偿弱回波"的路线。

## 5. 其他 RCS 相关证据（不同场景，勿混比）

- **V11 transition 扫描**（`_archive/2026-09-18/results_v11_transition_rcs*`，2026-09-15，seed 2026，MC=50）：
  同为 4 km 区域但场景不同（每目标仅 1 个本地观测，观测数 10），RCS 25→50 时
  `proposed_c2f_adaptive_pd` 的 P_D 为 0.916→0.970（K=0）/ 0.926→0.974（K=8）。
  与本文第 2 节的曲线**不可拼接**，仅作高 RCS 端的独立旁证。
- **V12 RCS 敏感性**（MC=20）：RCS 0.025–0.2 下 "joint − local" 配对区间**全部跨零**，
  RCS 升高抬高了绝对检测率但没改善协同的增量。
- **V13 RCS 鲁棒 bundle**：把 RCS 区间下界（ζ=0.5，即 0.025 m²）纳入定价后，
  在声明的下端点显著降低了**聚合**预测缺口，但**最差目标不变**；promotion gate = **NO-GO**。

## 6. 数据可信度边界（必须随结论一起引用）

1. **判决阈值漂移**：本节的 P_D 均由**当前代码**（2026-09-15 `03f9612` 之后的
   `calibrated_fused_threshold`）产生；论文主结果 CSV 生成于 09-14，用的是旧的
   Cornish–Fisher 阈值。同场景 MC=100 实测两者在 RCS=50 附近相差约 ±0.005
   （4 km：0.982→0.975；本次宽扫描：0.980）。**低 RCS 端未做该项对照**，
   而 LLR 分布偏离高斯的程度在弱信号下更大，可能被低估或高估，需专门复跑。
2. **seed 不同**：宽扫描用 seed 10917、MC=100；论文主结果是 seed 2026、MC=1000。
   两者 RCS=50 的 0.980 vs 0.9762 **不可作为严格的判决漂移量**，仅指示量级。
3. **无起伏模型**：`rcs_model="mean"`，未启用 Swerling-I 与方位依赖
   （`rcs_aspect_enable=False`）。真实小目标带起伏时，worst-case 只会更差。
4. **单场景族**：全部为 15 UAV / 10 目标、同一几何先验族；未做多种群确认。
5. **低 RCS 段来自探索性扫描**，不是等效性/非劣性证明。

## 7. 复现

```bash
# 低 RCS 段（已有）
python tools/audit_v1_rcs_joint.py --mc 100 --seed 10917 --workers 12 --out results_v1_rcs_joint
# 高 RCS 段（本次新增）
python tools/audit_v1_rcs_wide.py --mc 100 --seed 10917 --workers 12 --out results_v1_rcs_wide
```

产物：`protocol.json`（含基础配置与源码哈希）、`trials.csv`（方法级逐场景记录）、
`summary.json`（配对差与区间）。本次运行耗时 7 分 27 秒（12 workers，800 个 trial-job）。

## 8. 对论文的含义

- 主稿表 1 已声明 RCS = 50 m²，**这是必须保留的限定**：正文的 0.976/0.941/0.978
  只在大型目标下成立。若审稿人问泛化性，第 2 节曲线是最直接的回应。
- "通信协同有用"的表述应限定到 **RCS 0.2–5 m²（−7 至 +7 dBsm）** 区间；
  论文的 50 m² 工作点上 K=8 相对 K=0 为 −0.001，与现稿"通信影响不大"的口径一致，
  但那只是曲线右端的一个特例。
- 不要写"协同能救援低 RCS 目标"：第 4 节显示零增益下算法救不回来，
  需要 ~17.5 dB 硬件净增益才成立，且该点尚未合格。

## 9. 口径验证与配套文档（2026-09-16 追加）

第 2 节的曲线走 `tools/audit_v1_exact_budget.config()`，它显式覆盖了
`selector.score_mode='detector_pd'`，并把报告预算写成硬上限 K。为确认这不影响结论，
已用论文口径（CLI，`score_mode='exact_utility'`，`max_remote_reports=-1` 不设上限）
复跑了四个判决点，P_D 最大偏差 **0.007**：

| 配置 | 审计口径 P_D | 论文口径 P_D |
|---|---:|---:|
| 600 m, 0.2 m² | 0.7020 | 0.7000 |
| 4 km, 0.2 m² | 0.2020 | 0.2090 |
| 600 m, 5 m² | 0.9740 | 0.9740 |
| 4 km, 50 m² | 0.9800 | 0.9800 |

因此**第 2 节的 P_D 结论可跨口径引用**；但第 2 节的**报告数与时延受 K=8 上限约束**，
论文口径在低 RCS 段会多用约 2–5 条报告（600 m/0.2 m²：6.75 封顶 vs 9.22 不封顶，
时延 7.20 ms vs 9.83 ms）。引用这两个量时必须注明是否带上限。

完整的口径表、距离轴与机制分解见 `DEPLOYMENT_RANGE_ANALYSIS.md`。

## 10. 小目标区 RCS 0.05–0.2 m² 全口径枚举（2026-09-18 追加）

> 本节回答「RCS 固定落在 0.05 / 0.1 / 0.2 m² 时，系统性能各是多少」。
> 同一个 RCS 值在本库有 **五套互不可比的口径**（几何、统计量、方法入口都不同），
> 因此逐口径列出、每格注明来源与统计量，**禁止跨口径拼表**。
> 复算命令见 §10.7；本节全部数字已对冻结产物逐格复核。
>
> ⚠️ **2026-09-18 追加**：主场景已重新定义为 **500–800 m × RCS 0.05–0.2 m²**
> （链路预算位移 −2.0…+12.1 dB，包住已发布工作点；4 km 低 RCS 为 −30…−24 dB 故不可检，
> 400 m 为 +10…+16 dB 故出带）。场景定义、口径与证据台账见
> **`LOW_RCS_SCENARIO_500_800.md`**，新扫描产物 `results_v1_lowrcs_500_800`。
> 本节以下各口径仍在原几何上，**引用时以新场景文档为准**。

### 10.0 口径对照（先读这张）

| # | 口径 | 权威产物 | 几何 | 统计量 | 可否比较 |
|---|---|---|---|---|---|
| **A** | 联合 V1 审计协议：`target-local-v1` + Gaussian replacement，`seed=10917`、MC=100、100 个**共享**场景、`score_mode='detector_pd'`、报告硬上限 K∈{0,2,8} | `results_v1_rcs_joint`（4 km）/ `results_v1_rcs_600m`（600 m） | 4000 m / 600 m | **平均目标 P_D** | A 内部**可逐场景配对** |
| **B** | 协调实验：MC=200 × 4 部署种子、无报告上限 | `results/coordination_experiment_precision` | 500 m | **worst-target P_D 的跨种子 MIN** | 仅 B 内部 |
| **C** | 单链路最佳可送达视图代理（解析） | `tools/probe_rescue_levers.py` | 500 / 600 m | worst-target 代理 | **只排序，绝对值为废** |
| **D** | 感知 SINR 链路预算代理（逐目标解析界） | `results_v1_lowrcs_shortfall/shortfall.csv` | 400 / 600 m | `pd_pred` + 需补 dB | **只排序 / 定量缺口** |
| **E** | V15 主动系统屏幕（含硬件净增益坐标） | `results_low_rcs_rescue_*` | 800 m | heldout worst-target P_D | **屏幕，不可提升为结论** |

### 10.1 A 口径：4 km（论文几何）vs 600 m（紧凑几何），逐场景配对

方法 = `proposed_c2f_adaptive_pd`（产物里的 `v1`）。P_FA 全格落在 **0.0477–0.0489**（名义 0.05）。

| RCS (m²) | 4 km K=0 | 4 km K=8 | **600 m K=0** | **600 m K=8** | K=8 配对差（600m−4km） |
|---:|---:|---:|---:|---:|---:|
| 0.05 | 0.083 | 0.093 | 0.461 | **0.475** | **+0.382** |
| 0.1 | 0.113 | 0.136 | 0.565 | **0.580** | **+0.444** |
| 0.2 | 0.167 | 0.202 | 0.680 | **0.702** | **+0.500** |

（K=2 档：4 km 0.086 / 0.124 / 0.180；600 m 0.458 / 0.568 / 0.688。）

**协同（K=8 − K=0，v1，逐场景配对，原始 95% 区间）**

| RCS | 4 km ΔP_D | 4 km CI95 | 600 m ΔP_D | 600 m CI95（**12 项校正后**） |
|---:|---:|---|---:|---|
| 0.05 | +0.010 | [−0.0070, +0.0270] | +0.014 | [−0.0092, +0.0372] |
| 0.1 | +0.023 | [+0.0048, +0.0412] | +0.015 | [−0.0091, +0.0391] |
| 0.2 | +0.035 | [+0.0144, +0.0556] | +0.022 | [−0.0036, +0.0476] |

⇒ **4 km 端在 0.1/0.2 处显著（0.2 在校正后仍存活），600 m 端三档在校正后全部不显著。**
即「600 m 更好」是**几何净收益**，不是协同增益——`V1_RCS_600M_REPORT.md` 已明确二者不可混说。

**算法侧最好档（600 m，K=8，相对 v1）**：`balanced` 0.522 / 0.651 / 0.784
（+0.047 / +0.071 / +0.082，27 项校正后 0.05 与 0.1/0.2 均存活）；
`robust` 在 0.2 m² 为 0.771（+0.069）。4 km 端最好档仅为 0.096 / 0.140 / 0.209。

**资源随 RCS 的走向在两个几何上相反**（K=8）：600 m 随 RCS 升高自动节流
（观测 44.67→34.36、报告 7.66→6.75）；4 km 反而略增（观测 48.38→52.85、报告 7.00→7.71），
而 P_D 只挪了 0.109 ⇒ 4 km 端多堆的观测/报告**换不来检测率**。
（成因未单独归因，引用时按现象陈述，不要写成「调度器失效」。）

### 10.2 B 口径：500 m / RCS 0.2 的协调前沿（0.2 档唯一有确定性达标路径）

统计量 = **worst-target P_D 的跨 4 种子 MIN**，每格 800 trials，全档 800/800 收敛。
P_FA 27 臂全在 0.0479–0.0511。**与 10.1 不同表**（几何 500 m、统计量 worst-target MIN、无报告上限）。

| L（时间） | uncoordinated | mask_only | sparse |
|---|---:|---:|---:|
| 16（1×） | 0.7800 | 0.8150 | 0.8950 |
| 32（2×） | 0.8300 | 0.8600 | 0.9350 |
| **48（3×）** | 0.8450 | 0.9150 | **0.9550 ✅** |
| **96（6×）** | 0.9200 | **0.9500 ✅** | 0.9650 |
| **256（16×）** | **0.9550 ✅** | 0.9750 | 0.9850 |

⇒ RCS 0.2 下：**协调 sparse 只要 3× 积分时间**（不协调需 16×）即可 4/4 达标。
**第二条独立路径**：把 `interference.direct_cancellation_db` 从发布值 40 dB 加深到 **60 dB**，
则不协调单臂 **1× CPI 即 4/4 达标**——但两条路径**互为替代品，不可叠加计功**（ROUTE §13.6）。

### 10.3 C 口径：单链路代理探针（**只可用于排序**）

| 场景 | rinr | 送达覆盖率 | 基线 worst P_D | L=32 | L=64 | L=144 | L=256 | power ×10 | 已达标 | 到 0.95 还差 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| **600 m / 0.1** | +11.05 dB | 68.0% | 0.1146 | 0.157 | 0.226 | 0.366 | 0.522 | +0.010 | 5/10 | ~+12 dB γ |
| **500 m / 0.2** | +11.52 dB | 69.3% | 0.1740 | 0.255 | 0.385 | 0.624 | **0.819** | +0.014 | 6/10 | ~+10 dB γ |
| 4 km / 0.05（论文几何） | −0.06 dB | 49.0% | 0.0371 | — | — | — | 0.0470 | +0.0002 | 0/10 | 不可达 |

**两条硬读数**：① 两个低 RCS 点都**确实干扰受限**（rinr ≈ +11 dB），
故 power ×10 只买 +0.010~0.014（分母饱和），**looks 是唯一强杠杆**；
② 4 km 端算法喘息空间 ≈ ΔP_D 0.01 且仍低于 P_FA ⇒ 该工作点上小目标**不可检**。
⚠️ 全部为**单链路视图**，与发布系统的多链路融合不是一个口径，**绝对值不得进论文**。

### 10.4 D 口径：解析链路预算缺口（400 / 600 m，目标 0.80 弱目标要求）

代理量 `pd_pred`（目标平均）+ `required_gain_db_worst`（把最弱目标抬到 0.80 还缺多少 dB）。
⚠️ **只是解析界，不是 MC 的 P_D**；`corr.enable` 会破坏「链路独立」假设 ⇒ 偏乐观。

| 场景 | L=16 | L=64 | L=128 |
|---|---:|---:|---:|
| 400 m / 0.05 | 0.459（缺 7.90 dB） | 0.565（缺 5.26） | 0.617（缺 4.27） |
| 400 m / 0.1 | 0.563（缺 5.52） | 0.669（缺 3.39） | 0.726（缺 2.60） |
| 400 m / 0.2 | 0.670（缺 3.53） | 0.783（缺 1.84） | 0.833（缺 1.15） |
| 600 m / 0.05 | 0.450（缺 7.50） | 0.561（缺 4.74） | 0.619（缺 3.84） |
| 600 m / 0.1 | 0.559（缺 4.87） | 0.677（缺 3.04） | 0.735（缺 2.27） |
| 600 m / 0.2 | 0.674（缺 3.20） | 0.793（缺 1.58） | 0.847（缺 **0.96**） |

⇒ **积分时间只能买 √L**：400 m/0.05 从 L=16 到 L=128（8×）只把缺口从 7.90 压到 4.27 dB。
「缺口」与「P_D」不同构（d′ 对 γ 是凹的），别把 dB 差额直接当 P_D 差额。

### 10.5 E 口径：800 m / RCS 0.05 的救援屏幕（V15 主动系统）

| 配置 | heldout worst P_D |
|---|---:|
| 零增益、48 目标能量、标准模式（基线） | 0.2112 |
| 零增益、自适应 128 能量、≤5 观测 | 0.4617 |
| 零增益、64-look 救援模式 | 0.7705 |
| 零增益、144-look、设计目标 0.97（3 种子） | 0.4568（0.9703 / 0.0873 / 0.3127，**极不稳**） |
| **+15 dB** 净增益、标准模式 | 0.8837 |
| **+17.5 dB** 净增益、标准模式 | 0.9749 |
| **+17.5 dB** 净增益 + 纯 max–min | **0.9857** |

**共同诊断（本节最重要的一条）**：所有档的 `lossless_worst_pd_headroom ≡ 0.0`
⇒ 把上报当作无差错也提不上去 ⇒ 瓶颈是**证据形成（formation-limited）**，不是传输/融合。
**这条直接否定「靠 fusion / 中继补偿弱回波」的路线。**
⚠️ 屏幕结果：几何种子 3 个、P_FA 校准分辨率低（17.5 dB 档最大场景 P_FA 达 0.0808，
**不满足严格 P_FA ≤ 0.05**），不可提升为结论。

### 10.6 一页结论

1. **RCS 是主导变量，但在论文几何上是「不可检」而非「较差」**：4 km 下 RCS 0.05→0.2（+6 dB）
   只把 P_D 从 0.083 抬到 0.202（+0.084），全程与 P_FA≈0.048 **同一量级**。
2. **紧凑部署是同一协议下唯一显著抬升的手段**：600 m 得 +0.382 / +0.444 / +0.500（配对、全部显著），
   等效约 **+12–13 dB** RCS；但代价是干扰几何同步变近，纯雷达路径损耗的 28 dB 只兑现一半。
3. **600 m 三档仍全部低于 0.80 弱目标要求**（最好 0.702），且 K=8 协同增益校正后不显著；
   能显著抬升算法的只有 `balanced`（+0.047 / +0.071 / +0.082）。
4. **只有 RCS 0.2 有确定性达标路径**：500 m 下协调 3× 时间，或对消深度 40→60 dB 的 1× 时间
   （二者互为替代品）。RCS 0.05 需要 ~+15–17.5 dB **硬件**净增益，且该点尚未合格。
5. **失败机制统一为形成受限**：所有小目标档 `lossless` headroom ≡ 0，
   算法侧只能重分配预算、造不出预算。
6. **未做**：RCS 0.05–0.2 × 距离的二维完整网格；`rcs_model="swerling1"` 起伏；
   多种群/多几何族确认；RCS 0.2–0.5 之间的达标门槛（`bracket_minimum_detectable_rcs`
   当前只有暂定区间 (0.2, 0.5] m²）。

### 10.7 复现

```bash
PY=E:/anaconda/3_11_python/python.exe
# A 口径（4 km / 600 m 低 RCS，逐场景配对，MC=100）
$PY tools/audit_v1_rcs_joint.py  --mc 100 --seed 10917 --workers 12 --out results_v1_rcs_joint
$PY tools/audit_v1_rcs_600m.py   --mc 100 --seed 10917 --workers 12 --out results_v1_rcs_600m
# B 口径（500 m / 0.2 协调前沿，单档双 800 trials）
$PY -u tools/run_coordination_experiment.py --mc 200 --seeds 4 --looks 48 --penalty 0.1 \
    --rounds 12 --out results/coordination_experiment_precision
# C / D 口径探针
$PY tools/probe_rescue_levers.py --area 600 --rcs 0.1
$PY tools/probe_rescue_levers.py --area 500 --rcs 0.2
$PY tools/probe_lowrcs_shortfall.py --out results_v1_lowrcs_shortfall
# 新场景（500–800 m × 0.05–0.2，7 格旋钮，MC=100，seed 10917）
$PY -u tools/audit_v1_lowrcs_sweep.py --mc 100 --areas 500 600 700 800 \
    --rcs 0.05 0.1 0.2 --seed 10917 --workers 14 \
    --cells base looks64 looks128 maxmin maxmin_looks64 maxmin_looks128 gain15 \
    --out results_v1_lowrcs_500_800
$PY tools/analyse_lowrcs_500_800.py   # 600 m 跨工具逐位校验 + 地图表
```

⚠️ 三次低 RCS 扫描的实际覆盖与自述**不一致**，引用前必须核 `trials.csv` 行数：

- `results_v1_lowrcs_sweep`（protocol 声明 400/600 m × 0.05/0.1/0.2）**只有表头、零行**
  ——该扫描**从未产出数据**，任何引用它的结论都无产物支撑。
- `results_v1_lowrcs_cheap`（protocol 声明 400/600 m × 0.05/0.1/0.2、9 格）实际只有
  **(400, 0.05) 全套 100 trials + (400, 0.1) 43/100**；**600 m 一格未跑**。
  其 `summary.json` 已于 2026-09-18 用 `--report-only` 重建（产物齐全）。
  ⚠️ 因此"400–600 m"的一切表述都应读作 **400 m**。
- `results_v1_lowrcs_screen`（154 行）/ `_power`（46 行）是部分格，**无 `summary.json`**，
  只能当筛选线索。
