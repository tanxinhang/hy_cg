# P1-3 准入测量：per-link 的 `rho` 有没有散布？

**结论：`P1-3`（可辨识性感知的选择器）值得做。判据 (A) 与 (B) 双双 PASS。**

但这轮真正的收获不止判决本身：**过程中挖出两个会静默伪造结论的缺陷**，
一个作废了角度探针 §4.3 的旧数字，一个作废了本测量自己的前两轮数字。两者都已在代码中修掉并钉成断言。

---

## 一、要回答什么

角度探针已经判过：「DD 重叠主要是 DD-only 抽象的产物，小阵列能大幅救回」。
但**那只说明 ρ 的绝对水平能被救回来**，不能说明**选择器有事可做**。两个独立问题：

| | 问题 | 判据（跑之前写死） |
| --- | --- | --- |
| **(A)** | 同一目标的 ρ 在链路之间**有没有散布**？若所有链路都是 0.6，按 ρ 选 = 随机选 | `spread = ρ_p90 − ρ_p10 ≥ 0.20`（M ∈ {4,8,16} 取最大）PASS；`< 0.05` FAIL |
| **(B)** | 这个散布**是不是现有选择器已经看得见的信息**？若 ρ 与感知 SINR 单调相关，现有按 SINR 选的选择器已经挑走了好的，加 ρ 无用 | `\|Spearman(ρ, γ_sense)\| ≤ 0.70` PASS；`> 0.70` FAIL |

**决策：P1-3 值得建 ⇔ (A) 且 (B) 都 PASS。**

一致性自查（上一份探针的规则字符串因常数自指而**结构上必然 FAIL**，这次先查）：
PASS 与 FAIL 阈值之间留有 0.05–0.20 的灰带（不是同一条件）；(B) 的两档互补。**无自指。**

## 二、测什么（口径）

* **per-link ρ**：接收机 `j` 上、由照射源 `i` 照到目标 `q` 的那条回波模板，
  对**该接收机上其他所有目标的信念回波**张成空间的逃逸率
  `ρ = 1 − ‖P_others a‖²/‖a‖²`（与生产 `cancellation_glrt` 同一泛函）。
* **不加功率权重**：这里问的是"选择器在这条链路上看到什么"。若把回波功率折进 Gram，
  就等于把 SINR 又塞回那个正要拿去和 SINR 做对照的量里（(B) 会自证）。
  （顺带核实：功率加权对本逃逸率**确为恒等变换**——同接收机逐列对账，加权/不加权完全相同。）
* 阵列 = 半波长 ULA，**单位模导向**（隔离口径增益，只测空间选择性）；主口径 **体轴 x**（固定，不看数后再挑），y 轴并列报告。
* 场景 `paper-canonical` + 600 m + RCS 0.1，15 UAV / 10 目标，信念几何；**10 trials**（慢路径成本，见 §五）。

## 三、结果（10 trials）

| M | 口径 | ρ p10 | ρ p50 | ρ p90 | **spread** | 接收机间 spread | Spearman(ρ, SINR) |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| **1**（DD-only） | — | 0.0000 | 0.0017 | 0.4206 | 0.421 | 0.070 | −0.032 |
| 4 | 8 cm | 0.1570 | **0.8153** | 0.9776 | **0.780** | 0.467 | −0.012 |
| 8 | 18 cm | 0.4854 | **0.9445** | 0.9922 | 0.485 | 0.261 | −0.011 |
| 16 | 38 cm | 0.7852 | **0.9848** | 0.9981 | 0.209 | 0.106 | 0.020 |

**选择余量**（同一目标，取最好的 k 条链路 vs 取中位数排名的 k 条；"top1 − mean" = 相对随机挑）：

| M | k=1 | k=2 | k=3 | top1 − 全链路均值 |
| --- | ---: | ---: | ---: | ---: |
| 1 | 0.852 | 0.829 | 0.823 | 0.729 |
| 4 | **0.185** | 0.180 | 0.182 | 0.326 |
| 8 | 0.056 | 0.056 | 0.056 | 0.162 |
| 16 | 0.015 | 0.015 | 0.015 | 0.073 |

## 四、判决

* **(A) PASS**：M=4 时 `spread = 0.780`（p10 0.157 → p90 0.978）。
  同一目标的链路从"几乎被完全掩蔽"到"几乎自由"，**选择器有事可做**。
* **(B) PASS**：`|Spearman(ρ, γ_sense)| = 0.011–0.032 ≈ 0`。
  **ρ 与现有选择器用的感知 SINR 基本无关** —— 现有选择器对它**零信息**，
  把 ρ 加进目标函数是**新增的一维**，不是已有维度的重述。

⇒ **P1-3 值得建。**

### 但要在哪里建（这是本测量给出的第二个结论）

| M | 水平（中位 ρ） | 余量（top1 − 中位排名） | 判断 |
| --- | ---: | ---: | --- |
| 1 | 0.002 | 0.852 | 余量最大但**水平无用**——全是烂链路里挑稍微不烂的 |
| **4** | **0.815** | **0.185** | **甜点**：水平已经很好，余量仍可观 |
| 8 | 0.945 | 0.056 | 水平更好，余量开始被天花板压掉 |
| 16 | 0.985 | 0.015 | **天花板**：几乎条条都好 ⇒ 选不选都一样 |

⇒ 可辨识性感知的选择器**在小阵列（M = 4–8）上价值最大**；
到了 M=16，ρ 饱和，选择器无事可做——**这本身是一个该写进论文的结论**：
"孔径足够大以后，可辨识性不再是选择器的约束"。

另：接收机之间的 spread 在 M=4 时达 **0.467**，说明**"fusion 选哪个接收机"**这一步对 ρ 极敏感，
是 P1-3 最直接的落点。

## 五、过程中挖出的两个缺陷（都已修 + 已钉断言）

### 1. `escape_from_gram` 共轭方向错误 —— 2.33e-15 的证书没看见它

原式 `conj(u) G_ss⁺ u`，正确是 `u G_ss⁺ conj(u)`。**单列 nuisance 时 `G_ss⁺` 是标量，两式都是 `|u|²`**，
而生产证书（吻合 2.33e-15）取的正是**两模板**用例 ⇒ 缺陷不可见。
`|others| ≥ 2` 且复数据时偏差达 **0.23**，且会**变负被 clip 成 0**（读成"完全不可逃逸"）。
⇒ **角度探针 §4.3 的旧数字作废并已重跑**（见该文档 §4.3/§4.5）。

### 2. Schur 补快路径 + 抽样守卫 —— 守卫通过，结果全错

曾用 `ρ_c = 1/inv(corr)[c,c]` 替掉逐列 `pinv`（快 ~1000×），守卫是"每 17 列抽 1 列与慢路径比对"。
守卫判定 68% 配置"一致（最大偏差 4.5e-8）"，但**逐列**是
`0.988 vs 0.346`、`0.501 vs 0.050`、`0.304 vs 0.113`，**中位 0.909（慢） vs 0.374（快）**：
140 模板相关矩阵上 `inv` 的反向稳定性不足以支撑"取倒数"，且**错误随列而变** ⇒ 抽样恰好抽到一致的列。
⇒ **整条快路径删除**（不是加开关），本测量前两轮数字作废，按慢路径重跑（成本 ~6.5 min/trial）。

> 教训（已进技能）：**错误若随样本而变，抽样验证就不是证书**；
> 同一家族今天共三例 —— Gram 未先归一化 / 共轭方向 / 抽样证书。

## 六、复现与门禁

```
PY=E:/anaconda/3_11_python/python.exe
$PY tools/probe_perlink_rho.py --trials 10 --out results_perlink_rho   # ~66 min
$PY -m pytest tests/test_perlink_rho.py -q
```

单测钉住：`_headroom` 在平坦剖面下必须给 0（否则"P1-3 无意义"是假的）、k 超出链路数不得崩、
`_spearman` 符号/秩变换不变性；M=1 的**中位**（不是最大）ρ 应≈0（少数 DD 孤立目标本就有高 ρ）；
ρ 随 M 单调不减；接收机级散布必须在**跨接收机池化**后计算（否则单元素分组静默返回 NaN）；
以及预注册常数不自相矛盾（防止重蹈上一份探针字符串自指的覆辙）。
