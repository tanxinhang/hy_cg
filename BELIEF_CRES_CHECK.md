# `C_res` 补信念误差项：测过了，**不能**去掉 P_D 的注脚

结论先说：**这一项把 P_FA 减半，同时也把 P_D 减半，AUC 保持在随机水平。它不是一次标定，而是一次整体削弱。**
所以"每条 P_D 都要带未标定注脚"这件事**暂时保留**——但这一轮把**为什么**量出来了，并指向真正的修法。

## 一、做了什么

检测器在**信念**状态下建模板，回波却来自**真实**状态 ⇒ 模板错位 ⇒ 回波泄漏进残差，
这就是 belief 模式下 P_FA 高于标称（0.15–0.23 vs 0.05）的物理来源。

新增 `cancellation.belief_error_in_cres`（**默认关闭**，发布数字逐位不变）：
把错位的一阶协方差计入 `C_res`——对每条信念回波追加两列

```
sqrt(P) · sigma_l · da/dl    与    sqrt(P) · sigma_k · da/dk
```

`sigma_l / sigma_k` 由声明的 `belief_sigma_pos_m / belief_sigma_vel_mps` 换算成 **DD 格单位**：

```
sigma_l = (sigma_p / c) · L · Δf        sigma_k = (2 sigma_v / λ) · N · T
```

两个刻意的选择（都写进代码注释）：

1. **排除被测目标自身**。`C_res` 是 **H0 下**的残差协方差，H0 里该目标不存在，
   把它那份泄漏也算进去，等于在**设定虚警门限的地方**把协方差吹大——那不是标定，是削弱检验。
   代价如实说明：H1 下被测回波自身的错位没被计费，所以这一项换来的是**偏乐观的 P_D**。
2. **不是系数先验**。`prior_variance` 描述的是"被拟合的干扰幅度有多不确定"；
   这一项描述的是"模板放不准"。两者互相当对方计费，就会留下正好被测量的那点失准。

## 二、结果（60 trials，600 m / RCS 0.1）

| arm | P_FA 关 | P_FA 开 | P_D 关 | P_D 开 |
| --- | ---: | ---: | ---: | ---: |
| perfect_channel | 0.233 | **0.117** | 0.317 | 0.183 |
| tp_uic_stage1 | 0.150 | **0.083** | 0.167 | 0.100 |
| tp_uic_full | 0.183 | **0.067** | 0.200 | 0.100 |

标称 P_FA = 0.050。**门限无关的配对 AUC**（这才是"可分辨性有没有变好"）：

| arm | AUC 关 | AUC 开 | Δ |
| --- | ---: | ---: | ---: |
| perfect_channel | 0.520 | 0.480 | −0.040 |
| tp_uic_stage1 | 0.468 | 0.469 | +0.001 |
| tp_uic_full | 0.477 | 0.494 | +0.017 |

⇒ 两个率**同比例**下降，AUC 仍在 0.48–0.52（= 随机）。**没有买到可分辨性，也没有把 P_FA 拉到 0.05。**

## 三、为什么（这是本轮真正的信息）

测出来的换算结果：

```
sigma_p = 150 m  →  0.960 延迟格
sigma_v = 15 m/s →  1.259 多普勒格
距离单元 = c/(2 L Δf) = 78.1 m   ⇒  150 m ≈ 1.92 个距离单元
```

**声明的信念误差 ≈ 一个完整分辨单元。** 在这个量级上：

* 一阶（切向）展开失效——实测 `tr(C_belief)/‖s‖² = 18`，即线性化给出的错位能量是回波本身的 18 倍，
  而真实的整格错位最多约 2 倍；
* 更根本的是：**模板落在了错误的格子里**。这不是协方差能修的——`C_res` 只能说"我对残差的把握变差了"，
  不能把模板挪回正确的格子。

（量级佐证：这一项每格只比噪声底高 **2.7 dB**，并不是把协方差吹爆；问题不在量级，在结构。）

⇒ 与 §"#1/#6" 那条线一致：**瓶颈是"信念误差 vs 分辨单元"这一对比例**，不是接收机侧的协方差记账。

## 四、所以下一步该动什么（按性价比）

1. **把信念不确定性纳入搜索**（`cancellation.search_half_width` 现在是 1）：σ ≈ 1 格时，
   ±1 格的搜索窗本来就覆盖不全。先做一个 `search_half_width ∈ {1,2,3}` × 是否含信念项的 2×3 扫描——
   这是**同一次实验**里最可能闭合 CFAR 的旋钮，且不需要改模型。
2. **把一阶展开换成精确错位**：对 δ 做 3–5 点 Gauss–Hermite 求积，
   `E[(a(l+δ)−a(l))(·)^H]`，在任意 σ_l 下都成立（现行版本只在 σ_l ≪ 1 时成立）。
3. **或者承认并写进 assumptions**：600 m/0.1 这个工作点上，σ_p = 150 m > 1 个距离单元，
   在此条件下**不做**绝对 P_D 的标定声明。

## 五、复现与门禁

```
PY=E:/anaconda/3_11_python/python.exe
$PY tools/run_belief_cres_check.py --trials 60 --out results_belief_cres   # ~13 min
$PY -m pytest tests/test_belief_cres.py -q                                  # 4 passed
```

单测钉住：默认关闭且"关→开→关"经 `C^{-1/2}` 探针**逐位复原**；开时必须改变某个数（否则实验测不到它）；
`dictionary="truth"` 时不计费（真值模板没有错位）；**被测目标的泄漏不计费**（列数 = 2 × 非被测源数）；
以及 `sigma_l = 0.96 / sigma_k = 1.26` 格这个"超出线性区"的事实被显式记录。
