# 部署距离的影响：600 m 与 4 km 对照

> ⚠️ **数字口径**：本文引用的是 2026-09-16 晚重跑前的 V1 主结果（`0.9762` 等）。
> 重跑后主比较 $P_D$ = **0.9764**（漂移 +0.0002，落在 MC 噪声内）。
> **本文的定性结论（几何净增益 +9~13 dB 的量级、达标 RCS 门槛从 ≈21.7 → ≈2.81 m²）不受影响。**
> 新数字见 `SUBMISSION_TRACEABILITY.md` §3/§7。

日期：2026-09-16。本文回答"4 km 是不是太远了、改成 600 m 会怎样"，全部数据来自实跑，
并标注口径、交叉验证与边界。配套数据：`results_v1_geometry_wide/`、
`results_v1_distance_paperproto/`；机制探针：`tools/probe_geometry_scaling.py`。

## 0. 一句话结论

**4 km 不是设定错误，600 m 也不是万能药。** 换成 600 m 净赚约 **9–13 dB** 等效增益，
把"达到 P_D≥0.95 所需的目标 RCS"从约 **22 m² 降到约 2.8 m²**；但 RCS=0.2 m² 这类弱目标
在 600 m 下仍只有 **P_D=0.700**（4 km 为 0.209）。真正的瓶颈是 **RCS 与距离的比值**，
而缩区域只买到"一半"——因为回波走双程 R⁻⁴、干扰走单程 R⁻²，两者同时变强。

**不建议把论文基线改成 600 m。** 在 600 m + 论文工作点 RCS=50 m² 下，系统退化为
**纯本地感知**（远程报告 0.00、观测 10.00、时延 0.00 ms），"通信受限协同融合"这一命题
在 600 m 上几乎失去对象；要在 600 m 保留协同问题，就必须把 RCS 降到 0.2–5 m²，
而那时 P_D 只有 0.70–0.97。

## 1. 实验口径与跨口径验证

所有新数据基于冻结协议：`target-local-v1` preset、Gaussian replacement 失败模型、
`seed=10917`、MC=100、15 UAV / 10 目标，**只改 `geometry.area_xy`、`detect.target_rcs`、
`selector.max_remote_reports`**。`results_v1_rcs_600m`（600 m 低 RCS）与
`results_v1_rcs_joint`（4 km 低 RCS）、`results_v1_rcs_wide`（4 km 高 RCS）共享同一
100 个基础场景，因此可逐场景（逐 trial）配对，而不是当独立样本比较。

**口径差异已核实并消除。** 扫描走 `tools/audit_v1_exact_budget.config()`，它显式覆盖了
`selector.score_mode='detector_pd'`，并把报告预算写成硬上限 K。论文口径走 CLI
（`score_mode='exact_utility'`，`max_remote_reports=-1` 即不设上限）。为此用论文口径
复跑了四个判决点：

| 配置 | 审计口径 P_D | 论文口径 P_D | 差 | 审计 obs | 论文 obs | 审计报告 | 论文报告 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 600 m, 0.2 m² | 0.7020 | **0.7000** | 0.002 | 34.36 | 34.37 | 6.75（封顶） | 9.22 |
| 4 km, 0.2 m² | 0.2020 | **0.2090** | 0.007 | — | 52.87 | — | 12.16 |
| 600 m, 5 m² | 0.9740 | **0.9740** | 0.000 | — | 11.01 | — | 0.74 |
| 4 km, 50 m² | 0.9800 | **0.9800** | 0.000 | 12.42 | 12.42 | 0.85 | 0.85 |

**结论：P_D 对口径不敏感（最大偏差 0.007）。** 唯一实质差异是审计口径的 K=8 硬上限在
低 RCS 段会封顶报告数（600 m/0.2 m² 时 6.75 vs 9.22），使时延偏低（7.20 ms vs 9.83 ms）。
本文件所有 P_D 曲线两者皆可引用；**涉及报告数/时延的数值须注明是否带上限**。

论文口径复跑 4 km/50 m² 得到 0.9800 / 12.42 / 0.85 / 0.9067 ms，与审计口径逐位一致；
与已发表 `results_target_local_v1/main/main.csv`（0.9762 / 12.101 / 0.751 / 0.801 ms）
的差异来自**种子不同**（10917 vs 论文主实验）与 09-15 判决阈值更换，不是口径错误。

## 2. 600 m 全 RCS 曲线（K=8，平均目标 P_D，方法 `proposed_c2f_adaptive_pd`）

| RCS (m²) | 600 m K=0 | 600 m K=8 | 4 km K=0 | 4 km K=8 | 600m 报告 | 4km 报告 | 600m 观测 | 4km 观测 |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 0.05 | 0.461 | 0.475 | 0.083 | 0.093 | 7.66 | 7.00 | 44.67 | 48.38 |
| 0.1 | 0.565 | 0.580 | 0.113 | 0.136 | 7.26 | 7.56 | 40.24 | 51.03 |
| 0.2 | 0.680 | 0.702 | 0.167 | 0.202 | 6.75 | 7.71 | 34.36 | 52.85 |
| 1 | 0.905 | 0.907 | 0.434 | 0.471 | 3.80 | 7.91 | 19.78 | 49.79 |
| 5 | 0.974 | 0.974 | 0.769 | 0.801 | 0.72 | 6.43 | 11.01 | 33.12 |
| 20 | 0.989 | 0.988 | 0.940 | 0.947 | 0.05 | 2.49 | 10.09 | 16.78 |
| 50 | 0.989 | 0.989 | 0.981 | 0.980 | 0.00 | 0.85 | 10.00 | 12.42 |

**达到 P_D≥0.95 所需 RCS（对数插值）：**

| 门槛 | 600 m | 4 km | 比值 | 等效 dB |
|---|---:|---:|---:|---:|
| P_D ≥ 0.90 | 0.95 m² | 12.80 m² | 13.5× | +11.3 dB |
| P_D ≥ 0.95 | **2.81 m²** | **21.74 m²** | 7.7× | **+8.9 dB** |

600 m 曲线尾部饱和在 **0.989**，不是 1.000：DD 门捕获率上限约 0.98，属模型上限而非调度失败。

**协同增益在 600 m 上更早失效。** 600 m 时 K=8−K=0 的配对差：

| RCS | ΔP_D | 95% 区间 |
|---:|---:|---|
| 0.05 | +0.0140 | [−0.0017, +0.0297] |
| 0.1 | +0.0150 | [−0.0013, +0.0313] |
| 0.2 | +0.0220 | [+0.0047, +0.0393] |
| 1 | +0.0020 | [−0.0069, +0.0109] |
| 5 | 0.0000 | 不可估 |
| 20 | −0.0010 | [−0.0030, +0.0010] |
| 50 | 0.0000 | 不可估 |

4 km 的对应倒 U 峰值在 1 m²（+0.037），600 m 的峰值挪到 0.2 m²（+0.022）且更矮。
原因是紧凑部署本身就能拿到足够强的本地观测（RCS=5 m² 时观测数只有 11.01，4 km 要 33.12），
远程报告的边际价值随之消失。

## 3. 距离轴：RCS=0.2 m²、K=8、方法 `v1`

| 部署边长 (m) | 600 | 800 | 1000 | 1500 | 2000 | 3000 | 4000 |
|---|---:|---:|---:|---:|---:|---:|---:|
| P_D | 0.702 | 0.688 | 0.660 | 0.576 | 0.467 | 0.297 | 0.202 |
| 平均观测数 | 34.36 | 35.86 | 38.25 | 45.20 | 50.12 | 53.47 | 52.85 |
| 远程报告 | 6.75 | 7.05 | 7.19 | 7.75 | 7.90 | 7.89 | 7.71 |

与 4 km 基线的逐场景配对差（全部显著）：800 m +0.486、1000 m +0.458、1500 m +0.374、
2000 m +0.265、3000 m +0.095、600 m +0.500。

**距离的杀伤力取决于 RCS。** 同一条协议下把 RCS 换成论文工作点 50 m²（K=8 审计口径）：

| 部署边长 (m) | 600 | 4000 |
|---|---:|---:|
| P_D（RCS=0.2 m²） | 0.702 | 0.202 |
| P_D（RCS=50 m²） | 0.989 | 0.980 |
| 观测数（RCS=50 m²） | 10.00 | 12.42 |
| 远程报告（RCS=50 m²） | 0.00 | 0.85 |

弱目标在 2 km→4 km 掉 0.265；强目标在论文自带的几何扫描（2500→5500 m）只掉 0.060。
**在 50 m² 上距离几乎无关紧要；在 0.2 m² 上 600→4000 m 就是生死线。**
（论文 2500/4000/5500 m 三点来自 `results_target_local_v1/overview/geometry`，
生成于 09-14 旧判决阈值，绝对量级有约 0.002–0.007 的口径差，仅用于看趋势。）

## 4. 为什么只买到一半：机制分解

`target_gain = λ² σ / [(4π)³ d_iq² d_jq²]`（双程 R⁻⁴），而 `path_gain` 用
`detect.path_loss_exp = 2.0`（单程 R⁻²，承载直射干扰）。缩区域时两者一起变强：

| 区域 | UAV–目标斜距均值 | UAV–UAV 斜距均值 | 回波增益 | 直射干扰增强 | 净感知 SINR |
|---:|---:|---:|---:|---:|---:|
| 4000 m | 2109.3 m | 2098.6 m | 0.00 dB | 0.00 dB | **0.00 dB** |
| 3000 m | 1595.8 m | 1579.1 m | +4.09 | −1.73 | +2.36 |
| 2000 m | 1088.2 m | 1061.9 m | +9.67 | −4.15 | +5.52 |
| 1500 m | 839.4 m | 805.5 m | +13.50 | −5.88 | +7.62 |
| 1000 m | 598.3 m | 552.7 m | +18.70 | −8.31 | +10.38 |
| 800 m | 506.1 m | 453.7 m | +21.47 | −9.65 | +11.82 |
| **600 m** | 418.2 m | 357.2 m | **+24.93** | **−11.34** | **+13.58** |

探针给出的净增益 **+13.58 dB**，与实测点对点等效 RCS 增益在低 RCS 段一致
（RCS=0.05 m²、K=8 时 +13.10 dB；RCS=0.1 时 +12.31；RCS=1 时 +11.36）。

若只看雷达链路，斜距比 5.04 应给 4×10log₁₀(5.04) ≈ **+28 dB**。实际只兑现 +13.6 dB，
**差额 14 dB 全被同步增强的直射干扰吃掉**（UAV–UAV 斜距同时缩短 5.88 倍）。
这与 `V1_RCS_600M_REPORT.md` 的保守声明一致：结果属完整紧凑部署对照，
**不能归因为只改变雷达路径损耗**。

**点对点等效 RCS 增益随 RCS 单调收窄**（低 RCS 段 +13.1 dB，RCS=5 m² 时 +9.3 dB），
因为 4 km 曲线在高 P_D 端饱和，要匹配高 P_D 只能靠极端 RCS。因此：
**"600 m 值多少 dB"没有单一答案——按 P_D≥0.95 门槛算是 +8.9 dB，按逐点等效算是 +13.1 dB，
机制上界是 +13.6 dB。** 引用时必须说明评估方式。

## 5. 对论文的含义

1. **表 1 的 `RCS = 50 m²` 与 `4000 m × 4000 m` 都应当保留。** 在 50 m² 上距离几乎不敏感
   （600 m 相对 4 km 只涨 0.009），所以 4 km 的选取没有把结论压在悬崖边。
2. **真正的限定条件是 RCS，不是距离。** 正文 0.976/0.941/0.978 只在大型目标下成立；
   若审稿人追问泛化性，应给 RCS 轴而不是距离轴。
3. **600 m 会换掉问题本身。** 在 600 m + 50 m² 下报告数降到 0.00、观测降回 10.00、
   时延 0.00 ms，系统退化为纯本地感知；而在 600 m + 0.05–0.2 m² 下报告数升到 6.75–7.66
   但 P_D 仅 0.475–0.702。**"通信协同有用"与"绝对检测性能可接受"在当前模型里几乎不相交**，
   这是一个值得写进 limitation 的结构性观察。
4. **不建议把"距离更近"当作性能卖点。** 缩区域付出的是干扰几何与部署规模的代价，
   净收益只有约 13 dB，且高 RCS 端几乎为零。

## 6. 边界与未做项

- **单几何族、单种子**：本文件是探索性扫描（100 个共享场景），不是等效性/非劣性证明，
  也不构成"600 m 与 4 km 等价"的结论。
- **判决阈值漂移**：本文件所有 P_D 均来自当前代码（`simulate.evaluate_detection` 使用
  `calibrated_fused_threshold`），而论文主结果 CSV 是 09-14 旧判决生成的。
  同一配置下该漂移在 4 km/50 m² 处约 −0.007，**低 RCS 端未单独对照**。
- **审计口径 K=8 上限**：第 2、3 节表格中的报告数/时延受 K=8 硬上限约束；论文口径不设上限，
  低 RCS 段会多用约 2–5 条报告（时延 +2.6 ms 量级）。P_D 不受影响。
- **无起伏模型**：`detect.rcs_model="mean"`、`rcs_aspect_enable=False`。真实小目标带 Swerling
  起伏时最差情况只会更差，`rcs_model="swerling1"` 尚未纳入距离轴。
- **未做**：距离 × RCS 的二维完整网格（只做了两条一维切片 + 4 个论文口径判决点）；
  600 m 下 `joint` / `balanced` / `robust` 三个对照方法的距离轴；
  高度分布随距离收缩的联合敏感性（本文件按 600 m 扫描的约定**只改水平边长，保留原高度分布**）。

## 7. 复现命令

```bash
PY=E:/anaconda/3_11_python/python.exe

# 600 m 高 RCS 段 + 距离轴（审计口径，13 条件 × 100 场景）
$PY tools/audit_v1_geometry_wide.py --mc 100 --seed 10917 --workers 14 \
    --out results_v1_geometry_wide
$PY tools/summarize_v1_geometry_wide.py

# 机制分解（回波增益 vs 直射干扰）
$PY tools/probe_geometry_scaling.py --mc 100 --seed 10917

# 论文口径交叉验证（4 个判决点，逐点约 5 分钟）
FLAGS="--mode main --preset target-local-v1 --set detect.comm_error_model=gaussian_replacement \
  --mc 100 --seed 10917 --methods proposed_c2f_adaptive_pd \
  --paired-reference proposed_c2f_adaptive_pd --quiet --no-plots"
$PY run_isac_sim.py $FLAGS --set detect.target_rcs=0.2 --set geometry.area_xy=600  --out results_v1_distance_paperproto/d600_rcs0p2
$PY run_isac_sim.py $FLAGS --set detect.target_rcs=0.2 --set geometry.area_xy=4000 --out results_v1_distance_paperproto/d4000_rcs0p2
$PY run_isac_sim.py $FLAGS --set detect.target_rcs=5   --set geometry.area_xy=600  --out results_v1_distance_paperproto/d600_rcs5
$PY run_isac_sim.py $FLAGS --set detect.target_rcs=50  --set geometry.area_xy=4000 --out results_v1_distance_paperproto/d4000_rcs50
```

输出：`results_v1_geometry_wide/{protocol,summary}.json`、`geometry_summary.csv`（119 行）、
`geometry_contrasts.json`（含逐场景配对区间与等效 RCS 换算）；
`results_v1_distance_paperproto/*/main/{main.csv,trials.csv,config.json}`。
