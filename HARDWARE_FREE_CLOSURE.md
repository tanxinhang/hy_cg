# 不引入硬件增益假设：用算法闭合 500–800 m / RCS 0.05–0.2 的达标缺口

**日期**：2026-09-18　　**场景**：600 m / RCS 0.1 m² / `compact-small-uav` / M=15 / L=16
**问题**：proposed 要达标，到底需不需要"假设雷达链路多 15 dB 净硬件增益"？
**结论**：**不需要**。在 `G_hw = 0 dB` 的发布默认值下，协同算法单独即可把最差目标
P_D 推到达标线；κ 对消是**加分项**，不是必要条件。

---

## 0. 先定事实：`15 dB` 从来不在论文里

`tools/audit_v1_lowrcs_sweep.py` 的 `gain15` 是一个**探针对照臂**
（`radio.radar_net_gain_db = 15.0`），作用是分离"买分子"与"压分母"两条路线，
它**不是**任何已发表数字的前提。

| 出处 | 内容 | 状态 |
|---|---|---|
| `ReproducibilitySupplement.tex:60` | `$G^{\rm hw}=1$` | 明确声明硬件增益 = 1（0 dB） |
| `SystemModel.tex:110` | `G^{hw}_{ij}` 含天线增益与损耗 | 模型里有该项，但取值为 1 |
| 主比较 `config.json` | `radar_net_gain_db=None`、Tx/Rx 各 0 dBi、损耗 0 dB | 实际生效 `G_hw = 1.0` |

`G_hw` 的回落分支见 `isac_sim/model.py:41-54`：`net_gain_db` 为 `None` 时取
`tx + rx − system_loss = 0 + 0 − 0 = 0 dB`。

> 附：config 里另外三处 `15.0` 与硬件增益**无关**——`detect.rician_K_db`（莱斯
> 因子）、`prior.belief_sigma_vel_mps`（速度先验）、`scale.M`（UAV 数 = 15）。

---

## 1. 哪些改动算"新假设"？以发布门禁为准，不以直觉为准

`tools/check_release_identity.py` 把参数分成冻结（94 项）与可标定（16 项）。
判定"是不是引入新假设"的客观标准是它落在哪一类：

| 参数 | 门禁 | 性质 | 改它意味着什么 |
|---|---|---|---|
| `selector.tx_penalty` | **不受约束** | 纯算法（选择器的软代价） | 不算新假设 |
| `selector.max_tx_nodes` | **不受约束** | 纯算法（硬上限） | 不算新假设 |
| `selector.score_mode` | FROZEN | 方法口径 | 换方法 |
| `interference.direct_cancellation_db`（κ） | FROZEN | 物理对消**能力** | **新物理假设** |
| `interference.sense_gate_by_active_tx` | FROZEN | 协调**协议**能力 | **新系统假设** |
| `radio.radar_net_gain_db` | FROZEN | 硬件增益 | **新硬件假设** |
| `detect.n_looks` | FROZEN | CPI 资源 | 新资源假设 |

**这条表推翻了一个常见误解**：把 κ 从 40 dB 提到 60/80 dB **不是**"免费换硬件"。
κ 与 `G_hw` 在门禁里同级——它建模的是射频前端能把直射泄漏压到多低，属于**能力**
而非**算法**。用它替掉 15 dB 硬件，等于用一个物理假设换另一个，论文里照样要声明。

真正"零新假设"的只有两条：
1. `selector.tx_penalty` / `max_tx_nodes` —— 纯算法，门禁不管；
2. 协调所需的 `sense_gate_by_active_tx` —— 属于**协议假设**，必须显式声明
   （且模型未对协调信令开销与失效建模，见 §4）。

---

## 2. 三条算法路径作用于 SINR 的哪个位置

`γ = signal / ((n0 + residual + eps)(1 + INR))`，三条路径分别攻击不同项：

| 路径 | 作用项 | 机制 | 门禁 |
|---|---|---|---|
| **协同静默**（`tx_penalty` + 门控） | 分母 `residual` | 不参与观测的节点在 CPI 内不辐射，残差下降 | 门控 FROZEN / 代价 free |
| **对消**（`κ`） | 分母 `residual` | 把自身直射泄漏再压 κ dB | FROZEN |
| **均衡选择**（`maxmin`） | 重分配 | 把预算挪给最弱目标，不增加总预算 | free |
| ~~硬件增益~~ `G_hw` | 分子 `signal` | 抬高回波 | FROZEN（本次**不采用**） |

前两条攻击同一个 `residual`，因此**彼此是替代品、不可叠加计功**——这一条在
400 m 工作点已由 MC 证实，本场景需重新测（§3 的 2×2 矩阵就是为此设计）。

---

## 3. 实验设计：G_hw × κ × 协同 的 2×2(+1) 矩阵

工具：`tools/run_coordination_experiment.py`（唯一接通
`isac_sim.coordination.select_with_coordination` 的入口，把掩码反馈进两阶段选择
直到不动点）。本次新增 `--hardware-gain-db` 与 `--kappa-dc` 两个维度。

每个单元格跑 40 trials × 4 seeds = 160 trials，三臂**配对**同 trial：

| 臂 | 选择阶段 | 掩码 | 含义 |
|---|---|---|---|
| `uncoordinated` | 无代价，选择看不到掩码 | 无 | 基线 |
| `mask_only` | 同上 | 事后施加 | 分离"静默"与"重选" |
| `sparse` | 带 `tx_penalty` 的选择 + 掩码反馈 | 有 | **真正的协调** |

统计口径：论文相关统计量是**跨种子最差值**（worst seed），不是均值。

> ⚠️ **口径隔离**：本工具与主比较（`run_isac_sim.py --mode main`）**数字不可直接
> 比较**——检测器调用、报告上限、seed 集都不同。同一张表内只做臂间与单元格间比较。

---

## 4. 声明为假设的部分（写论文时必须保留）

1. **协调信令**：模型未计入节点间协商掩码的开销，也未建模协商失效。
2. **静默可行性**：假设非照射机可在感知 CPI 内不辐射而不影响其通信任务。
3. **掩码与选择时序**：当前掩码在**选择之后**生成并在不动点迭代中反馈
   （`select_with_coordination`），迭代式**不保证收敛**，已加环路检测；
   未收敛的 trial 会显式告警，不得静默计入。

---

## 5. 实测：600 m / RCS 0.1，G_hw × κ × 协同 全矩阵

40 trials × 4 seeds = 160 trials/单元格，三臂配对同 trial。
统计量：**跨种子最差 / 均值 / 标准差**（论文相关统计量是最差值，不是均值）。

| G_hw | κ | uncoordinated | mask_only | **sparse（协同）** |
|---|---|---|---|---|
| **0 dB** | 40 dB | 0.8500 / 0.8625 / 0.0125 | 0.8750 / 0.8938 / 0.0207 | **0.9500 / 0.9500 / 0.0000** |
| **0 dB** | 60 dB | 0.9500 / 0.9563 / 0.0108 | 0.9500 / 0.9625 / 0.0125 | **0.9250 / 0.9500 / 0.0250** |
| **+5 dB** | 40 dB | 0.9000 / 0.9312 / 0.0207 | 0.9750 / 0.9813 / 0.0108 | **0.9750 / 0.9813 / 0.0108** |
| **+15 dB** | 40 dB | 1.0000 / 1.0000 / 0.0000 | 1.0000 / 1.0000 / 0.0000 | **1.0000 / 1.0000 / 0.0000** |

### 四条结论

1. **零新假设即可闭合。**
   在 `G_hw = 0 dB`、κ 保持预设 40 dB、不动任何 FROZEN 项的前提下，协同把最差
   目标 P_D 从 0.8500 推到 **0.9500**：四个种子全部改善
   （+0.075 / +0.10 / +0.075 / +0.10），跨种子标准差 **0.0125 → 0.0000**。
   这是最坏情况收敛，不是均值提升。
2. **+15 dB 硬件是过量的，且会毁掉可辨识性。**
   该档位下**三臂全部 1.0000 饱和**——连 `uncoordinated` 都是 1.0，算法差异被完全
   抹平。它不是一个"优化手段"，而是一个把问题变简单的假设。
3. **等效换算：协同算法 ≈ +10.0 dB 硬件净增益。**
   纯硬件（无协同）要达到协同在 `G_hw=0` 拿到的 0.9500，需要在 `uncoordinated`
   曲线上插值到约 **+10 dB**（0 dB→0.850、5 dB→0.900、15 dB→1.000）。
   即协同免费提供了原先要靠 10 dB 硬件才买得到的东西。
   （`mask_only` 只等效约 +2.5 dB——**真正值钱的是"重选"，不是"事后静默"**。）
4. **⚠️ κ 与协同是替代品，不可叠加计功。**
   κ 从 40 提到 60 时，`uncoordinated` 自己就到了 0.9500，但**协同反而降到 0.9250**
   （per-seed 增量 `[+0.025, −0.050, +0.025, −0.025]`，不再一致改善），
   跨种子标准差从 0.0000 恶化到 0.0250。原因：κ 已把残差压近噪声地板，
   协同静默没有分母可压，而 `tx_penalty` 扭曲选择的代价（链路 15→10.5、
   #TX 15→1.5）反而显现。**想同时用两条路的人会两头落空。**

### 800 m / RCS 0.05（更难的工作点）

| G_hw | κ | uncoordinated | mask_only | **sparse（协同）** |
|---|---|---|---|---|
| 0 dB | 40 dB | 0.6500 / 0.7000 / 0.0354 | 0.7000 / 0.7312 / 0.0207 | **0.7500 / 0.7812 / 0.0325** |
| 0 dB | 60 dB | 0.9000 / 0.9125 / 0.0125 | 0.9000 / 0.9125 / 0.0125 | **0.8500 / 0.8750 / 0.0306** |

- κ 40 → 60 单独给 **+0.25**（0.6500 → 0.9000），说明该点**确实是干扰受限**。
- 但协同的作用**随 κ 翻号**：κ=40 时 **+0.10**，κ=60 时 **−0.05**
  （per-seed `[+0.025, −0.075, −0.050, −0.050]`，三个种子变差）。
- 即便 κ=60 + 无协同的 0.9000，仍差 0.05 达标。

### ⚠️ 机制规律（两个工作点独立复现）

**协同只在"干扰受限"时有效；一旦干扰被对消掉，它的净作用转为负。**

| 工作点 | κ=40（干扰受限） | κ=60（近噪声受限） |
|---|---|---|
| 600 m / 0.1 | **+0.100**（0.850→0.950） | **−0.025**（0.950→0.925） |
| 800 m / 0.05 | **+0.100**（0.650→0.750） | **−0.050**（0.900→0.850） |

两个工作点、两个量级的最差 P_D，协同的符号**完全一致地**随 κ 翻号。
原因：协同攻击的是分母里的 `residual`；κ 已经把残差压近噪声地板时，
没有分母可压，而 `tx_penalty` 扭曲选择的代价（链路被裁剪、#TX 被压缩）
就暴露出来了。**"对消 + 协同"不是 1+1，而是互斥。**

---

## 6. 发布口径（MC=1000，`run_isac_sim --mode main`）的 κ 扫描

前面 §5 的 0.9500 来自**协调工具口径**（`target-local-v1` + `evaluate_detection`、
L=16、40 trials × 4 seeds）。它与论文主比较**不是同一口径**，不能直接和 0.7169 比。
本节用论文主比较口径重测 κ，作为可比证据。

600 m / RCS 0.1 / compact-small-uav / MC=1000 / G_hw = 0 dB：

| 方法 | κ | P_D | 最差 P_D | 比特 | 时延 ms | 观测数 | P_D/kbit |
|---|---|---|---|---|---|---|---|
| **proposed** | 40 | 0.7169 | 0.6990 | 4142 | 6.903 | 27.96 | 0.173 |
| **proposed** | **60** | **0.8848** | **0.8700** | **1248** | **2.080** | 14.82 | **0.709** |
| **proposed** | 80 | 0.8892 | 0.8760 | 1224 | 2.039 | 14.58 | 0.727 |
| exact_marginal | 40 | 0.6860 | 0.6610 | 7589 | 12.649 | 26.59 | 0.090 |
| exact_marginal | 60 | 0.8442 | 0.8280 | 4948 | 8.246 | 14.11 | 0.171 |
| sense_sinr | 40 | 0.7429 | 0.7160 | 8612 | 14.354 | 27.96 | 0.086 |
| sense_sinr | 60 | 0.9026 | 0.8880 | 4251 | 7.085 | 14.82 | 0.212 |

**发布方法 `proposed_c2f_adaptive_pd` 的优势（同 run 内边际差，口径安全）**：

| κ | P_D | vs exact_marginal | vs sense_sinr |
|---|---|---|---|
| 40（预设） | 0.7169 | +0.0309 | −0.0260 |
| 60 | 0.8848 | **+0.0406** | −0.0178 |
| 80 | 0.8892 | +0.0389 | −0.0169 |
| 40 + `tx_penalty` 但**门控关** | 0.7025 | +0.0203 | −0.0456 |

> **⚠️ 已更正**：初稿此表误把 κ=40 行的 `proposed_c2f_adaptive_pd` 与 κ=60/80 行
> 的**配对参考方法** `proposed_c2f` 并列（19 方法跑里 harness 指定 `proposed_c2f`
> 为配对基准，P_D 0.8671/0.8728，低于发布方法）。同法名重算后结论**方向反转**。
> CSV 的 `paired_*_delta` 列在 19 方法模式下指的是 `proposed_c2f`，**不可**
> 与 3 方法模式的 `adaptive_pd` 配对值混用。

### 四条结论（发布口径）

1. **κ=60 是膝点。** κ 40→60 买到 **+0.168 P_D**（0.7169 → 0.8848），最差目标
   +0.171（0.699 → 0.870）；κ 60→80 只再买 +0.0044。与协调口径的 rinr 走势一致
   （8.12 → 0.34 → 0.26，60 已触地板）。**80 dB 是浪费，论文若用 κ 应取 60。**
2. **κ 同时是通信效率杠杆，量级比对 P_D 还大。** 开销 **4142 → 1248 bit（−70%）**、
   时延 6.903 → 2.080 ms（−70%）、观测数 27.96 → 14.82（SINR 变好后满足检测门限
   所需链路减半）。P_D/kbit 从 0.173 升到 **0.709（×4.1）**。
   ⇒ **κ 主要买的是通信效率，不是检测率。**
3. **κ 不压缩 proposed 的优势**（与 §5 协调口径的直觉相反）。同法名比：
   vs exact_marginal **+0.0309 → +0.0406**，反而略增。
4. **κ 也不会加剧 sense_sinr 反转**：劣势从 −0.0260 **收窄到 −0.0178**。
   但**符号始终是负的**——proposed 在纯 P_D 上仍输给 sense_sinr，
   κ 只能缩小差距、不能翻盘。⇒ 卖点仍只能是**通信效率**
   （κ=60 时 bits 1248 vs sense_sinr 4251，少 71%，而 P_D 只差 0.018）。

> **口径警告**：本节数字**只能和同口径的 κ=40 行比**（0.7169）。
> 不能和 §5 的 0.9500 比——后者是协调工具口径（L=16、MC 160、不同评估入口）。

---

## 7. 推荐口径（以及为什么）

### 7.1 三个候选（**协调工具口径**，彼此可比）

| 选项 | G_hw | κ | 协同 | 最差 P_D | 引入的假设 |
|---|---|---|---|---|---|
| **推荐** | **0 dB** | 40 dB（预设） | **开** | **0.9500** | **协议假设**：门控打开（FROZEN）+ `tx_penalty`（free） |
| 备选 A | 0 dB | 60 dB | 关 | 0.9500 | **物理能力假设**：对消 +20 dB（FROZEN） |
| 备选 B | 0 dB | 60 dB | 开 | 0.9250 | 两者都要，且**比单独用更差**（互斥） |
| 不推荐 | +15 dB | 40 dB | 任意 | 1.0000 | **硬件假设** + 饱和导致算法不可辨识 |

> **上一版把"推荐"标成"无新假设"是错的，已更正。**
> `interference.sense_gate_by_active_tx` 在 `FROZEN_PATHS` 内（§1），
> 打开它等于假设"节点可被调度为感知期静默"——这是一个**协议/半双工能力**假设，
> 不是免费午餐。它比 15 dB 天线增益弱得多（不需要任何射频性能提升，只需要调度），
> 但在门禁里和 κ、`G_hw` 同属 FROZEN，论文里**必须作为假设声明**。
>
> 真正**零 FROZEN 改动**的只有 `selector.tx_penalty` / `max_tx_nodes` 两个键；
> 但门控关闭时掩码根本不生成，`tx_penalty` 是空转的（见 §7.3 的失败对照）。
> ⇒ **"完全不动冻结项 + 达标"在当前代码里无解。**

**推荐组合满足三条**：达到 0.95；不引入任何**射频性能**假设（G_hw 仍为 0 dB、
κ 仍为预设 40 dB）；跨种子标准差为 0（确定性达标，而非"平均达标"）。

#### 0.9500 到底有没有吃硬件增益？——没有，三条证据

1. **配置重建**：`apply_preset(default_config(), "target-local-v1")` 后
   `radar_net_gain_db=None`、收发各 0 dBi、`radar_system_loss_db=0` ⇒
   `radar_hardware_gain` = 1.0（线性）= **0.0 dB**；`direct_cancellation_db` = 40.0。
2. **时间线**：该次运行早于 `--hardware-gain-db/--kappa-dc` 两个参数加入，
   命令行里没有它们，只能取预设默认。
3. **实证单调性（最硬的一条）**：600 m/0.1 下 `uncoordinated` 臂的最差 P_D 在
   G_hw = 0 / 5 / 15 dB 分别为 **0.8500 / 0.9000 / 1.0000**（15 dB 档三臂全饱和）。
   该次运行的 uncoordinated = **0.8500** ⇒ 落在 0 dB 档。

且 `n_looks = 16` 与发布 config 相同 ⇒ **G_hw、κ、n_looks 三项物理假设与发布口径
完全一致**。0.9500 与发布口径的 0.7169 之差**不来自硬件**，只来自评估入口
（`evaluate_detection` + `select_c2f_adaptive` vs `--mode main` 的
`proposed_c2f_adaptive_pd`）、MC 规模（160 vs 1000）以及被测量的量（掩码是否反馈）。

> 顺带订正：`SYSTEM_AUDIT_2026-09-17.md:257` 称 `select_with_coordination` 默认
> 选择器为 `lagrangian_selector`，**已过时**。`coordination.py:220` 现在是
> `select_fn = _release_selector if selector is None else selector`，默认走发布方法。
> 因此三臂共用同一个 `select_c2f_adaptive`，**+0.10 可干净归因于协同，不是换了算法**。

协同的代价是辐射节点数（#TX 15 → 2.1）与链路数（15 → 11.5），这本身也是卖点：
**用更少的辐射换更高的最坏情况检测率**。

### 但 800 m / 0.05 仍未闭合

| 组合 | 最差 P_D | 达标 | 新假设 |
|---|---|---|---|
| κ=40 + 协同（推荐口径） | 0.7500 | ❌ | 无 |
| κ=60，无协同 | 0.9000 | ❌ | 有（κ） |
| κ=60 + 协同 | 0.8500 | ❌ | 有（κ，且互斥） |

⇒ **分母类算法（对消 + 协同）在 800 m / RCS 0.05 吃不下 0.20 的缺口。**
纯算法能闭合的是 **600 m / RCS 0.1**。若主工作点必须落在 800 m / 0.05，
则必须补**分子类**手段（looks / 感知处理增益 / 几何压缩），那是另一条独立账。

### 7.2 800 m / RCS 0.05 仍未闭合

| 组合 | 最差 P_D | 达标 | 引入的假设 |
|---|---|---|---|
| κ=40 + 协同（推荐口径） | 0.7500 | ❌ | 协议（门控） |
| κ=60，无协同 | 0.9000 | ❌ | 物理（对消 +20 dB） |
| κ=60 + 协同 | 0.8500 | ❌ | 两者，且互斥 |

⇒ **分母类算法（对消 + 协同）在 800 m / RCS 0.05 吃不下 0.20 的缺口。**
纯算法能闭合的是 **600 m / RCS 0.1**。若主工作点必须落在 800 m / 0.05，
则必须补**分子类**手段（looks / 感知处理增益 / 几何压缩），那是另一条独立账。

### 7.3 阴性对照（门控关 + `tx_penalty=0.2`）—— 证明惩罚项 ≠ 协同

| 指标（`proposed_c2f_adaptive_pd`，κ=40，MC=1000） | 基线 | 加惩罚、门控关 | 变化 |
|---|---|---|---|
| P_D | 0.7169 | **0.7025** | **−0.0144** |
| 最差目标 P_D | 0.6990 | **0.6860** | **−0.0130** |
| 开销 | 4142 bit | **5425 bit** | **+31%（变差）** |
| 时延 | 6.903 ms | 9.041 ms | +31% |
| 观测数 | 27.96 | 28.39 | +0.43 |
| vs exact_marginal | +0.0309 | +0.0203 | 优势缩水 |
| vs sense_sinr | −0.0260 | **−0.0456** | **劣势扩大 1.8 倍** |

**结论：门控关闭时 `tx_penalty` 是纯损害**——六个指标全变差，开销不降反升。
原因：掩码不存在 ⇒ 惩罚项没有物理对象，只在选择目标函数里引入了一个
无信息的偏置，把选择推离 SINR 最优解，于是需要**更多**链路去补检测率。

这排除了一种偷懒写法：不能声称"我们加了干扰感知代价项"就算做了协同。
**协同 = 门控（掩码）+ 迭代反馈（`select_with_coordination`）**，
代价项只是让选择去利用掩码，本身不能创造掩码。

> 记录一次误判：该臂曾因目录里只看到 `config.json`（运行中状态）被误判为"崩了
> 无产出"，实际它跑满 1h42m 并完整落盘。判断后台任务是否失败**必须等通知**，
> 不能凭目录快照。

### 7.4 ⚠️ 决定性发现：协同在发布口径里**没有接线**

`results_lowrcs_main_gate`（κ=40 + `sense_gate_by_active_tx=True` + `tx_penalty=0.2`）
与 `results_lowrcs_main_sparse`（κ=40 + `tx_penalty=0.2`、**门控关**）对比：

> **234 个 CSV 字段逐位相同**，唯一的 21 处差异是配对差列在 3 方法模式下为空。
> 即：**把门控打开，物理结果一个比特都没变。**

config 已核对确认真开了（`sense_gate_by_active_tx = True`），不是 `--set` 没生效。

**原因（代码级）**：

1. `model.py:642` — `gate_echo = bool(ic.sense_gate_by_active_tx and active_tx_mask is not None)`。
   门控与 `active_tx_mask is not None` 是**与**关系；掩码为 `None` 时门控必然失效。
2. 发布路径里 `compute_link_tables` 只在两处传掩码：
   `simulate.py:584`（slot-MAC 的**通信**干扰，掩码 = 该时隙的报告机）
   与 `simulate.py:882`（报告期重建）——**都不是感知协调掩码**。
3. 感知协调掩码只由 `isac_sim/coordination.py::select_with_coordination` 产生，
   而该函数**只被 `tools/*` 调用**（`run_coordination_experiment.py`、
   `probe_coordination_*`、`audit_coordination_*`、`probe_price_stage`），
   `run_isac_sim.py` 与 `simulate.py` **从未调用**。

⇒ **`--set interference.sense_gate_by_active_tx=true` 在 `--mode main` 下是一个静默空转的开关。**
它不会报错、不会告警，只是什么都不做。

### 这对结论意味着什么

| 说法 | 状态 |
|---|---|
| 协同在 600 m/0.1 达到最差 0.9500 | ✅ 真实，但**仅在协调工具口径** |
| 该结果可在论文主比较里复现 | ❌ **不能**，发布路径没有接线 |
| 只靠 `--set` 打开发布口径的协同 | ❌ **不行**，见上 |
| 加 `tx_penalty` 但不开门控 | ❌ 纯损害（§7.3） |

**要在论文主比较里写协同，必须先做一件工程活**：把 `select_with_coordination`
（或等价的掩码构造）接进 `run_isac_sim --mode main` 的感知链路表构建，
并确保它走发布的选择器与评估入口。在那之前，§5 的 0.9500 **只能作为"协调
机制的独立验证"引用，不能与 0.7169 / 0.8848 并列进同一张主结果表**。

> 这也是 §7.1 里"推荐组合"目前**无法在论文口径落地**的唯一原因——不是它不成立，
> 是发布代码里没有这条路。

### 7.5 实验账本

| 单元格 | 口径 | 状态 |
|---|---|---|
| κ=40 / 60 / 80 × {base, maxmin, looks128, gain15} | 扫掠（MC=100） | ✅ |
| 600 m/0.1 × {G_hw 0,5,15} × {κ 40,60} × 3 臂 | 协调工具（MC=160） | ✅ |
| 800 m/0.05 × {κ 40,60} × 3 臂 | 协调工具（MC=160） | ✅ |
| κ=40 基线（proposed / exact_marginal / sense_sinr） | 发布（MC=1000） | ✅ |
| κ=60 / κ=80 全 19 方法 | 发布（MC=1000） | ✅ |
| 只加 `tx_penalty` 不开门控（阴性对照） | 发布（MC=1000） | ✅ 见 §7.3 |
| κ=40 + 门控 + `tx_penalty`（发布口径协同） | 发布（MC=1000） | ✅ 但与门控关**逐位相同**，见 §7.4 |


> 发布口径 κ=60/80 跑的是**全 19 方法**（未传 `--methods`），而 κ=40 基线只跑 3 个。
> 两者 trial 集相同、可直接按方法名比对，但**配对比对的"proposed"标识不同**
> （基线 = `proposed_c2f_adaptive_pd`，19 方法跑 = `proposed_c2f`），
> 因此 §6 的配对差一律按方法名直读，不采用 CSV 里的 `paired_proposed_delta_*` 列。
