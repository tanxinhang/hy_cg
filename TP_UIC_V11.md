# TP-UIC V1.1 — 换的是检测器，不是消除器

> ⚠️ **本文件的第五节表格已被 2026-09-19 的回波生成修正取代，不可再直接引用。**
> 当时 `build_observation` 把单位模随机系数赋给了**每个目标的全部列**（含
> fractional-DD 切向列），即每个目标实际写入 **3 条**散射回波，能量比物理模型高
> **+12.39 dB**（17.3×，`A_true @ alpha` 实算）。检测器的 nuisance 块只取中心列，
> 于是 H0 里留有它声明不存在的东西 ⇒ §5.6 记的「解析门限 P_FA = 0.40–0.94」有相当一部分
> 是**生成模型错**，不只是 `C_res` 标定错。修正后的复跑、以及三条 P0 修复的完整记录见
> **`TP_UIC_V12.md`**。本文件其余各节（检测器定义、可辨识性分析与结论方向）仍然有效，
> 但凡是**数字**都要以 V1.2 为准。

> 本版**不动 V1 的消除器**（`isac_sim/cancellation.py` 一行未改），只把它输出的
> `(r, C_res, η_surv)` 接到一个**目标条件化的白化 GLRT** 上，并补一次**连续 DD 可辨识性审计**。
> V1 的结论（保护代价昂贵、参考预算只买估计项、保留项主导残余）全部保留。
> 新增文件：`isac_sim/cancellation_glrt.py`（消费侧）、`tools/run_tp_uic_v11.py`、
> `tools/summarise_tpuic_v11.py`、`tests/test_cancellation_glrt.py`。

---

## 一、本版要回答的三个问题（决策已定）

用户给的决策（原文口径）：

- **P_D 口径**：正式采用 **target-conditioned whitened GLRT**；**单目标场景只作机制验证**；
  **不抬 RCS**、**不把单目标当主结果**。
- **DD**：**暂不改** `N` / `L` / 带宽 / CPI；**先做 continuous-DD 可辨识性审计**，
  只有当「完美对消后仍不可辨识」成立时，才把「抬物理分辨率」提上桌。
- **`interference_tangent_order = 1` 继续关闭**（先验参数化是错的，见第十节）。
- **不宣称「TP-UIC 优于固定 40 dB」**——40 dB 是**理想化残余模型**，不是可比的算法臂。
- **Stage-1 == No-IC 不是 bug**，是理论结果（保守对消 / 安全初值）；提出的方法是 **TP-UIC full**。

V1.1 必须回答：

| # | 问题 | 本版结论落在 |
| --- | --- | --- |
| Q1 | TP-UIC 相对 plain LS 是否**减少弱目标损伤**？ | 5.2 / 6 |
| Q2 | 这种保留是否**在正确检测器下转成 P_D**？ | 5.1 / 5.2 / 6 |
| Q3 | 残余失败来自**对消（IC）**还是**多目标不可辨识**？ | 4.2 / 4.3 / 5.4 / 6 |

---

## 二、为什么 V1 的检测器问错了问题

V1 用**整个受保护块**的能量打分：

```
T = ‖U_w^H r‖² / rank(U_w)
```

这在 600 m 场景里问的是「**这个 DD 斑块里有没有能量**」。而十个散射体共享若干个 delay bin，
把被测目标 `q` 拿掉之后，另外九个回波**仍然落在同一个子空间里**，于是 `T(H1) ≈ T(H0)`
是**构造出来的**——实测只差 `+0.06 ~ +0.52 dB`，而物理推算应有 `+17 dB`。
**坏的环节是检测器，不是对消器。**

V1 已经证明了这个诊断（第六节第 4 条），并把「P_D 不进主表」作为临时口径。V1.1 就是来换这个口径的。

---

## 三、V1.1 的检测器：target-conditioned 白化 GLRT

### 3.1 形式

对被测目标 `q`，把零假设/备择假设写成**条件于其余目标**的形式：

```
H0,q:  r = A_-q a_-q + v
H1,q:  r = A_q a_q + A_-q a_-q + v,      v ~ CN(0, C_res)
```

其中 `(r, C_res)` 由消除器**交出**。白化、投影掉其它目标的流形、只检验 `q` 剩下的那部分：

```
r~    = C_res^{-1/2} r
A~_x  = C_res^{-1/2} T_e A_x
P_-q  = I - A~_-q A~_-q^†                （A~_-q 的正交补投影）
B_q   = P_-q C_res^{-1/2} T_e A_q
T_q   = r~^H P_{B_q} r~                                     (1)
```

于是正式命题从「这个斑块是否被占」变成「**给定另外九个目标和残余干扰，目标 `q` 是否仍提供证据**」。
论文可以声明的链条是：

```
TP-UIC  →  (r, C_res, η_surv)  →  白化  →  干扰投影  →  T_q
```

即：消除器**不只是减功率，它出口了检测器需要的统计量**，两者是**协同设计**而不是事后拼接。

### 3.2 残余协方差 `C_res`

`T_d` 是作用在直连场上的算子、`T_e` 是作用在回波上的算子，则

```
r = T_d x + T_e s + (I - F) n - c + (M X) e
```

`e` 是**基于参考**的估计的系数误差。估计器臂 `T_e = T_d = I - F`；oracle 臂 `T_e = I`（回波原样通过）、
`T_d = direct_gain·I`。把直连系数当高斯、对角方差 `R_diag`：

```
C_res = sigma² I + T_d X R_diag X^H T_d^H + (M X) C_h (M X)^H / n_cpi
      = sigma² I + U S U^H,     U = [B, T_d X diag(sqrt R), M X L]        (2)
```

**恒等 + 低秩**，所以在 `span(U)` 的约 60×60 限制上做特征分解即可精确施加 `C_res^{-1/2}`，
**永不形成 `K×K` 矩阵**——这才让 4096 bin 上的白化是可承受的。
单测 `test_residual_covariance_matches_its_own_generative_model` 用蒙特卡洛把式(2) 的低秩特征表示钉住；
`test_whitener_satisfies_the_defining_identity` 钉住 `C_res^{-1/2} C_res C_res^{-1/2} = I`。

> **`sigma² I` 地板是一个必须写明的建模决策。** 一个在**自己将要清洗的那份观测**上拟合系数的
> 消除器，在 `span(M X)` 上也把噪声一起去掉了，所以它的残余协方差在那个 14 维子空间上是**奇异**的
> ——而检测器不能往零空间里白化。`cancellation.n_cpi` 本来就声明系数来自参考预算，那个读法给出
> **独立的系数误差噪声**、一个真正满秩的地板，并让**参考预算第一次出现在检测度量里**。
> V1 实测参考预算买不到对消**深度**（保留项主导）；在这里它买到的是**标定**——而这正是检测器消费的那一项。

### 3.3 门限与自由度的口径

复数方向贡献 2 个实自由度，所以 `dof = 2·rank(B_q)` 恒为偶数，`T_q ~ (1/2) χ²_dof`：

```
threshold = 0.5 * chi2_quantile(1 - p_fa, dof)              (3)
```

自由度偶数 ⇒ CDF 是有限和（Erlang）、分位数是安全二分，**这个模块因此只依赖 numpy**
（`scipy` 只在测试里用来交叉核对）。一个「只是为了定门限」就引入 scipy 的检测器，会给发布路径
带来它本来不需要的依赖。

单测 `test_threshold_achieves_the_requested_false_alarm_rate` 在**白噪声**上把
`p_fa = 0.05`、已知 `dof` 的经验虚警率钉在 `[0.04, 0.06]`——它同时锁住**尾、尺度、自由度**三件事。
（为什么需要它：第七节记的两个门限 bug 都**没有**被任何逻辑测试抓到。）

### 3.4 每个臂都是仿射映射（探针复原）

`cancellation.py` 的所有臂都形如

```
removed = F y + c,     F = B s B^H,    rank(F) <= d  或  d + |candidates|
```

因为 `protected_map` / `joint_refine` 解的小正规方程右端**对 `y` 线性**。两个臂不是估计器
（`fixed_kappa`、`perfect_channel` 减的是**已知**向量），所以 `F = 0`，直连场只是被缩放。

`F` 用**探针**恢复：把该映射作用在张成 range 与 row space 的一组正交基上，得到的小矩阵**精确**
复现它——探针误差是**被测出来并断言**的，不是假定的。
`test_every_arm_is_reproduced_by_its_low_rank_form` 把探针结果与 `cancellation_arms` 本身对照，
这就是 V1 若被改动时本文件仍然诚实的保证。

---

## 四、可辨识性：测出来，不是断言出来的

### 4.1 定义与两个端点

```
rho_c = ‖P_-q C^{-1/2} a_c‖² / ‖C^{-1/2} a_c‖²   ∈ [0, 1]        (4)
```

它是模板**自身**白化匹配滤波能量在干扰投影后**存活的比例**，`1 - rho` 就是被掩掉的比例。
两个端点由单测钉住：模板落在其它目标张成里 ⇒ `rho = 0`（`test_rho_is_zero_for_a_template_inside_the_nuisance`）；
与它正交 ⇒ `rho = 1`（`test_rho_is_one_when_there_is_no_nuisance`）。
`xi_rel_q` 是矩阵版本（`λ_min(G_keep^{-1/2} G_full G_keep^{-1/2})`），`g_q` 用能量加权版
`rho_weighted`。**`rho` 与信号幅度无关**：单测
`test_glrt_is_invariant_to_the_nuisance_amplitudes` 钉住这一点——所以「抬 RCS」不会改变 Q3 的答案，
这正是用户决定不抬 RCS 的定量依据。

### 4.2 patch-covering：掩码曲线的**形状**就是机理

`masking_curve` 按**距离**排序，依次把最近的 `n` 个同场景目标当干扰，测 `rho`：

- 若是**逐个邻居**把 `rho` 压掉 ⇒ 换观测几何就能救；
- 若是**并集覆盖**（随 `n` 渐进塌陷）⇒ 绑定的是 DD 面上**共位散射体的数目**，只能靠物理分辨率。

实测是后者（表 5.3）：`n=0 → 1.00`，加第一个邻居掉到 `0.38`，`n=9 → 0.05`，而且是**渐进**的。
十个散射体在 DD 面上占约 **7.5 bin** 的斑块，邻近模板（含一阶切向）的**并集把被测模板盖住**。
**这解释了 V1 的 `T(H1)≈T(H0)`，并且它是场景的结论而不是代码缺陷。**

### 4.3 continuous-DD 审计

`identifiability_audit` 在三个层次上测 `rho`：**在格**、**加一阶/二阶切向流形**、
以及把被测模板**挪到离格**（分数 `Δk/Δl`）。用户给的判据是
`xi_q = λ_min(B_q^H B_q)` 或 `g_q`——本版两个都报，且**离格**是关键一项：

- 分数偏移（±0.4 bin）**救不回来**：因为切向流形本来就覆盖那点邻域；
- 把偏移加到**数个 bin**（5 bin）才把 `rho` 拉回 `0.95`。

所以判据的答案是：**完美对消后仍然不可辨识**（`perfect_channel` 臂的 `rho` 也是 ~1e-23），
⇒ 失败**不是 IC 不够**。同时「挪几个 bin 就能恢复」证明它是**分辨率受限**的，而不是「物理上不可分」。
按用户的决策，**抬分辨率这一动作本版不做**，只把结论和判据留在这里。

---

## 五、结果

> 口径：`paper-canonical`，`area = 600 m`，`RCS = 0.1 m²`，`seed = 2026`，
> receiver rule = `median`，`p_fa = 0.05`，`n_cpi = 1`，`max_protected_targets = 3`，
> `cancellation.enable = True`。字典 = `belief`（含 belief 误差），另有 `truth + 无 belief 误差` 对照臂。
> 所有臂都跑 **两趟支撑门限**（与 `run_tp_uic_v1.py` 同一约定），与 V1 可比。
> 本表实际的生产配置：主实验 `--trials 16 --audit-trials 5 --single-trials 40`，
> 对照 `--trials 10 --audit-trials 4 --single-trials 20`（几何/掩码类量对 trial 数不敏感，
> 故取小；`AUC` 的小样本偏差见 5.6 的说明）。`--trials 40 --audit-trials 12` 是文档默认值。

### 5.1 主场景（10 目标）matched H1/H0 — belief 字典

| arm | C_IC (dB) | η_surv | ρ_w | T_H1 | thr | P_D | P_FA | **AUC** | ncp_best |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| no-IC | 0.00 | 1.000 | 0.0080 | 51.72 | 20.67 | 0.875 | 0.812 | **0.543** | 0.039 |
| fixed 40 dB | 40.00 | 1.000 | 0.0109 | 107.17 | 20.67 | 0.938 | 0.875 | **0.520** | 0.045 |
| plain LS | 35.46 | 0.756 | 0.0073 | 48.90 | 20.67 | 0.875 | 0.938 | **0.551** | 0.035 |
| ridge LS | 35.49 | 0.757 | 0.0077 | 51.23 | 20.67 | 0.875 | 0.875 | **0.531** | 0.037 |
| protected LS | 8.02 | 0.805 | 0.0063 | 41.27 | 20.67 | 0.938 | 0.750 | **0.520** | 0.029 |
| TP-UIC stage-1 | 8.02 | 0.808 | 0.0063 | 41.38 | 20.67 | 0.812 | 0.688 | **0.508** | 0.029 |
| **TP-UIC full** | 29.02 | **0.938** | 0.0081 | 49.63 | 20.67 | 0.875 | 0.875 | **0.531** | 0.038 |
| perfect channel | inf | 1.000 | 0.0109 | 113.23 | 20.67 | 0.938 | 0.875 | **0.512** | 0.045 |

`ncp_best` 是「单位幅度回波能给出的期望偏置」——本表**每一臂都 ~0.03–0.045**，即
**一个单位幅度目标在这个子空间里几乎是不可见的**。`AUC` 是**无门限**的可分性
（`P(T_H1 > T_H0)` 在所有配对上取平均）：**每一臂都落在 0.51–0.55**，`perfect_channel` 也只有 0.512。

### 5.2 单目标机制验证（一个回波、一个保护基、无干扰）

| arm | C_IC (dB) | η_surv | T_H1 | thr | P_D (belief) | ncp_best (belief) | ncp_best (truth) |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| no-IC | 0.00 | 1.000 | 55.17 | 20.67 | 0.825 | 2.210 | 2.006 |
| fixed 40 dB | 40.00 | 1.000 | 55.88 | 20.67 | 0.825 | 2.379 | 2.051 |
| plain LS | 36.60 | 0.947 | 54.70 | 20.67 | 0.825 | 2.210 | 2.004 |
| ridge LS | 36.66 | 0.948 | 54.72 | 20.67 | 0.825 | 2.210 | 2.004 |
| protected LS | 12.16 | 0.974 | 54.84 | 20.67 | 0.825 | 2.054 | 1.578 |
| TP-UIC stage-1 | 12.16 | 0.976 | 54.78 | 20.67 | 0.825 | 2.054 | 1.579 |
| **TP-UIC full** | 36.30 | **0.980** | 56.04 | 20.67 | 0.825 | **2.230** | **2.108** |
| perfect channel | inf | 1.000 | 55.93 | 20.67 | 0.850 | 2.383 | 2.056 |

**可辨识之后，`P_D` 对每一臂都一样**（belief 0.825 / truth 1.000）——也就是说**限制来自公共项
（belief 误差），不是臂**。臂的差异只能从**无门限的非中心参数**看出来：
`TP-UIC full` 的非 oracle 最高（belief 2.230 / truth 2.108），**逼近 `perfect_channel`**（2.383 / 2.056）；
纯保护（`protected_ls` / `tp_uic_stage1`）反而**最低**（2.054 / 1.578）。
原因是可辨识时保护省下的回波很小（η_surv 0.947 → 0.980，只 3 个百分点），
而保护**抬高残余地板**（C_IC 36.6 → 12.2 dB）的代价更大——所以**只有 stage-2 的联合精修能把代价赚回来**。

### 5.3 掩码曲线（`rho_weighted` vs 邻近目标数）

| arm | n=0 | n=1 | n=2 | n=3 | n=5 | n=9 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| no-IC (belief) | 1.000 | 0.381 | 0.187 | 0.065 | 0.044 | 0.017 |
| plain LS (belief) | 1.000 | 0.381 | 0.187 | 0.065 | 0.039 | 0.016 |
| TP-UIC full (belief) | 1.000 | 0.382 | 0.186 | 0.065 | 0.043 | 0.016 |
| perfect channel (belief) | 1.000 | 0.399 | 0.190 | 0.068 | 0.045 | 0.018 |
| no-IC (truth) | 1.000 | 0.351 | 0.065 | 0.016 | 0.003 | 7.9e-05 |
| plain LS (truth) | 1.000 | 0.351 | 0.064 | 0.016 | 0.002 | 6.9e-05 |
| TP-UIC full (truth) | 1.000 | 0.350 | 0.066 | 0.017 | 0.003 | 7.0e-05 |
| perfect channel (truth) | 1.000 | 0.358 | 0.073 | 0.021 | 0.003 | 8.2e-05 |

**形状是关键证据**：加**第一个**邻居就掉到 0.38，然后**渐进**塌到 0.02——是**并集覆盖**，不是「某一个
邻居把目标挡住」。而且**真值字典 + 无 belief 误差下塌得更快**（n=2 就到 0.065）⇒ 掩码**不是**估计
误差造成的，是**几何**。

### 5.4 可辨识性审计（在格 / 流形 / 离格）

| arm | ρ_grid | ρ_grid_min | ρ_manifold | ρ_manifold_min | ξ_rel_grid | off-grid 中位 | off-grid 最大 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| no-IC (belief) | 1.83e-04 | 1.34e-08 | 3.26e-14 | 1.93e-23 | 5.50e-10 | 2.65e-15 | 5.74e-06 |
| plain LS (belief) | 1.61e-04 | 7.91e-09 | 1.74e-14 | 1.14e-23 | 4.43e-10 | 1.64e-15 | 4.61e-06 |
| TP-UIC full (belief) | 1.71e-04 | 9.60e-09 | 2.68e-14 | 1.87e-23 | 4.80e-10 | 2.59e-15 | 5.54e-06 |
| perfect channel (belief) | 2.05e-04 | 1.62e-08 | 3.41e-14 | 2.35e-23 | 6.94e-10 | 3.15e-15 | 6.00e-06 |
| no-IC (truth) | 5.09e-07 | 1.34e-08 → | 6.46e-20 | 6.17e-24 | ~1e-9 | 3.02e-19 | 8.67e-10 |
| perfect channel (truth) | 7.28e-07 | — | 6.71e-20 | 8.01e-24 | ~1e-9 | 3.04e-19 | 8.76e-10 |

**离格列（±0.4 / ±0.5 bin 的分数偏移）全部 ~1e-15**——**分数偏移救不回来**，因为一阶切向流形
本来就覆盖那点邻域。`perfect_channel` 与估计器臂**同量级**（~1e-23 对 ~1e-23）⇒ **不是 IC 不够**。
（把偏移加到**数个 bin** 才恢复 `rho`，见 4.3；本驱动只扫分数偏移，所以这一格只报"分数偏移无效"。）

### 5.5 对照：真值字典 / 无 belief 误差

| arm | C_IC (dB) | η_surv | ρ_w | T_H1 | P_D | P_FA | **AUC** | ncp_best |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| no-IC | 0.00 | 1.000 | 1.26e-04 | 19.96 | 0.400 | 0.500 | **0.510** | 6.6e-04 |
| fixed 40 dB | 40.00 | 1.000 | 1.53e-04 | 23.26 | 0.600 | 0.700 | **0.450** | 8.6e-04 |
| plain LS | 36.81 | 0.725 | 1.22e-04 | 17.57 | 0.400 | 0.400 | **0.470** | 6.2e-04 |
| ridge LS | 36.85 | 0.728 | 1.22e-04 | 15.87 | 0.300 | 0.500 | **0.460** | 6.3e-04 |
| protected LS | 7.60 | 0.821 | 5.04e-05 | 21.61 | 0.500 | 0.600 | **0.560** | 2.0e-04 |
| TP-UIC stage-1 | 7.61 | 0.823 | 5.26e-05 | 20.88 | 0.500 | 0.500 | **0.540** | 2.1e-04 |
| TP-UIC full | 28.89 | 0.850 | 1.17e-04 | 17.05 | 0.400 | 0.500 | **0.470** | 6.3e-04 |
| perfect channel | inf | 1.000 | 1.57e-04 | 23.95 | 0.600 | 0.700 | **0.440** | 8.8e-04 |

**给接收机完美跟踪器之后，`AUC` 仍然全部 ≈ 0.5（0.44–0.56）** ⇒ 不可辨识**不是 belief 误差造成的**。
（`T_H1` 从 belief 的 41–113 掉到 truth 的 16–24，说明 belief 误差确实往白化残余里**灌了额外的、
未被 C_res 建模的能量**——这解释 5.6 的标定缺口的一半，但**不是全部**。）

### 5.6 ⚠️ 一个必须写明的缺口：解析 CFAR 在本工作点**未标定**

`thr = 20.67`（`dof = 28`）是解析门限，按定义应给出 `P_FA = 0.050`。实测：

| | belief | truth |
| --- | ---: | ---: |
| `P_FA`（解析门限） | 0.69 – 0.94 | 0.40 – 0.70 |

即**白化残余的实际功率比 `C_res` 模型高**（量级 ~1.5–2×，约 1.7–3 dB），
所以**解析门限在本工作点不是 CFAR 门限**。这不影响本版的结论，因为
**`AUC` 是无门限量**（而且 V1 的 `P_D/P_FA` 用的是**经验 H0 分位数**，本来就不是解析门限——
V1 的驱动 docstring 明确把两者分开）。但它有两个直接后果：

1. **`C_res` 目前不能承担"发布用 P_D"**——必须先标定（或沿用 V1 的秩式经验 CFAR）。
2. `perfect_channel` 在 truth 下也 `P_FA = 0.70` ⇒ 缺口**不只是 belief 误差**，
   更可能是**直连系数先验的形状**（`prior_variance = 1` 只对了**幅度**，真值是**单位模 + 随机相位**，
   不是高斯）与**低秩截断**。这是下一版的第一件事（第十节第 1 条）。

---

## 六、三个问题的答案

### Q1：TP-UIC 相对 plain LS 是否**减少弱目标损伤**？—— **是（在保留量上），且是本场景非 oracle 里最好的**

主场景 belief 字典下，弱回波的存活比例 `η_surv`：

| arm | η_surv | 被吃掉 |
| --- | ---: | ---: |
| plain LS / ridge LS | 0.756 / 0.757 | **24.4%** |
| protected LS / TP-UIC stage-1 | 0.805 / 0.808 | 19.2% |
| **TP-UIC full** | **0.938** | **6.2%** |
| `no_ic` / `perfect_channel` | 1.000 | 0% |

⇒ **TP-UIC full 把 plain LS 吃掉的 24.4% 压到 6.2%**，是唯一接近 oracle 的算法臂。
这与 V1 的结论方向一致，V1 报的是 0.6915 → 0.9824（V1 的 `max_protected_targets=3` 口径下损失更大）。

**但可辨识时这个优势会缩小**：单目标下 plain LS 只吃 5.3%（0.947），TP-UIC full 吃 2.0%（0.980）
——因为「plain LS 吃掉 31%」在多目标场景里主要是**其余九个目标的列把弱回波吸进字典**造成的，
字典只剩一个目标时能吸的就少。

### Q2：这种保留是否**在正确检测器下转成 P_D**？—— **在本工作点：不转**

10 目标场景里，**没有任何一臂能把 `H1,q` 与 `H0,q` 分开**：

| | belief | truth（完美跟踪器） |
| --- | ---: | ---: |
| `AUC`（无门限可分性） | 0.508 – 0.551 | 0.440 – 0.560 |
| `ncp_best`（单位幅度回波的期望偏置） | 0.029 – 0.045 | 0.0006 – 0.0009 |
| `P_D` vs `P_FA` | 0.812–0.938 vs 0.688–0.938 | 0.300–0.600 vs 0.400–0.700 |

`AUC ≈ 0.5` 就是「统计量对被测目标在不在**不含信息**」的严格表述——而且它**不依赖门限、
不依赖 `C_res` 标定**（见 5.6），所以这个结论不受标定缺口影响。
`ncp_best ≈ 0` 是同一件事在能量尺度上的说法：**一个单位幅度目标在这个子空间里给出 ~0 dB 偏置**。

**保留确实存在（Q1 为是），但它兑现不了**，因为在掩码区 `T_q` 连"目标在不在"都测不出来。
**在可辨识之后（单目标）它就兑现了**：所有臂都 `P_D` = 0.825（belief）/ 1.000（truth），
且 `TP-UIC full` 的非中心参数最高（2.230 / 2.108），**逼近 `perfect_channel`**（2.383 / 2.056）；
纯保护臂反而最低（2.054 / 1.578）——**保护只有在 stage-2 联合精修之后才有净收益**。

### Q3：残余失败来自**对消**还是**多目标不可辨识**？—— **多目标不可辨识（DD 分辨率受限）**

三条互相独立的证据：

1. **换掉对消器到完美也没用**：`perfect_channel` 臂（直连场精确减去）的
   `ρ_manifold_min = 2.35e-23`，`AUC = 0.512`，`ncp_best = 0.045` ——与估计器臂**同量级**。
2. **换掉跟踪器到真值也没用**：`truth + 无 belief 误差` 下 `ρ_w ≈ 1.2e-4`、`AUC ≈ 0.5`（5.5）。
3. **掩码的形状是并集覆盖**：`ρ` 随邻居数**渐进**塌陷（1.000 → 0.381 → 0.017，n=0→9），
   而且**真值字典下塌得更快** ⇒ 绑定的是 DD 面上**共位散射体的数目**、不是估计误差。
   分数离格（±0.4 / ±0.5 bin）**救不回来**（`~1e-15`），因为一阶切向流形本就覆盖那点邻域。

**结论的措辞**：本工作点的失败是「**受 DD 域分辨率限制的本征重叠**」，
**不是「对消没做好」，也不是「检测器设计得不对」**。按用户决策，**本版不改物理分辨率**；
判据（`ξ_q` / `g_q` / 离格曲线）已经留在这里，等决策后再动（第十节第 4 条）。

### 一句话总结

> **V1.1 把「检测器问错了问题」这一条修掉了，并因此得到一个干净的否定结论：
> 在这个工作点上 TP-UIC 的保留优势（24.4% → 6.2% 的弱回波损伤）是真实的，
> 但它在 DD 域共位掩码下无法兑现为 P_D；而掩码被三条独立证据定位为几何问题，不是对消问题。
> 单目标对照同时给出正面的一半：可辨识时 TP-UIC full 的非中心参数逼近 oracle。**


---

## 七、过程中踩到并修掉的坑

1. **CFAR 门限尾写反**：`thr = quantile(p_fa)` 把门限放在**第 5 百分位**，噪声有 95% 概率越过，
   实测 `P_FA = 0.95`。正确是 `quantile(1 - p_fa)`。
2. **CFAR 门限尺度漏 0.5**：`T` 是 `(1/2)χ²_dof`，门限必须是 `χ²_dof` 分位数的**一半**。
   漏掉 ⇒ 门限高 ~8 dB ⇒ 实测 `P_FA = 0.000125`。**两个 bug 都没被逻辑测试抓到**，
   因为没有任何断言在查**实测虚警率** ⇒ 补上 3.3 节那条白噪声虚警率断言。
3. **残余协方差不是 PSD**（`span(M X)` 上出现负特征值）：根因是把 `C_res` 建模成
   `σ²(I−F)(I−F)^H + ...`。自拟合消除器在那个子空间上是奇异的。改成 **`σ²I` 地板** +
   系数误差项 `(MX) C_h (MX)^H / n_cpi`（见 3.2 的建模决策）。
4. **最小特征值 0.9846 < 1**：不可能的数，不是舍入。低秩块里**误加了 `basis`**（`F` 的 range），
   那是旧约定的残留；只应加 `direct_factor` 与 `est_factor`。
5. **探针容差过紧**：`joint` 臂的伪逆放大 ~1e-8，`tol = 1e-8` 误判；放宽到 `1e-6`。
6. **oracle 臂的先验被错误置零 / trace 对不上**：直连场的 belief 先验对所有臂**同一个**；
   oracle 臂的解析 trace 漏了 `direct_gain²`。
7. **`C_red` 广播错误**：需先 `Q0.conj().T @ c_apply(Q0)` 对称化。

---

## 八、与 V1 的关系 / 本版没有改动的东西

- **消除器一行未改**：`cancellation.py` 只加了一个 `Observation.targets_belief` 字段
  （默认 `None`，既有结果逐位不受影响），用于按 belief 位置做连续 DD 审计。
- **`model.py` 未动**；`cancellation.enable = False` 是默认值，**默认路径逐位不变**（第九节门禁 2/2b）。
- **κ 的地位不变**：κ 仍是**可行性前提**（`κ=0` 时 P_D ≈ P_FA），不是性能增益。
- **不宣称 TP-UIC 优于 40 dB**：`fixed_kappa` 臂只是把**理想化残余模型**放到同一套单位里读，
  它是**下界性质的对照**，不是算法。
- **没有抬 RCS、没有改 DD 分辨率**（按用户决策）。单目标臂只作**机制验证**，不进主表。

---

## 九、门禁与复现

四道门禁在新增 `cancellation_glrt.py` 之后重跑（`PY=E:/anaconda/3_11_python/python.exe`），全绿：

| 门禁 | 命令 | 结果 |
| --- | --- | --- |
| 1 发布身份 | `tools/check_release_identity.py --check` | CLEAN，96 冻结键 / 0 违规 |
| 2 重构口径逐位 | `tools/parity_check.py --baseline --strict` | CLEAN，1596 格 / 0 差异 |
| 2b 发布口径逐位 | `tools/parity_check.py --release-baseline --strict` | CLEAN，1653 格 / 0 差异 |
| 3 契约引用 | `tools/check_contract_refs.py` | CLEAN，32 条引用全部解析 |
| 4 单测 | `pytest tests/ -q` | **215 passed**, 6 subtests passed |

复现本版结果：

```bash
PY=E:/anaconda/3_11_python/python.exe

# 第五节两张主表的生产命令（trial 数见 5 节前言）
$PY tools/run_tp_uic_v11.py --trials 16 --audit-trials 5 --single-trials 40 \
    --receiver-rule median --out results_tp_uic_v11
$PY tools/run_tp_uic_v11.py --trials 10 --audit-trials 4 --single-trials 20 \
    --dictionary truth --no-belief-error --out results_tp_uic_v11_truth

# 规约成第五节的表（含无门限 AUC）
$PY tools/summarise_tpuic_v11.py results_tp_uic_v11
$PY tools/summarise_tpuic_v11.py results_tp_uic_v11_truth

# 文档默认的全量配置（更长，几何类量不变，速率类量更稳）
$PY tools/run_tp_uic_v11.py --trials 40 --audit-trials 12 --single-trials 40 \
    --receiver-rule median --out results_tp_uic_v11_full

# GLRT 回归测试
$PY -m pytest tests/test_cancellation_glrt.py -q
```

---

## 十、下一版要做的（按优先级）

1. **标定 `C_res`（本版测得的第一顺位）**。5.6 实测解析门限给出 `P_FA = 0.40–0.94` 而不是 0.05，
   即 `C_res` 把白化残余**低估 ~1.5–2×**，而且**在真值字典下也低估** ⇒ 不只是 belief 误差。
   候选成因两条，按怀疑程度排序：
   (a) **直连系数先验的形状**——`prior_variance = 1` 只对了**幅度**，真值是**单位模 + 随机相位**
       （`|h_true| = 1`），不是高斯；白化后的二次型因此比 `(1/2)χ²` 有更重的尾。
       (b) **低秩表示的截断**——`span(U)` 上的 ~60×60 限制是否漏掉了直连场的一部分协方差。
   修法要么让 `C_res` 用与生成过程一致的先验，要么**退回 V1 的秩式经验 CFAR**
   （`quantile(H0, 1 − p_fa)`，V1 的驱动本来就是这么算 `P_D/P_FA` 的）。
   **在这之前，`P_D`/`P_FA` 不进主表；本版的比较一律用 `AUC` 与 `ncp`。**
2. **target-conditioned protection 落地**（用户 §9 的方向，本版只做到检测侧）。
   现在保护的是 `max_protected_targets` 个**最弱目标**的整体子空间
   （`_protection_basis`，按目标回波功率升序取前 `budget` 个）；下一步只保护
   `U_q = [a_q, a_{τ,q}, a_{ν,q}]`，把其余目标放进 `A_{-q}` 当 GLRT 干扰。
   5.2 已经给出它值不值得做的判据：**保护单独用是负收益**（`ncp_best` 2.054 < plain 2.210），
   所以新方案必须**带 stage-2 联合精修**一起评。
3. **stage-2 候选门限的**自指**问题**（本版实测到、未改）：候选集从 stage-1 残余的能量定阈，
   而**受保护目标**的回波**故意留在**残余里 ⇒ 被测目标必然成为候选 ⇒ joint 阶段把它重新拟合。
   修法是**把已进保护基的目标从候选集中排除**。这改的是**消除器**的臂定义（不是检测器），
   会动到 V1 的臂数字，**需要一次决策**后再做，不与本版混在一起。
4. **DD 分辨率决策**（用户已置后）：若要把 Q3 的「分辨率受限」变成「已解决」，
   就要改 `N` / `L` / `Δf` / CPI 并在新工作点重跑。本版只提供判据（4.3 / 5.4）。
5. **`interference_tangent_order > 0` 的先验错配**（V1 遗留）：切空间列的真值系数恒为零，
   却给了 `R_h = prior_variance`，保留项被抬高。默认 `order = 0` 不受影响。

---

*本文件记录的是已落地的实现与已跑出的数字。第五节的表由 `tools/summarise_tpuic_v11.py`
从 `results_tp_uic_v11*/` 直接生成，不做手工调整；「单目标」表只作机制验证，不进主表。*
