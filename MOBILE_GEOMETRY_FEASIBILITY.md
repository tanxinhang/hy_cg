# 移动能不能换来"更容易的观察几何"？

日期：2026-09-16。缘起：小目标性能盘点后提出的替代路线——"要么补 ~20 dB 硬件净增益，
要么换一个更容易的观察几何"。本文回答后半句能不能靠**移动**实现。

数据：`results_v1_move_geometry/move_geometry.json`；探针：`tools/probe_move_geometry.py`。
口径：`target-local-v1`（= 论文口径 `orthogonal`，`rho=0.8`，`kappa_dc=40 dB`，
`G_hw=1`，`M=15`，RCS=50 m²，mc=8/seed=10917）。

## 0. 一句话结论

**"移动"在当前模型里不是一个自由度，所以它单独不产生任何增益；但把它读成"派双机去目标"
时，它确实是一条与"收拢整队"同量级、成本低得多的路线，二者都能买到约 +18 ~ +20 dB 的
单次观测 SINR——约为小目标所需差距的三分之二，且这是"最佳双站视角"的乐观上界。**

## 1. 先纠一个前提：当前模型里没有"移动"

- `isac_sim/model.py:270-310` 的 `generate_geometry` **一次性随机撒点**，之后位置不再变。
- 速度向量确实被抽了（`uav_speed` 20–60 m/s、`target_speed` 30–150 m/s），但**只进两处**：
  OTFS 多普勒 bin（`model.py:383-392`：`nu_ijq = (rr_tx + rr_rx)/lam`）与
  belief 的匀速预测（`belief.py:61`、`prior.py:97` 的 `p_pred = p + v*dt`）。
- **没有任何一行代码把 `p + v·t` 写回感知几何**。项目自己的边界声明也是一致的：
  `ACTIVE_EVIDENCE_ACQUISITION.md:239` "single-interval and does not update a posterior
  belief or optimize trajectory"；`ISAC_V1_OPTIMIZATION_AUDIT.md:30` 明确把轨迹排除在外。

**推论（决定了整条路线的性质）**：既然是单区间快照，**"移动到好位置再感知"与"一开始就摆在
那个位置"在数学上完全相同**。移动本身增益 ≡ 0，真正有增益的是**位置**。想要"移动"有意义，
必须引入时间轴（多 CPI、跟踪、轨迹约束）——那是模型升级，不是调参。

## 2. 于是问题变成：把节点放到目标附近，能买到多少？

用模型自己的增益函数（`target_gain`、`path_gain`、`noise_power`、`denominator_guard`）
在三种架构下测算，取**该目标的"最佳双站视角"**（210 条链路里 SINR 最大的一条）：

| 架构 | 含义 |
|---|---|
| `compress` | 整个编队收拢到 `scale×4 km` 的方阵（已实测过的轴） |
| `compress3d` | 同上，**高度带也一起压**（否则只有水平footprint在缩） |
| `dispatch` | **只派 2 架**（1 照射 + 1 接收，目标居中于二者之间）飞到半径 r，其余 13 架留在 4 km 站位 |

最佳视角的实测（dB）：

| 接近距离 | `compress` | `compress3d` | `dispatch` |
|---:|---:|---:|---:|
| 4000 m | 12.74 | 12.74 | 10.57 |
| 2000 m | 19.19 | 22.58 | 14.37 |
| 1000 m | 27.93 | 31.40 | 15.37 |
| 600 m | 25.59 | 29.95 | 17.05 |
| 400 m | 31.75 | 32.30 | 20.26 |
| 200 m | 32.81 | 28.36 | 26.37 |
| 80 m | 29.90 | 22.53 | **28.93** |

`compress` 在 600 m 得 **+12.9 dB**，与 `DEPLOYMENT_RANGE_ANALYSIS.md` §4 实测的
**+13.58 dB** 吻合（差 0.7 dB，来自 mc/Q 不同）⇒ 探针复现了已实测的机制，不是新编的数。

## 3. 机制：为什么整队收拢只兑现了 R⁻⁴ 的 63%

对 `log10(r_eff)` 回归（`r_eff = sqrt(d_iq·d_jq)`，即最佳视角两条回波腿的几何平均）：

| 架构 | κ_dc | 回波斜率 | 伙伴直射干扰斜率 | **SINR 斜率** |
|---|---:|---:|---:|---:|
| `compress` | 40 dB | −44.69 | −24.95 | **−28.10** |
| `compress3d` | 40 dB | −39.44 | −27.02 | **−17.77** |
| `dispatch` | 40 dB | −44.68 | −26.52 | **−41.90** |
| `dispatch` | 60 dB | −44.68 | −23.03 | **−44.64** |
| `dispatch` | 80/120 dB | −44.68 | −23.03 | **−44.68** |

单位是 dB/decade，R⁻⁴ = 40、R⁻² = 20。

三个可直接引用的读数：

1. **回波的确是 R⁻⁴**（−44.7 dB/decade，略陡于 40 是因为两条腿同时缩）。
   **直射干扰是 R⁻²**（−25 ~ −26.5）。两者同时变强 ⇒ 净 SINR 只有 **−28.1 dB/decade，
   即 R⁻²·⁸，仅兑现回波红利的 63%**。
2. **把直射对消加深到噪声以下（κ_dc ≥ 80 dB），SINR 斜率立刻变成 −44.68 = 纯 R⁻⁴。**
   也就是说那 37% 的损失**完全可归因于直射路径**，没有别的隐藏机制。
3. **`dispatch` 的斜率（−41.9）远好于 `compress`（−28.1）**，因为派双机**不动干扰底噪**：
   表里 `rest`（机群其余节点造成的干扰）在 `dispatch` 全程冻结在 −139 ~ −141 dB，
   而 `compress` 是 −140.4 → −119.9（跟着缩了 20 dB）。代价是 `dispatch` 的
   **绝对**回波差很多（−120.4 vs −118.5 起点，且机群侧观测完全没改善）。

## 4. 两个反直觉的实测发现

**(a) 水平收缩在 ~200 m 处撞到高度天花板。** `compress` 的 `r_eff` 从 200 m 方阵的
**134 m** 反而回升到 80 m 方阵的 **159 m**——因为 `geometry.area_xy` 只缩水平footprint，
UAV 高度带（800–1200 m）与目标高度带（700–1500 m）**没有跟着缩**。一旦水平尺度降到与
高度差同量级，斜距就由高度差决定，再压水平毫无用处。

**(b) 但压高度带也救不回来。** `compress3d`（三轴同比收缩）在 1000–2000 m 段比 `compress`
好 +3.1 ~ +3.5 dB，**在 <200 m 段反而更差**（80 m 处 22.5 vs 29.9）：高度一拉平，
干扰机彼此也在三维上更近，`rest` 从 −119.9 涨到 −110.3（+9.6 dB），吃掉了回波的全部收益。
**这是同一个规律的第三次出现：你压缩的每一维，都会同时把干扰拉近。**

## 5. 这些增益够不够？

论文工作点（RCS 50 m²）下，4 km 随机撒点的最佳视角是 **+12.7 dB**；最好配置
（`dispatch` 80 m + κ_dc ≥ 80 dB）是 **+32.0 dB**。

- 几何（含派双机这种"用移动实现的摆放"）**最多供给约 +19 ~ +20 dB 单次观测 SINR**。
- 小目标相对论文工作点低 **−30 dB RCS**。
- ⇒ **几何最多补上这个缺口的三分之二，而且用的是"最佳双站视角"这个乐观统计量**。

注意这**不等于**说小目标能达标：系统指标是 **10 个目标里最差的那个**，而本探针算的是
**单个目标的最佳视角**，两者不是一回事。要把 SINR 增益换算成 worst-target P_D，
必须实跑（`tools/audit_v1_lowrcs_sweep.py` 一类），本文不给 P_D 数字。

## 6. 对路线选择的含义

1. **不要把"移动"写成卖点。** 在单区间模型里它是一个恒等变换；真要做，必须连同时间轴
   （多 CPI、轨迹约束、τ 感知/通信时间划分）一起升级——那是 `ISAC_V1_OPTIMIZATION_AUDIT.md`
   明确排除的范围，也是 16 天投稿窗口内不该动的东西。
2. **"派双机"值得单独记录**，因为它**用移动 2 架替代了重部署 15 架**，在 80–200 m 半径上
   达到与整队收拢同级的效果。这是一个可以在 limitation / future work 里写得具体且诚实的方向。
3. **但天花板是硬的**：无论怎么摆，回波 R⁻⁴ 与直射 R⁻² 都成对出现。要打破它只有两条路，
   都与移动无关：
   - **加深直射对消**（`interference.direct_cancellation_db`，现 40 dB）——实测模型内
     ≥80 dB 即可让 SINR 恢复纯 R⁻⁴；
   - **调度门控**（`interference.sense_gate_by_active_tx`，默认 False）——乐观上界 +2.77 dB
     （`RHO_COORDINATION_LEVER.md` §3）。
4. **硬件净增益仍是最后那 1/3。** 几何给出 +20 dB，剩下的约 +10 dB 只能是前端
   （天线增益 / EIRP / 系统损耗）或换更有利的观察几何表述（例如把论文工作点改成
   "系统适用于 RCS ≥ X m²"）。

## 7. 边界与未做项

- **最佳视角是乐观统计量**：它是 210 条链路的最大值，对 10 目标系统里的 worst target 不适用。
- **无 DD/碰撞惩罚**：探针刻意剥离了 `coll` 与 `eta_fine`（约 −0.9 dB 量级），以保持
  它是纯传播几何测量。
- **`dispatch` 是理想化构造**：双机被放在目标两侧的等距点上（三角形不等式给出的最优），
  现实里还要满足 `comm_range` 与可飞性约束，实际增益只会更低。
- **没有跑 MC**：mc=8 只用于确认斜率，不足以支撑任何 P_D 级结论。
- **未做**：几何 × RCS 二维网格、Swerling 起伏下的同一对照、把 +20 dB 换算成
  worst-target P_D 的实跑。

## 8. 复现

```bash
E:/anaconda/3_11_python/python.exe tools/probe_move_geometry.py \
    --mc 8 --seed 10917 --out results_v1_move_geometry
```
