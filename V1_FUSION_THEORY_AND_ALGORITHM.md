# V1 融合位置：理论补强与最小算法改进

> ⚠️ **数字口径**：本文提到 `0.9762` 处指**重跑前**的原发布实验。主比较已于
> 2026-09-16 晚按当前冻结代码重跑，$P_D$ = **0.9764**（漂移 +0.0002）。
> 本文的候选方法与理论结论独立于该数字。新数字见 `SUBMISSION_TRACEABILITY.md` §3/§7。

日期：2026-09-16。此文记录候选方法及证明，不把新方法结果替换为原 V1 的 MC=1000 结果。

## 1. 问题诊断

当前主要缺口不是公式数量，而是距离规则与任务目标之间没有可验证的最优性联系。
最近预测目标的 UAV 未必拥有最多已选观测，也未必能可靠收到其他观测。
同时，矩匹配检测概率不具备已经证明的交付可靠性单调性。
不能从“本地交付更可靠”直接跳到“该位置的最终检测概率更高”。

本次只优化融合目的节点，保留既有功率、波形、DD门、有限码长、报告包长、
独立观测与串行MAC，不加入轨迹、波束、低RCS救援或新的权重超参数。

## 2. 定理一：当前报告模型下的单观测 deflection 单调性

固定同一个感知观测，其本地零假设均值为0、方差为 v>0，备择均值为 δ。
在V1的 gaussian_replacement 模型中，交付失败以零均值、方差 av 的无信息随机量替代，
其中 a≥0；成功概率 χ∈[0,1] 与假设无关。则

\[
d(\chi)=\frac{\chi^2\delta^2}{v[a+(1-a)\chi]}.
\]

当分母为正时，

\[
d'(\chi)=\frac{\delta^2}{v}
\frac{\chi[2a+(1-a)\chi]}{[a+(1-a)\chi]^2}\ge0.
\]

证明：a≤1时分子各项非负；a>1时，括号项在χ∈[0,1]上的最小值为a+1>0。
a=0时对χ>0有d(χ)=χδ²/v，并令完全擦除χ=0的信息量为0，结论仍成立。
局部交付χ=1因此具有最大单观测deflection。在独立观测及deflection权重下，
融合deflection是各单项之和，所以保持其他项不变时，局部交付不会降低它。
该等式针对解析deflection最优权重；代码中的EPS是数值稳定项，不能用于声称有限精度下严格恒等。

**意义与边界：** 这是从已实现的失包混合模型推导出的具体结果，替代泛泛的“可靠性提高”。
但deflection单调不等于CF矩匹配PD单调，也不等于改变整个融合位置后所有链路都变好。
零SINR观测δ=v=0时应按无信息项处理，不能直接计算0/0。

## 3. 定理二：报告数下界与可计算证书

固定C2F已选出的目标q观测集S_q，记

\[
n_q=|S_q|,\qquad a_{qm}=\sum_{(i,j)\in S_q}\mathbf1\{j=m\}.
\]

在一个观测对应一个固定包、一个目标只用一个融合UAV的设定下，

\[
R_q(m)=n_q-a_{qm},\qquad
R_q(m)\ge n_q-\max_m a_{qm}=R_q^{\rm LB}.
\]

证明：恰好a_qm个观测产生于融合节点m，其余观测各需要一次远程报告。
任意m的a_qm不超过最大值，故结论成立。
若某个可行目的节点达到此下界，则它对固定S_q的报告数已经全局最优。
这个下界忽略报告可行性和检测约束，因而不能声称总能达到。

这是一个**接收端观测集中程度**结论，不是空间距离结论：距离只用于初始化。
若最近目标规则已达到下界，就不必再为减少该观测集的报告数而搜索位置。

**最近目标规则的充分最优条件：** 若原规则可行，且对每个目标，最近节点也是该目标
已选观测的接收端众数，即其a_qm达到最大值，那么原位置已经达到总报告数下界。
因此，在保持这些观测并以原预测PD为下限的全部位置方案中，原规则就是报告数全局最优解。
这一条件可以逐试验直接检查，既不要求距离与检测率严格单调，也不把最近规则普遍最优化。

**最近距离不是报告最优的反例：** 若最近目标的节点为0，而固定观测集仅含(2,1)，
把融合放在0需要一个可行远程包，把融合放在1则无需报告。
这直接否定了无附加条件的“最近目标必然最小报告数”；不依赖新的传播或随机模型。

## 4. 算法改进：固定观测集的检测约束融合修正

原V1给出S_q和f_q^0。用相同的belief侧精细观测估计计算
p_q^0=PDhat(S_q,f_q^0)，定义

\[
\mathcal A_q=\{m:\text{S_q全部报告在m可行，局部数量约束满足，}
\widehat P_D(S_q,m)\ge p_q^0\}.
\]

随后求解

\[
f_q^*\in\arg\min_{m\in\mathcal A_q}(n_q-a_{qm}).
\]

### 可直接执行的步骤

1. 使用原V1做预测位置初始化和adaptive C2F，保留其已选观测。
2. 按a_qm从大到小遍历融合节点；只检查报告数严格小于初始位置的节点。
3. 对每个节点先检查报告方向j→m的通信可行性，再计算相同检测器的PD预测。
4. 接受第一个预测PD不低于初始值的节点；否则保留初始节点。
5. 在任何远程统计量发送前完成此规划；不对已经发送的包追溯扣费。

不添加新的代价系数或可接受检测损失阈值。等报告数时保留初始节点，
不为无法节省通信的微小预测变化而迁移。独立目标可按任意顺序处理。

### 定理三：固定集下的精确最优与原目标非退化

在串行正交报告、固定感知表、无跨目标融合容量约束下，上述逐目标算法精确求解

\[
\min_{f_1,\ldots,f_Q}\sum_qR_q(f_q)
\quad\text{s.t.}\quad f_q\in\mathcal A_q.
\]

证明：初始位置自身可行，因此每个A_q非空。目标可加、可行域为各A_q的笛卡尔积，
故问题按q分解。枚举按报告数递增，首个满足约束的候选即该目标的最优解。
算法只跳过报告数不低于初始解的节点，它们不可能改进最小值；因此提前停止是精确的。
逐目标最优值求和即上述固定集问题的全局最优值。

进一步，每个目标的预测PD不下降、报告数不增加。对于原文的截断soft-min与非负二次
缺口惩罚，效用对各PD坐标非递减；λ_c≥0时，原目标F也不下降。
总观测和接收端处理数保持不变，已有总报告上限继续满足。

**明确限制：** 这不是联合选择(S,f)全局最优，也不证明真实PD不下降。
若存在每个融合节点的目标容量、融合处理容量或CPU容量，笛卡尔积结构消失。
实现会拒绝这些配置。该证明也不覆盖随报告集合变化的干扰或并行MAC。

### 计算量

接收端计数O(|S|)，排序O(QM log M)。最多QM次目的节点预测，独立固定集下每次
融合矩计算O(|S_q|)，因此最坏为O(M|S|+QM log M)。
实际实现先计算Q个初始预测，再仅对能严格减少报告数且通信可行的目的节点计算预测。
候选数不超过Q(M−1)。仅使用已选观测的精细DD值，不增加精细DD评估次数。
此处的规划计算时间和控制消息成本不能解释为零；实验单独记录额外PD预测次数。

## 5. 预测保证如何关联真实检测

若能独立证明在两个比较位置都有|PD−PDhat|≤ε_q，则
PD(S_q,f_q*)≥PD(S_q,f_q0)−2ε_q。这由两次误差界和预测PD不下降直接相加得到。
当前系统尚无这样的统一误差界，不能把这个条件推论写成已验证的真实检测保证。
必须用配对Monte Carlo检测、虚警和belief误差条件检查外推风险。

## 6. 实现与验证

- 实现：`isac_sim/fusion_polish.py`。
- 候选方法：`proposed_c2f_adaptive_pd_fusion_polish`，不进入默认方法列表。
- 集成：只在belief侧选择结束后修正plan；truth侧仅用于最终评估。
- 测试：随机非单调PD表与4^2个完整位置组合穷举比较、方向可行性、局部上限、
  耦合模型拒绝、原始结果不变及真实仿真流程。
- 对照脚本：`python tools/audit_fusion_polish.py --mc 100 --workers 4 --conditions nominal no_price belief_stress`。
- 输出：`results_v1_fusion_polish_audit/trials.csv`、`summary.json`。

这一步是否值得进入主算法，由实际报告节省决定。如果原V1多数已达到下界，
更合适的贡献是“可计算最优性证书及剩余空间审计”，而不是再堆叠一个算法模块。

## 7. 创新性定位：已有工作与剩余差异

检索工作流：多来源定向检索。专用学术MCP未挂载，使用网络检索及arXiv作者全文。
以下是针对最接近方向的核查，不是完整系统文献综述。

| 已有工作 | 已有内容 | 本文应如何区分 |
|---|---|---|
| [Chen等，TSP 2025 / arXiv:2405.16791](https://arxiv.org/html/2405.16791v2) | 在CRLB与MAC约束下联合考虑节点选择和量化资源，最小化信道使用次数；有松弛问题KKT收敛证明 | 不能宣称首次通信感知联合选择；差异限于目标级融合目的节点、本地统计量免报告及检测概率约束 |
| [Ge等，Target Handover，arXiv:2411.01871](https://arxiv.org/html/2411.01871v1) | 根据预测轨迹选择共享信息的邻居并交接跟踪职责，以减少交换 | “prediction-only、目标相关、减少通信”本身不足以构成首创；区分单CPI双基地证据报告与跨时刻轨迹交接 |
| [Bai等，BP Target Handover，arXiv:2506.23118](https://arxiv.org/html/2506.23118v1) | 基于belief的交接准则与消息传递，以较少通信获得接近集中处理的跟踪效果 | “相近性能、显著省通信”的故事已有先例；应突出具体报告图、接收端计数下界与固定集最优证书 |

**判断：** 枚举与可分离优化本身是通用工具，不宜命名为全新优化理论。
本次改进的实际价值是把距离启发式的不足变为可度量、可修正、可证明的子问题。
主贡献仍应是一项具体架构机制及其证据，理论和算法为该机制提供支撑。
在完成更广泛的分布式雷达与融合节点选择文献核查前，不使用“首次”措辞。

## 8. C2F保留的诚实边界

当前adaptive frontier未提供所有被删候选的有效增益上界，不能得到无损筛选或统一近似比。
上述固定集证书不覆盖被筛掉的观测，也不能拿来证明C2F全局最优。
本轮不通过假设DD增益或CF-PD单调来强行补出一个不成立的C2F保证。

## 9. 已完成的配对实验与决策

先以seed=2026做三组各100次探索，再以独立seed=7319做三组各200次复核。
后者在前者结果出来后固定了种子和条件，没有根据后者结果调参。
这是独立种子复核，不是预注册的大样本等效性检验。
所有组都比较同一配置下的原C2F与固定集融合修正，观测集和精细DD次数逐试验一致。

| 独立种子条件（每组MC=200） | 原报告数 → 修正报告数 | 原PD → 修正PD | 原位置已达计数下界的试验比例 |
|---|---|---|---|
| 名义V1，λ=0.005 | 0.740 → 0.705（下降4.73%） | 0.9745 → 0.9745 | 98% |
| 关闭价格，λ=0 | 4.760 → 1.000（下降78.99%） | 0.9730 → 0.9730 | 0.5% |
| 位置预测误差500m，λ=0.005 | 1.515 → 1.455（下降3.96%） | 0.8970 → 0.8970 | 96% |

名义条件修正后达到0.705的计数下界；关闭价格条件的无约束计数下界为0.980，
修正结果1.000略高，因为还须满足通信可行性与预测PD下限。这说明仅凭接收端计数还不能忽略这些约束。
三个条件的精细DD评估均保持不变；修正额外候选PD评估平均为0.025、3.730、0.045次，
另有每个非空目标一次初始PD计算。

三组独立种子试验均未观察到逐试验平均PD差异；不报告退化为[0,0]的正态置信区间，
也不据此证明总体等效或真实PD严格非退化。全局虚警均值分别约为0.04862、0.04940、0.04870。
原始V1论文中的0.9762和83.7%属于原发布实验，这里没有用它们替代本次重算结果。

### 取舍

1. **适合进入理论主线：** 报告数下界、最近规则何时达到下界的充分条件、固定集最优性证书。
2. **保留为候选算法：** 固定集融合修正，无需改变物理模型，也无需新调参系数。
3. **不宣传为新的大幅检测增益：** 名义条件已接近固定集报告最优，提升空间较小。
4. **暂不删除通信价格：** λ=0加修正的1.000报告仍高于原带价格V1的0.740，不能称为全面替代。
5. **创新性仍需克制：** 贡献是具体报告结构与可验证保证，枚举子问题本身不是新的通用优化算法。

验证：全套140项测试、6项子测试通过，包括20个随机非单调PD实例的完整位置穷举核对。
数据及复现配置见`results_v1_fusion_polish_heldout/`；逐试验CSV与全部配置均已保存。
