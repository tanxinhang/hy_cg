> **归档提示（2026-09-18）**：本文引用的部分 `results_*` 产物已移入 `_archive/2026-09-18/`；正文中的路径引用已同步更新为归档位置，命令行示例里的 `--out` 目录仍写作历史原名（重跑时依旧输出到该名）。

# 主场景定义：500–800 m × RCS 0.05–0.2 m²

**状态**：场景已冻结（2026-09-18）。本文是 500–800 m 小目标带的唯一场景口径来源；
任何引用该带 P_D 的文档以本文的 §3 口径与 §6 台账为准。
**增益入口的闭合性**（"系统里还有没有额外增益"）不在本文，见
`PERFORMANCE_OPTIMIZATION_ROADMAP.md` §8。

---

## 0. 一句话结论（先读这段）

改判**成立**，但结论取决于两件必须一起接受的事：**垂直几何**（§2，编辑决策）
与**论文主口径下的一处反转**（§5C，物理结论）。

| 指标（`compact-small-uav` 几何） | 已发布点 4 km / 50 m² | 新标称点 600 m / 0.1 m² |
|---|---:|---:|
| 平均目标 P_D（主口径 MC=1000） | 0.9764 | **0.7169** |
| 最差目标 P_D（min-of-means） | 0.966 | **0.699** |
| 全部目标达标概率 | 0.726 | **0.005** |
| 远程报告 / 比特 / 时延 | 0.751 / 481 bit / 0.801 ms | **6.472 / 4142 bit / 6.903 ms** |
| vs `exact_marginal_greedy` 配对 ΔP_D | **+0.0356** [+0.0311, +0.0401] | **+0.0309** [+0.0233, +0.0385] |
| vs `sense_sinr` 配对 ΔP_D | −0.0020 [−0.0050, +0.0010] **未分辨** | **−0.0260** [−0.0334, −0.0185] **已分辨（差）** |
| 相对 `sense_sinr` 的比特/时延节省 | **83.7%** | **51.9%** |

四条硬结论：

1. **对 `exact_marginal_greedy` 的优势在带内保持**（+0.0309，CI 不含 0；幅度与原工作点
   +0.0356 同量级），且**同时更省**（−45.4% 比特）⇒ 这条叙事可原样搬进新场景。
2. ⚠️ **对 `sense_sinr` 从"未分辨"变成"显著更差"**（−0.0260 ≈ 3σ）。原工作点上是
   −0.0020 且 CI 含 0，论文据此写"检测无显著变化、卖点是通信效率"；**新工作点上这句话不成立**。
   在带内只能写成**权衡**：用 **0.026 P_D** 换 **51.9%** 比特与时延。
3. **绝对节省率腰斩**（83.7% → 51.9%）：低 RCS 下回波弱、需要更多报告，V1 的报告数
   从 0.751 涨到 6.472（8.6×），而 `sense_sinr` 涨到 13.457（2.9×）⇒ 比值被压缩。
4. **几何必须选**：`paper-vertical`（h 800–1200 m）在带内平均只有 0.41–0.73、最差目标尾部
   ≈0；`compact-small-uav`（h 200–500 m）为 0.62–0.81、最差目标 0.56–0.73（§5 / §5B）。
   论文主比较已按 `compact` 跑（§5C）。**两套不许混表。**

---

## 1. 为什么换场景：三档难度对照（链路预算位移）

把「双站 R⁴ 距离」与「RCS」折算成相对**已发布工作点**（4 km / RCS 50 m²，
`target-local-v1`，平均目标 P_D 0.9764）的净位移：

`Δ = 40·log10(4000/R) + 10·log10(σ/50)`  [dB]

| R (m) | σ=0.05 | σ=0.1 | σ=0.2 | σ=50 |
|---:|---:|---:|---:|---:|
| 400 | +10.00 | +13.01 | +16.02 | +40.00 |
| **500** | **+6.12** | **+9.13** | **+12.14** | +36.12 |
| **600** | **+2.96** | **+5.97** | **+8.98** | +32.96 |
| **700** | **+0.28** | **+3.29** | **+6.30** | +30.28 |
| **800** | **−2.04** | **+0.97** | **+3.98** | +27.96 |
| 4000 | −30.00 | −26.99 | −23.98 | 0.00 |

| 场景带 | 相对已发布工作点 | 判定 |
|---|---|---|
| 4 km × 0.05–0.2 | **−30 … −24 dB** | 不可检（实测 P_D 0.083–0.202，贴 P_FA 地板）⇒ 不可作主场景 |
| 400 m × 0.05–0.2 | **+10 … +16 dB** | 恒不比如今工作点更难 ⇒ 构不成压力测试 |
| **500–800 m × 0.05–0.2** | **−2.04 … +12.14 dB** | **包住 0 dB** ⇒ 主场景 |

⚠️ 上表是**纯水平缩放**的位移。实际斜距还受高度影响，见 §2 的分叉。

---

## 2. ⚠️ 场景定义有一个必须先定的分叉：垂直几何

「场地边长」不足以定义场景。把论文的垂直几何（UAV 800–1200 m、目标 700–1500 m）直接搬进
500–800 m 场地，**斜距由高度主导，水平轴被压扁**：

| 场地 | 垂直几何 | 双站距离中位数 (m) | 500→800 m 的实际距离增益 |
|---:|---|---:|---:|
| 500 | 论文 800–1200 / 700–1500 | 726 | — |
| 800 | 论文 800–1200 / 700–1500 | 997 | **仅 2.7 dB**（几何上限 8.2 dB） |
| 500 | 紧凑 200–500 / 200–500 | 577 | — |
| 800 | 紧凑 200–500 / 200–500 | 865 | **7.1 dB**（接近上限） |
| 4000 | 论文垂直几何 | 4138 | （参考：4 km 水平轴完全主导） |

⇒ 用论文垂直几何时，"500–800 m 距离轴"在**链路预算**上几乎是同一个工作点
（几何增益只有上限的 1/3）。

⚠️ **不要把这句话读成"P_D 对距离不敏感只发生在论文高度下"**。实测 P_D 对距离的斜率
（§5/§5B）在两套几何下**都**很弱，而 `compact` 反而略小（σ=0.05：−0.049 vs −0.082），
因为 `compact` 把整张图抬到接近饱和区。距离在带内是**弱轴**，两套都是；
`compact` 的作用是**整体抬升**（+0.077…+0.211），不是把距离轴拉直。

仓库里**已有**四个按物理命名的 500 m–4 km 小无人机预设，其中
`small-uav-compact-800m` = `area 800 / comm_range 1000 / h_uav 200–500 / h_target 200–500 / RCS 0.05`。
新带取其垂直几何，并按该预设**自身的比例** `comm_range = 1.25 × area_xy` 外推
（800 → 1000 m，与原预设**逐字段一致**）。

两种口径都在跑，**不要混表**：

| 标签 | 垂直几何 | comm_range | 说明 |
|---|---|---|---|
| `paper-vertical` | h_uav 800–1200 / h_tgt 700–1500 | 2500 m（沿用） | 只改水平边长的受控消融；与已发布 600 m 表**逐位可比** |
| **`compact-small-uav`** | h_uav 200–500 / h_tgt 200–500 | **1.25 × side** | 物理自洽的小无人机场景；锚定既有预设 |

### 2.1 ⚠️ `comm_range` 在带内不约束（已实测）

`geometry.comm_range` 只喂 `model.py:342` 的 `edge_mask`，而 `edge_mask` **确实**用在选择
（`selection.py:183`）与上报（`reporting.py:216,220,229,284,386,543`）。但实测连通边数：

| 面积 | `comm_range` = 1.25×边长 | = 2500 m | 连通边 / 210 | 最大 UAV 间距 |
|---:|---:|---:|---:|---:|
| 500 | 625 | 2500 | **210 / 210** | 568 m |
| 600 | 750 | 2500 | **210 / 210** | 666 m |
| 700 | 875 | 2500 | **210 / 210** | 767 m |
| 800 | 1000 | 2500 | **210 / 210** | 869 m |
| 4 000 | — | 2500 | **138 / 210** | 4 238 m |

⇒ 带内 UAV 图**恒为完全图**，`comm_range` 取 625 还是 2500 结果**逐位相同**。
论文 4 km 工作点才是部分图（138/210）。**任何依赖"邻居稀疏 / 拓扑选择"的结论不能搬进本带**；
带内的 `comm_range` 取值（含既有预设 `small-uav-compact-800m` 的 1000 m）是**装饰性**的。

---

## 3. ⚠️ 口径：本带有两套，混用即不可比

### 3.1 地图口径（§5 / §5B 的地图与资源表）

已发布 `target-local-v1` 预设，仅叠加：

```
comm.interference_model      = orthogonal          （论文主口径，非默认 full_concurrent）
detect.comm_error_model      = gaussian_replacement
selector.score_mode          = detector_pd         ← 注意：不是发布主线的 exact_utility
selector.max_remote_reports  = 8                   ← 报告硬上限 K=8（发布主线是 -1 无上限）
run.seed                     = 10917               MC = 100
+ 场景覆盖：geometry.area_xy / detect.target_rcs /（垂直几何按 §2 选一套）
```

- **按 (area, rcs, trial) 共享**几何、真值/信念增益、粗/细链路表、DD 分箱与融合计划，
  只重跑选择与检测判决 ⇒ 单元之间按 trial 配对。
- 种子 10917 是已发布 RCS/报告预算表的种子 ⇒ 见 §4 的跨工具锚点。
- 这一套也是 `results_v1_rcs_600m` / `results_v1_rcs_joint` / 400 m 救援屏幕的口径。

### 3.2 论文主口径（§5C 的主比较）

发布主线本身，与 `_archive/2026-09-18/results_target_local_v1/main` **同口径**、只换几何与 RCS：

```
selector.score_mode          = exact_utility       （预设默认）
selector.max_remote_reports  = -1                  （无上限）
selector.max_total_links     = 60
run.seed                     = 2026                MC = 1000
```

⚠️ **两套口径的同一格不是同一个数**：`compact @600 m/0.1` 的 base 平均在地图口径是
**0.7390**、在论文主口径（方法 `proposed_c2f_adaptive_pd`）是 **0.7169**。
引用时必须说明是"地图口径（cap 8 / detector_pd）"还是"论文口径（无上限 / exact_utility）"。

### 3.3 统计量：本库有 **5** 个"最差/弱"口径，全部不同名不同数

以 `compact @600 m / σ=0.1`、base 单元、100 trials × 10 targets 为例，**同一份数据**：

| 名称 | 定义 | 本格数值 | 出处 | 用途 |
|---|---|---:|---|---|
| `mean_target_pd`（`actual_mean_target_P_D`） | 先按目标跨 trial 平均，再跨目标平均 | 0.7390 | `summary.json` / `main.csv` | 平均性能（论文表 I） |
| `worst_target_pd`（`actual_worst_target_P_D`） | 先按目标跨 trial 平均，再取**最小目标**（min-of-means） | 0.6400 | 同上 | **公平性**（与论文表 I 的 worst-target 列同源） |
| `worst_target`（`analyse`） | 每 trial 先取目标最小，再跨 trial 平均（mean-of-mins） | 0.0700 | `map.json` | 典型最差抽样 |
| `min_worst`（`analyse`） | 每 trial 取目标最小，再跨 trial 取最小（min-of-mins） | 0.0000 | `map.json` | 尾部/地板 |
| `P_D_weak` | 每 trial 由 `weakest_belief_target` **挑一个**目标，再跨 trial 平均 | 0.7850（主口径） | `simulate.py:1303` | ⚠️ 跟踪的是**被选中的身份**，可与 pooled P_D 反向 |

⚠️ **本文件 §5 的"最差目标"列曾是 mean-of-mins，§5B 的同名列曾是 min-of-means**
（同名列两个统计量）；已统一为 §3.3 的命名，旧引用若只见列名须重查。

**跨种子 MIN**（协调线专用，800 trials/臂）与上表五个**全都不同**，不要跨线引用。

**只可排序、不可报绝对值的**：`tools/probe_lowrcs_shortfall.py` 的解析缺口（dB）。

---

## 4. 跨工具校验锚点（已验）

`paper-vertical` + `--seed 10917 --areas 600` 时，扫描的 `base` 单元必须**逐位**复现
`results_v1_rcs_600m/trials.csv` 的 `rcs*_k8` + `method=v1` 行。

**已验：1200/1200 个字段完全一致（0 处不符）**（3 个 RCS × 100 trials × 4 字段）。
`compact-small-uav` 场景**不适用**该锚点（不同物理），
其锚点是配置级：`@800 m` 与既有 `small-uav-compact-800m` 预设逐字段一致，
唯一差异是 V1 身份键 `fusion.rule`（`nearest_target` vs `max_inner_rate`）。

⚠️ `tools/analyse_lowrcs_500_800.py` 的 `--json-out` 曾默认写死
`results_v1_lowrcs_500_800/map.json` ⇒ 用 `--dir` 指向 `_compact` 时**会覆盖**另一套的摘要。
已改为默认 `<dir>/map.json`，两套摘要各自落在自己目录下（`map.json`）。

---

## 5. 结果：`paper-vertical` 地图（4×3 网格，正式统计量口径）

| RCS | 距离 | base 平均<br>(mean-of-means) | base 最差目标<br>(min-of-means) | base mean-of-mins | base min-of-mins | 最好软件档 | Δ平均 | +15 dB 硬件 | Δ平均 | P_FA |
|---:|---:|---:|---:|---:|---:|---|---:|---:|---:|---:|
| 0.05 | 500 | 0.4900 | 0.4300 | 0.0000 | 0.0000 | `maxmin_looks128` | +0.2610 | 0.9240 | +0.4340 | 0.0487 |
| 0.1 | 500 | 0.6070 | 0.5200 | 0.0000 | 0.0000 | `maxmin_looks128` | +0.2530 | 0.9620 | +0.3550 | 0.0490 |
| 0.2 | 500 | 0.7300 | 0.6200 | 0.0000 | 0.0000 | `maxmin_looks128` | +0.1970 | 0.9800 | +0.2500 | 0.0501 |
| 0.05 | 600 | 0.4750 | 0.3900 | 0.0000 | 0.0000 | `maxmin_looks128` | +0.2600 | 0.9150 | +0.4400 | 0.0486 |
| 0.1 | 600 | 0.5800 | 0.5000 | 0.0100 | 0.0000 | `maxmin_looks128` | +0.2670 | 0.9600 | +0.3800 | 0.0484 |
| 0.2 | 600 | 0.7020 | 0.6400 | 0.0300 | 0.0000 | `maxmin_looks128` | +0.2280 | 0.9770 | +0.2750 | 0.0489 |
| 0.05 | 700 | 0.4190 | 0.3700 | 0.0000 | 0.0000 | `maxmin_looks128` | +0.3000 | 0.8990 | +0.4800 | 0.0500 |
| 0.1 | 700 | 0.5570 | 0.5000 | 0.0000 | 0.0000 | `maxmin_looks128` | +0.2940 | 0.9590 | +0.4020 | 0.0494 |
| 0.2 | 700 | 0.6900 | 0.6500 | 0.0100 | 0.0000 | `maxmin_looks128` | +0.2390 | 0.9790 | +0.2890 | 0.0505 |
| 0.05 | 800 | 0.4080 | 0.3500 | 0.0000 | 0.0000 | `maxmin_looks128` | +0.3000 | 0.9000 | +0.4920 | 0.0505 |
| 0.1 | 800 | 0.5680 | 0.5100 | 0.0000 | 0.0000 | `maxmin_looks128` | +0.2630 | 0.9600 | +0.3920 | 0.0487 |
| 0.2 | 800 | 0.6880 | 0.5700 | 0.0300 | 0.0000 | `maxmin_looks128` | +0.2230 | 0.9780 | +0.2900 | 0.0501 |

- P_FA 全格 0.0484–0.0505（名义 0.05）✓
- **距离轴弱**：σ=0.05 从 0.490 → 0.408（−0.082）；σ=0.2 从 0.730 → 0.688（−0.038）。
- **RCS 轴是主变量**：0.05→0.2 买 +0.24（500 m）至 +0.28（800 m），约为距离轴的 3–6 倍。
- **最优软件档恒为 `maxmin_looks128`**，Δ+0.20…+0.30；它把额度用满（8 报告 / 60 观测 / 8.533 ms）。
- **最差目标（min-of-means）0.35–0.65，且 mean-of-mins 与 min-of-mins 几乎恒为 0**
  ⇒ 公平性在该几何下**站不住**。
- 出图 `results_v1_lowrcs_500_800/map.png`。

### 5.1 机制饱和（`paper-vertical`）

| 配置 | 平均 P_D | 报告（上限 8） | 观测 | 时延 |
|---|---:|---:|---:|---:|
| 4 km / 50 m²（已发布点） | 0.9764 | **0.751**（余量 91%） | 12.10 | 0.801 ms |
| 500 m / 0.2（新带最易角） | 0.7300 | **6.620**（余量 17%） | 34.44 | 7.061 ms |
| 600 m / 0.05（新带最难角） | 0.4750 | **7.660**（余量 4%） | 44.67 | 8.171 ms |
| 800 m / 0.05 | 0.4080 | **7.860**（余量 2%） | 46.56 | 8.384 ms |
| 600 m / 0.05 + 15 dB 硬件 | 0.9150 | 3.790（余量 53%） | 15.71 | 4.043 ms |

⇒ 在该几何 + 地图口径下，**报告额度从"用不到"变成"用满"**。
（⚠️ 这张表的报告数受**硬上限 8** 约束；论文主口径是**无上限**，见 §3.2 / §5C。）

---

## 5B. 结果：`compact-small-uav` 地图（4×3 网格，同口径同 seed）

| RCS | 距离 | base 平均<br>(mean-of-means) | base 最差目标<br>(min-of-means) | base mean-of-mins | base min-of-mins | 最好软件档 | Δ平均 | +15 dB 硬件 | Δ平均 | P_FA |
|---:|---:|---:|---:|---:|---:|---|---:|---:|---:|---:|
| 0.05 | 500 | 0.6680 | 0.5600 | 0.0400 | 0.0000 | `maxmin_looks128` | +0.2680 | 0.9440 | +0.2760 | 0.0482 |
| 0.1 | 500 | 0.7410 | 0.6700 | 0.0900 | 0.0000 | `maxmin_looks128` | +0.2310 | 0.9520 | +0.2110 | 0.0484 |
| 0.2 | 500 | 0.8070 | 0.6900 | 0.1100 | 0.0000 | `maxmin_looks128` | +0.1740 | 0.9610 | +0.1540 | 0.0488 |
| 0.05 | 600 | 0.6370 | 0.5600 | 0.0000 | 0.0000 | `maxmin_looks128` | +0.2830 | 0.9450 | +0.3080 | 0.0459 |
| 0.1 | 600 | 0.7390 | 0.6400 | 0.0700 | 0.0000 | `maxmin_looks128` | +0.2280 | 0.9610 | +0.2220 | 0.0475 |
| 0.2 | 600 | 0.7800 | 0.7300 | 0.1000 | 0.0000 | `maxmin_looks128` | +0.2050 | 0.9620 | +0.1820 | 0.0495 |
| 0.05 | 700 | 0.6330 | 0.5800 | 0.0100 | 0.0000 | `maxmin_looks128` | +0.2780 | 0.9450 | +0.3120 | 0.0483 |
| 0.1 | 700 | 0.7150 | 0.6800 | 0.0800 | 0.0000 | `maxmin_looks128` | +0.2430 | 0.9640 | +0.2490 | 0.0487 |
| 0.2 | 700 | 0.8040 | 0.7200 | 0.1400 | 0.0000 | `maxmin_looks128` | +0.1780 | 0.9640 | +0.1600 | 0.0489 |
| 0.05 | 800 | 0.6190 | 0.5600 | 0.0100 | 0.0000 | `maxmin_looks128` | +0.2850 | 0.9410 | +0.3220 | 0.0486 |
| 0.1 | 800 | 0.7040 | 0.6200 | 0.0200 | 0.0000 | `maxmin_looks128` | +0.2510 | 0.9570 | +0.2530 | 0.0487 |
| 0.2 | 800 | 0.7790 | 0.6800 | 0.0600 | 0.0000 | `maxmin_looks128` | +0.2000 | 0.9700 | +0.1910 | 0.0476 |

- P_FA 全格 0.0459–0.0495（名义 0.05）✓（600 m/0.05 的 0.0459 略偏低，属 MC 噪声）
- **相比 §5 的 `paper-vertical`：base 平均 +0.077…+0.211**，RCS 0.05 那一列涨得最多。
- **最差目标（min-of-means）从 0.35–0.65 抬到 0.56–0.73**——公平性这条主线第一次在带内站住。
- `maxmin_looks128` 达 **0.90–0.985**；12 格中 11 格 ≥ 0.90。
- **最差目标的尾部仍是地板**：各格 min-of-mins ≡ 0（所有软件档、+15 dB 硬件档都一样）。
- 距离轴同样偏弱（σ=0.05：0.668→0.619，比 `paper-vertical` 的 −0.082 还小），
  原因是整张图抬高后进入饱和区，**不是**斜距轴没拉开（斜距轴本身 2.7 dB → 7.1 dB，见 §2）。

### 5B.1 机制：效率叙事在 `compact` 下**回来了**

| 配置 | 平均 P_D | 最差目标 | 报告（上限 8） | 观测 | 时延 |
|---|---:|---:|---:|---:|---:|
| 4 km / 50 m²（已发布点） | 0.9764 | 0.966 | **0.751**（余量 91%） | 12.10 | 0.801 ms |
| 500 m / 0.2，base | 0.8070 | 0.6900 | 4.23 | 22.07 | 4.512 ms |
| 600 m / 0.1，base | 0.7390 | 0.6400 | 5.71 | 27.47 | 6.091 ms |
| 600 m / 0.1，`maxmin_looks128` | 0.9670 | 0.9400 | **8.00**（用满） | 60.00 | 8.533 ms |
| **600 m / 0.1，`+15 dB 硬件`** | 0.9610 | 0.9400 | **0.59** | 10.68 | 0.629 ms |
| **500 m / 0.2，`+15 dB 硬件`** | 0.9610 | 0.9300 | **0.17** | 10.23 | 0.181 ms |

⇒ 在 `compact` 几何下，**`+15 dB` 档的报告数（0.17–0.59）比已发布 4 km 工作点的 0.751 还低**：
"同一份额度换更多证据"的叙事不再是勉强辩护，而是**可量化的进一步节省**。
反过来，`maxmin_looks128` 是把 8× CPI 与额度用满换来 0.967——**算法档买性能、硬件档买效率**，
两者不必同时宣称。

---

## 5C. 论文主口径下的新标称点（MC=1000，seed 2026）

> 口径见 §3.2（与 `_archive/2026-09-18/results_target_local_v1/main` 逐字段同口径），
> 几何 = `compact-small-uav`，标称点 **600 m / RCS 0.1 m²**（带内中点）。
> 产物 `results_lowrcs_500_800_main/main/`（`main.csv` / `trials.csv` / `config.json`）。

| 方法 | P_D | 95% CI | P_D_weak | 最差目标<br>(min-of-means) | 全达标概率 | 观测 | 报告 | 比特 | 时延 |
|---|---:|---|---:|---:|---:|---:|---:|---:|---:|
| **`proposed_c2f_adaptive_pd`** | **0.7169** | [0.7079, 0.7256] | 0.785 | **0.699** | 0.005 | 27.964 | **6.472** | **4142 bit** | **6.903 ms** |
| `exact_marginal_greedy` | 0.6860 | [0.6768, 0.6950] | 0.790 | 0.661 | 0.003 | 26.593 | 11.858 | 7589 bit | 12.649 ms |
| `sense_sinr` | **0.7429** | [0.7342, 0.7513] | 0.776 | **0.716** | 0.007 | 27.964 | 13.457 | 8612 bit | 14.354 ms |

P_FA 0.0484 / 0.0488 / 0.0492。配对差（参考 = V1）：

| 对照 | 4 km / 50 m²（原发布点） | **600 m / 0.1 m²（新点）** |
|---|---|---|
| V1 − `exact_marginal_greedy` | **+0.0356** [+0.0311, +0.0401] | **+0.0309** [+0.0233, +0.0385] |
| V1 − `sense_sinr` | −0.0020 [−0.0050, +0.0010] **未分辨** | **−0.0260** [−0.0334, −0.0185] **显著更差** |

三条必须一起读的结论：

1. **对 `exact_marginal_greedy` 的优势保持**：+0.0309（CI 不含 0），且比特少 45.4%
   （4142 vs 7589）、时延少 45.4%（串行 MAC、定长包 ⇒ 两个节省同源）。
2. ⚠️ **对 `sense_sinr` 的反转变成了显著结果**：−0.0260 ≈ 3σ（半步宽 0.009）。
   原工作点的 −0.0020 落在噪声内，论文因此写"检测无显著变化"。
   **新工作点不能沿用这句话**，只能写成权衡：0.026 P_D ↔ 51.9% 比特/时延
   （4142 vs 8612 bit，6.903 vs 14.354 ms）。最差目标同样反转（0.699 vs 0.716）。
3. **绝对节省率腰斩**（83.7% → 51.9%），因为低 RCS 下两端都需要更多报告，
   而 V1 的增幅更大（0.751→6.472，8.6×；`sense_sinr` 4.595→13.457，2.9×）。
4. **`all_targets_satisfied_prob` 从 0.726 塌到 0.005**：带内"全部 10 个目标都达标"
   基本不发生。论文若报这一列，必须连同带宽一起说明。

⚠️ 同一个格在地图口径下是 0.7390（§5B）、在论文口径下是 0.7169——**不是矛盾**，
是两套口径（cap 8 + `detector_pd` vs 无上限 + `exact_utility`）。引用必须带口径。

---

## 6. 证据台账（引用前先看这张表）

| 场景 | 产物 | 口径 | 状态 |
|---|---|---|---|
| 4×3 网格 × 7 单元，`paper-vertical` | `results_v1_lowrcs_500_800` | §3.1 地图口径，seed 10917，MC=100 | ✅ 已跑完（§5）；摘要 `map.json` |
| 4×3 网格 × 7 单元，`compact-small-uav` | `results_v1_lowrcs_500_800_compact` | §3.1 地图口径 + 低空几何 | ✅ 已跑完（§5B）；摘要 `map.json` |
| **主比较 MC=1000（3 方法），`compact` @600/0.1** | **`results_lowrcs_500_800_main`** | **§3.2 论文主口径** | ✅ 已跑完（§5C） |
| 论文发布点 4 km / 50 m² | `_archive/2026-09-18/results_target_local_v1/main` | §3.2 论文主口径 | ✅ 已发布（§5C 左列） |
| 杠杆闭合 600/0.1（几何三档 + 四个未开采入口） | `results_v1_lever_closure_{pv600,_pv600_lowalt,_compact,_arm_*}` | 解析界，mc=24 | ✅ `PERFORMANCE_OPTIMIZATION_ROADMAP.md` §8 |
| 600 m / 0.05 · 0.1 · 0.2 | `results_v1_rcs_600m` | §3.1 地图口径，K∈{0,2,8} | ✅ 已发布（§4 锚点） |
| 500/0.2 协调前沿 | `results/coordination_experiment*` | 跨种子 MIN，800 trials/臂 | ✅ 但**不是**本表口径 |
| 800/0.05 救援屏幕 | `results_low_rcs_rescue_*`（11 目录） | 屏幕，P_FA 未合格 | ⚠️ 仅内部排序 |
| 400/600 × 0.05–0.2 | `results_v1_lowrcs_shortfall` | 解析缺口（dB） | ⚠️ 只排序 |
| 400/0.05（全）· 400/0.1（43/100） | `results_v1_lowrcs_cheap` | §3.1 地图口径，seed 10919 | ⚠️ 出带，仅历史 |
| ~~400/600 × 0.05–0.2~~ | ~~`results_v1_lowrcs_sweep`~~ | — | ❌ **零行，从未产出** |

单元 = `base`｜`looks64`｜`looks128`｜`maxmin`｜`maxmin_looks64`｜`maxmin_looks128`｜
`gain15`（`radar_net_gain_db=15`，硬件参考轴，非算法）

---

## 7. 复现

```bash
PY=E:/anaconda/3_11_python/python.exe
# paper-vertical（与已发布 600 m 表逐位可比的受控消融）
$PY -u tools/audit_v1_lowrcs_sweep.py --mc 100 --areas 500 600 700 800 \
    --rcs 0.05 0.1 0.2 --seed 10917 --workers 14 --scenario paper-vertical \
    --cells base looks64 looks128 maxmin maxmin_looks64 maxmin_looks128 gain15 \
    --out results_v1_lowrcs_500_800
# compact-small-uav（物理自洽场景，comm_range = 1.25 × area_xy）
$PY -u tools/audit_v1_lowrcs_sweep.py --mc 100 --areas 500 600 700 800 \
    --rcs 0.05 0.1 0.2 --seed 10917 --workers 14 --scenario compact-small-uav \
    --cells base looks64 looks128 maxmin maxmin_looks64 maxmin_looks128 gain15 \
    --out results_v1_lowrcs_500_800_compact
# 校验 + 地图表（--json-out 默认 <dir>/map.json，两套互不覆盖）
$PY tools/analyse_lowrcs_500_800.py --dir results_v1_lowrcs_500_800
$PY tools/analyse_lowrcs_500_800.py --dir results_v1_lowrcs_500_800_compact
$PY tools/make_lowrcs_500_800_fig.py
# 论文主口径主比较（MC=1000，新标称点）
$PY -u tools/rerun_target_local_v1.py --suite main --mc 1000 --seed 2026 --workers 6 --no-plots \
    --set geometry.area_xy=600 --set detect.target_rcs=0.1 --set geometry.comm_range=750 \
    --set geometry.h_uav_min=200 --set geometry.h_uav_max=500 \
    --set geometry.h_target_min=200 --set geometry.h_target_max=500 \
    --out results_lowrcs_500_800_main
```

⚠️ 模块级 `SEED = 10919` 保持不变（`tools/probe_lowrcs_shortfall.py` 从中导入）；
首选种子走 `DEFAULT_SEED = 10917` / `--seed`。

---

## 8. 未收口

- 两套几何的对比是**两个独立 MC=100 网格**，**没有做逐 trial 配对** ⇒
  "base 平均 +0.077…+0.211"是网格级差值，**不可当作配对显著差异**。
- **主比较只跑了带内一个点**（600 m / 0.1）。难角（800 m / 0.05）与易角（500 m / 0.2）
  的 MC=1000 主比较**未跑** ⇒ §5C 的"带内"结论严格说只有单点证据。
- 带内**只有** mean/min-of-means/mean-of-mins/min-of-mins 四个统计量，**没有**跨种子 MIN
  ⇒ 与协调线不可比。
- 协调前沿只覆盖 **500 m / 0.2 一个角**；难角（800 m / 0.05）**未跑协调**。
- `compact` 几何**没有**跨工具逐位锚点（只有配置级：`@800 m` 与 `small-uav-compact-800m`
  逐字段一致）；`paper-vertical` 的 1200/1200 锚点（§4）**不覆盖** `compact`。
- **`P_D_weak` 与 pooled P_D 可反向**（§3.3）：每 trial 重挑"最弱信念"目标，
  带内出现 `P_D_weak`(0.785) > `P_D`(0.7169) ⇒ 该指标**不能当公平性证据**。
- `bracket_minimum_detectable_rcs` 的暂定区间 (0.2, 0.5] m² 在 4 km 上括的，**本带未重括**。
- 论文 §"Operating Boundaries" 的部署扫描（2500/4000/5500 m）与新带不重叠，需重设区间。
- `compact` 的**平台可行性**（低空小无人机的滞空、载荷、抗风）**未论证** —— 见
  `MOBILE_GEOMETRY_FEASIBILITY.md`。
