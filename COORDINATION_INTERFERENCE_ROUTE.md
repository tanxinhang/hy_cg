# 机间通信降干扰路线（锁定场景 500 m / RCS 0.2 m²）

**日期**：2026-09-17
**可执行证据**：`tools/probe_coordination_gain.py`
**前置**：`ADVICE_002_INTEGRATION.md`（同场景的 looks/power 杠杆基线）

> **🛑 口径作废警告（2026-09-17 18:30，最高优先级）**
> 本文 **§1–§10 的全部协调 P_D 数字产生于两个后来被判定为错误的条件下**，一律**不可引用**：
> 1. **A7 未修**：`compute_link_tables` 的 `signal` 不查 `active_tx_mask` ⇒ 被静默的照射机
>    「丢了干扰却留着回波」，会让**掩码不覆盖被评估集合**的场合（如 `simulate.py:872` 的 `{j}` 约定，
>    实测 +0.0744）性能虚高（详见 `SYSTEM_AUDIT_2026-09-17.md` A7）。
>    ⚠️ **但**：本文 `run_coordination_experiment.py` 的掩码恒由**调度自身**导出 ⇒ A7 分支
>    实测触发 **0 / 1206** 次，对本文数字是 **no-op**。早期把虚高归因于 A7 的说法**已撤回**，
>    真正原因是下面第 2 条（见 §11.3 与 `RUN_LOG.md` 更正小节）。
> 2. **粗表评分**：实验脚本用 `dd_frac_loss` 评分，而 `simulate.py` 喂检测器的是**满细表**
>    （`dd_gain=eta_fine`）⇒ 系统性压低**两侧**（同一掩码同一调度：0.6825 vs 0.8280）。
>
> **权威数字在 §11（L=64）与 §13（时间 × 协调九档，`--mc 200`，2026-09-17 20:47–23:10 补完）**
> （A7 修复 + 细表评分 + 跑满轮数）。**§12 是同一批数据的 200-trial 粗精度版，已被 §13 取代**。
> 本文其余部分现在只作**推导过程与机制讨论**归档：
> 结论（协调有用、根因在目标函数、协调与 κ_dc 互为替代品）**方向仍然成立**，
> 但任何**数值**必须换成 §11/§13。
>
> **⚠️ 进度与修正（2026-09-17 01:30）**：第 1 步（门控口径）**已修完并验证**
> （`model.py:623-641`；`check_release_identity --check` CLEAN、`pytest tests/ -q` 180 passed、
> 门控全 True 现为 no-op）。第 2 步（掩码前置 + 不动点）**已实现**
> （`isac_sim/coordination.py`、`tools/probe_coordination_fixedpoint.py`）。
> **但多目标实测把本文第 1 节那个 +0.78 大幅修正**：真实选择下只能 +0.026。
> 原因与解法见新增的第 5、6 节——**结论反而更有价值**。

## 0. 自由度存在性检查（先做这一步，别空谈）

模型里**有**这条路：`active_tx_mask` 在 `simulate.py:584/873` 被消费，
门控开关是 `interference.sense_gate_by_active_tx`（`config.py:304`，默认 **False**）。

但审计 F1 已经指出：掩码是**选择之后**才生成的，选择器永远看不到它。
所以"机间协调"这条路线当前的真实状态是：**能力存在，但接错了位置**——
它被当成"事后灵敏度分析"，而不是"提前的调度协议"。

## 1. 结论摘要

| 手段 | rinr | worst P_D | 相对基线 |
|---|---|---|---|
| ~~**基线**（全员并发辐射，L=16）~~ | +10.91 dB | ~~0.174~~ | — |
| ~~looks 提到 256（16× CPI）~~ | +10.91 dB | ~~0.819~~ | ~~+0.645~~ |
| ~~power ×10~~ | ~+11 dB | ~~0.188~~ | ~~+0.014~~ |
| ~~**机间协调：仅 4 个节点辐射**~~ | +2.39 dB | ~~0.803~~ | ~~+0.629~~ |
| ~~**机间协调：仅参与双站对辐射**~~ | −2.16 dB | ~~0.947~~ | ~~+0.773~~ |

> 🛑 **整表作废**（属 §1–§10 作废口径：A7 前 + 掩码不覆盖全部照射机 + 粗表评分）。
> **修正版（§13.1，九档 800-trial，500 m / RCS 0.2 m²）**：L=16 时 uncoordinated **0.7800** →
> `mask_only` **0.8150** → `sparse` **0.8950**；即便 16× 时间（L=256），uncoordinated 也只到 **0.9550**。
> ⇒ **"静默掉不参与观测的节点是最强杠杆"这一定性结论仍成立**，
> 但**绝对值与幅度一律以 §13 为准**——**0.174 / 0.947 这两个数不得再引用**。

⇒ **让不参与本次观测的节点在 CPI 内静默，是最强杠杆**——它不花功率、不花处理增益，
只花**一条协调信令**（幅度以 §13 为准）。

## 2. 实测明细

### A. 门控口径不自洽（新发现，先修）

`gate=True` + 全 True 掩码本该是 no-op，实测 γ 从 1.718e-1 掉到 1.401e-1（−0.89 dB）。
原因在 `model.py:623-624`：门控分支用 `P_rad_sense = (P_sense + P_comm) * mask`，
而非正交分支的 `P_sense`。即**一开协调，辐射功率就从 ρP=0.8 变成 P=1.0**，
干扰凭空涨 25%。

⇒ 修法：门控分支应与正交口径一致用 `P_sense`（或显式声明用 P，并统一两处）。
**在这条 bug 修掉之前，任何"开协调"的对比都混进了 0.89 dB 的口径偏移。**

### B. 沉默多少个节点值多少

> 🛑 **表内数值属作废口径**（A7 前 + 掩码不覆盖全部照射机）。修正版前沿见 §13.1。
> 定性结论（**静默越多、干扰越低、P_D 越高，且 1 个节点辐射已触地板**）**仍成立**。

| 辐射节点 | rinr | γ | P_D |
|---|---|---|---|
| 全部 15 | +11.86 dB | 1.40e-1 | 0.141 |
| 8 | +5.61 | 4.93e-1 | 0.549 |
| 4 | +2.39 | 8.38e-1 | 0.803 |
| **2 (i,j)** | **−2.16** | 1.42 | **0.947** |
| 1 (i) | −2.16 | 1.42 | 0.947 |

`1 (i)` 与 `2 (i,j)` 完全相同 ⇒ **接收机 j 不需要辐射**，剩下的干扰只有
照射机 i 自身的直射泄漏（近远比项），这就是协调能到达的地板。

> 📌 **该"地板"会随对消深度下沉**：κ=80 时静默 **11/15 台**辐射机对检测统计**毫无影响**
> （`mask_only` 与 `uncoordinated` 逐位相同，见 §13.6）⇒ 本节的"静默几个值多少"在
> κ 足够深时**答案为 0**。这条把 §2.B 与 §2.C 绑成一个统一的图景：**对消深 ⇒ 静默无价值**。

### C. 协调与对消深度是**替代品**（重要）｜✅ **已定案，权威数字见 §13.6**

`κ_dc` 与协调攻击的是**同一个直射泄漏项**，且两者以**乘积**进分母：

```
residual_direct = κ_dc · I_sense_field[j] ,  I_sense_field = P_sense @ (掩码后的 direct_gain)
```

⇒ 预测的是**饱和 / 负交互**（一旦 `κ·field ≪ n0`，第二个杠杆无事可做），
不是固定加性偏移。**800 trials/臂/格实测（§13.6）**：

| κ_dc | 不协调 | `mask_only` | `sparse` | 协调增益 |
|---|---|---|---|---|
| **40（发布值）** | 0.7800 | 0.8150 | **0.8950** | **+0.1150** |
| 50 | 0.9400 | 0.9550 | 0.9500 | +0.0100 |
| 60 | **0.9500（4/4 达标）** | 0.9550 | 0.9500 | +0.0000 |
| 80 | 0.9500 | 0.9500 | 0.9500 | +0.0000 |

⇒ **替代品成立**：κ 40→80 买到 **+0.1700**（不协调臂），而协调自己买到的从 **+0.1150 塌到 +0.0000**
⇒ 交互量 **+0.1150**，**不可叠加计功**。

**工程取舍是双向的**（定案后新增的一层，旧版只说了一半）：

- **能协调** ⇒ 协调是**省时间**的那条路（3× CPI，§13.4），但**协调之后不要再加深对消**
  （1× CPI 下 0.8950→0.9500 那一截是"两臂被 κ 一起抬上去"，非协调之功）。
- **不能协调** ⇒ **κ 60 时 1× CPI 的不协调单臂就 4/4 达标**（0.9500）
  ⇒ 「+20 dB 对消深度的变体」可**替代**「协调 + 3× 时间」。

> 🛑 **下表为作废口径**（A7 前 + 掩码不覆盖全部照射机 + 粗表评分 + **代理**
> `predicted_pd_for_links`（单条冻结链路）+「协调后」掩码取 `{i,j}` 两点即**孤立单目标**，
> 不是发布多目标调度）。其中 `协调后 0.947` 正是审计点名不可引用的一类数。**仅作追溯，禁止引用。**
>
> | κ_dc | 全员辐射 P_D | 协调后 P_D |
> |---|---|---|
> | 40 dB | 0.141 | 0.947 |
> | 50 | 0.797 | 0.972 |
> | 60 | 0.964 | 0.974 |
> | 80 | 0.974 | 0.974 |
>
> 旧表"两条"结论方向仍成立，但具体数字一律换成上表（§13.6）。

### D. 分时代价：协调付得起

> 🛑 **表内数值属作废口径**（A7 前 + 掩码不覆盖全部照射机）。
> 定性结论（**分时代价付得起**）在 §13 的九档 800-trial 下已被独立证实：
> 即便把时间压到 **1×（L=16）**，`sparse` 仍到 **0.8950**；而 §13.4 显示
> 只需 **3×（L=48）** 即达标。

协调若要分时（每个时隙只服务一部分目标），每目标分到的 looks 会下降：

| 时隙数 | looks/目标 | P_D |
|---|---|---|
| 1 | 16 | 0.947 |
| 2 | 8 | 0.816 |
| 5 | 3 | 0.580 |
| 10 | 2 | 0.494 |

**即使付出 10 倍分时代价（每目标只剩 2 looks），P_D = 0.494 仍远高于不协调的 0.174。**
⇒ 干扰下降 ×√looks 损失的权衡，在这条路线上是压倒性偏向协调的。

## 3. 建议的实现路径

1. **先修 A 的口径 bug**（门控分支辐射功率不一致）——否则后续所有数都带 0.89 dB 偏移。
2. **把掩码提到选择之前**（修审计 F1）：最直接的形态是把候选生成与 `active_tx_mask`
   做一次固定点迭代（选 → 生成掩码 → 用掩码重建表 → 再选，2 轮即可验证是否收敛）。
   这是"机间通信"在代码里的落点，也是 +0.77 P_D 的来源。
3. **协调优先于 κ、优先于 looks**：三者有明确的优劣序（协调 +0.77 > looks 256 +0.65 > κ 单独 +0.79 但只在无协调时）。
   注意 κ 与协调不可叠加计功。
4. 论文叙事建议：**"一条协调信令换取干扰受限区的解除"**——比"更复杂的融合/更多算力"
   更有说服力，也更符合 ISAC 的"通感一体化"主题（用通信能力服务感知）。

## 4. 注意事项（别过度解读）

- 以上均为**单条最佳可送达视图**口径，发布系统是多链路融合，绝对值会更高，但排序结论不变。
- "仅参与对辐射"假设协调完全无冲突；真实系统要按目标分组，时隙数在 1–10 之间（见 D）。
- 协调需要一条可靠的机间控制信道；本模型**没有**为这条信令建模其开销与失效，
  论文里必须显式声明这是假设（否则是审稿人会抓的洞）。

---

## 5. 多目标现实：+0.78 拿不到，只能 +0.026（第 1 节的修正）

`tools/probe_coordination_fixedpoint.py`，真实选择器（`select_lagrangian`），500 m / 0.2：

```
round   #TX  #links  worst P_D  mean P_D   sat   note
    0    10      25     0.1690    0.7726     4   uncoordinated baseline
    1    10      25     0.1950    0.7853     5   FIXED POINT
=> worst P_D 0.1690 -> 0.1950 (+0.0260)
```

**不动点在 2 轮内到达**（rounds≥2 的答案完全不再变化，rounds=1 就是未回馈的基线）。
所以"2 轮"是**被验证过的预算**，不是拍脑袋。

但收益只有 **+0.026**，因为掩码是所有选中链路的照射机**并集**：25 条链路散布在
**10 个不同节点**上，15 个里只有 5 个能静默。第 1 节测到的 +0.78 是"只有 1 个照射机"
的孤立最优，不是多目标系统的可达值。

**分时也救不了**（同样把链路选择冻结）：

| 时隙数 | looks/目标 | 辐射节点/时隙 | worst P_D |
|---|---|---|---|
| 1 | 16 | 10 | 0.1950 |
| 2 | 8 | 5, 10 | 0.1679 ← 比基线还差 |
| 5 | 3 | 1,2,6,6,4 | 0.1300 |
| 10 | 2 | … | 0.1200 |

⇒ **轮转分时净亏**：干扰下降补不上 √looks 损失。

## 6. 真正的根因是目标函数，不是物理——稀疏照射前沿（新结论）

`select_lagrangian` 独立地为每个目标最大化检测，**从不问"这会新唤醒几个节点"**。
每多一个照射机就重新引入它自己的直射泄漏。

`tools/probe_coordination_aware_selection.py`：限制同时辐射的节点数上限 K，
候选池只允许来自这 K 个节点的链路，再看检测性能还剩多少。

**单次（seed 20260917）**：未协调参考 worst P_D = 0.2050，用了 **11 个**照射机、26 条链路。

| K | #链路 | worst P_D | vs 参考 |
|---|---|---|---|
| **2** | 20 | 0.7148 | **+0.510** |
| **3** | 21 | **0.7894** | **+0.584** |
| 4 | 24 | 0.7557 | +0.551 |
| 6 | 25 | 0.7485 | +0.544 |
| 10 | 24 | 0.4372 | +0.232 |
| 15（参考） | 26 | 0.2050 | 0 |

**只用 3 个照射机就能服务 21 条链路**——当前选择用了 11 个，其中 8 个是浪费的干扰源。

**多种子稳健性**（关键，因为它决定了结论是否是种子artifact）：

| seed | 参考 worst P_D（#照射机） | K=2 | K=3 |
|---|---|---|---|
| 20260917 | 0.2050 (11) | 0.7148 | **0.7894** |
| 101 | 0.3496 (9) | 0.6571 | **0.6622** |
| 202 | 0.3586 (9) | 0.8530 | **0.8581** |
| 303 | 0.8209 (8) | 0.8173 | 0.8510 |

**读法（这才是可发表的结论）**：
- 四种子平均 worst P_D：参考 **0.434** → K=3 **0.790**（+0.356）；
- **最差种子的 worst P_D：0.205 → 0.789**（K=3），即跨部署的最坏情况被大幅抬起；
- 种子间离散度：参考 std ≈ 0.24 → K=3 **std ≈ 0.078**，**压缩约 3 倍**；
- 增益**恰好在基线最差的部署上最大**，在基线已经 0.82 的部署上几乎为零（+0.03）。

⇒ **这是一个最坏情况/方差压缩机制，不是平均值机制**。叙事应该写成
「协调稀疏照射把跨部署最差 P_D 从 0.21 抬到 0.79」，
而不是「平均提升 0.36」——后者会被审稿人用 seed 303 打回来。

> ⚠️ **上两段属 §6 的早间口径**（`probe_coordination_aware_selection.py`）。
> 定性的"**最坏情况/方差压缩，不是平均提升**"结论**已由 §13 的 800-trial 独立确认**
> （九档 uncoordinated 0.7800→0.9550 单调升，而 `sparse` 在 L=48 即 **4/4 达标**）。
> ⇒ **写论文请引用 §13.4 的门槛表与 §13.5 的等价点**，
> **不要引用本节的 0.21 / 0.79**（旧口径具体值）。

**算法落点**：把照射机数量做成目标函数里的显式代价项
（`gain − λ·[新引入一个照射机]`），或对偶形式「在满足检测要求下最小化 #TX」。
这比"再复杂一点的融合"更贴 ISAC 主题：**用机间通信把干扰受限区解除掉**。

## 7. Step 3 落地：一次性稀疏选择（无迭代）✅

实现（两处，均默认关闭 ⇒ 发布路径 bit-exact，已复验 CLEAN + 180 passed + 选择结果逐位相同）：
- `selector.tx_penalty`：唤醒**新**照射机时的代价（效用单位）
- `selector.max_tx_nodes`：硬上限（对偶形式）
- 逻辑写在 `selection.py:_greedy_lagrangian` 内，**复用原有提交规则**，只加一项

`tools/probe_sparse_selection.py`（4 个种子，真·未协调参考）：

| 设置 | #TX | worst P_D 均值 | 最差种子 | std |
|---|---|---|---|---|
| **REFERENCE（不协调，全员辐射）** | 15 | **0.4335** | **0.2050** | 0.2318 |
| 只用掩码（事后掩盖，无上限） | 9 | 0.5967 | 0.3831 | 0.1934 |
| 硬上限 K=2 | 2 | 0.7239 | 0.3572 | 0.2393 |
| 硬上限 K=3 | 3 | 0.7406 | 0.4628 | 0.1943 |
| 硬上限 K=4 | 4 | 0.7411 | 0.4520 | 0.1901 |
| **软代价 penalty=0.2** | **4** | **0.7555** | **0.5732** | **0.1425** |
| 硬上限 K=1 | 1 | 0.4342 | 0.0000 | 0.3344 |

- 均值 **0.4335 → 0.7555（+0.322）**；最差种子 **0.2050 → 0.5732（+0.368）**；
  std **0.2318 → 0.1425**。
- **K=1 的 min = 0.0000** 说明上限太紧时会有目标完全无观测 —— 边界清楚。
- 这些**实现值还是保守的**：选择器是在**未协调**的表上优化的，它不知道干扰已被降低。

### 两处自我修正

1. **软代价其实优于硬上限**（我上一轮说"优先用硬上限"，错了）：
   penalty=0.2 在均值和最差种子上都更好（0.7555 / 0.5732 vs 0.7411 / 0.4520）。
   硬上限的优势只是"没有悬崖"。⇒ 推荐 **penalty≈0.2 为主，K 上限作稳健备选**。
2. **加了一个防呆**：`penalty ≥ 1` 时该代价超过所有边际收益，贪心的 `score<=0`
   停止规则会**返回空调度**（#TX=0）。静默选不出任何链路绝不是有效答案，
   已改为**抛异常并提示单位尺度**（效用是 O(0.1) 量级）。

### 探针自身的框架错误（已修）

初版把"knobs off"那行当基线，**但它仍然用自己推导的掩码**（9 节点而非 15），
所以那是"事后掩盖"变体、不是未协调参考。加入真参考行后，基线从 0.5967 修正为 **0.4335**
——**这也让 step-3 的增益从 +0.16 变成 +0.32**。凡是"基线"必须显式说明是否参与协调。

## 8. 策略自审（`tools/audit_coordination_strategy.py`，日志 `results/audit_coordination_strategy.log`）

先审再往下走。结论：**step 1 站得住；step 2 的做法需要改；K 上限那条结论反而被加强了。**

### A. step-1 门控修复：3 条不变量全 PASS

| 断言 | 结果 |
|---|---|
| A1 门控 + 全激活掩码 == 未门控发布表（**逐位**） | PASS |
| A2 静默后的干扰永不超过全激活 | PASS（最坏处比值 0.521，因 3 个保留节点含最强贡献者） |
| A3 全部静默后残留干扰降到 `residual_self` 地板 | PASS（max rinr 12.45 → 0.261 = 1e-14/3.83e-14 ✓ 与解析值吻合） |

「180 tests passed」不算证据，这三条才是。

### B. ⚠️ 不动点**不保证存在** —— 我上次说「2 轮已验证」是错的

> **⚠️ 2026-09-17 17:50 更正：本节的「周期振荡」结论作废。**
> 在 200 trial（4 种子 × 50）上按 §11.1 口径重跑：`rounds=4` → **cycle 0** / budget 34；
> `rounds=12` → **cycle 0** / budget 0，**200/200 全收敛**。即此工作点**没有周期振荡**。
> 本节的 seed 7（8 个种子里的 1 个）是**小样本假阳性**，不可外推——见 §11.4。
> 仍然成立的只有：① 移动的是**集合**不是**个数**；② **`rounds=2` 确实不够**（均值 3.7 轮）；
> ③ 环路检测应当保留（它现在的作用是把「预算用尽」与「真周期」分开，实测触发率 0/400）。
> **`--rounds` 的硬要求提高到 ≥ 8。**

8 个种子的实测（`rounds` 预算给 6）：

| seed | 用了几轮 | 收敛 | 路径（#TX） |
|---|---|---|---|
| 1 | 2 | ✓ | 9→9 |
| 2 | **4** | ✓ | 11→11→11→11 |
| 3 | 2 | ✓ | 15→15 |
| 4 | **3** | ✓ | 13→12→12 |
| 5 | 2 | ✓ | 14→14 |
| 6 | **3** | ✓ | 10→9→9 |
| 7 | **4** | ✗ **周期振荡** | 10→9→11→9（检测到 9 复发） |
| 8 | 6（用满） | ✗ **未收敛** | 8→6→5→4→4→4（集合一直在变，只是个数都是 4） |

两个关键事实：
1. **移动的是照射机的"集合"，不是"个数"**。seed 2 的路径 11→11→11→11 个数没变但集合一直在换；
2. **没有收敛证明**：映射 `mask -> mask'` 是确定性的，但 seed 7 出现真周期、seed 8 六轮不停。

⇒ 已加入**环路检测**（`RoundRecord.cycle_detected` / `cycle_to`），把"预算用尽"和
"检测到周期"区分开，不再静默烧预算。**但根本问题是：这个迭代形式本身不是一个健壮算法。**

**对上次「2 轮是验证过的预算」的更正**：那是 2 个种子的巧合。真实分布是
**6/8 收敛（2–4 轮）、1/8 周期、1/8 六轮不停**。

### C. K 上限那条结论被**加强**了（不是 oracle artifact）

限制 `#TX ≤ K` 的探针里，我是用**未协调最优解的照射机使用频次**来挑那 K 个节点的
—— 这是事后视角，必须审。用一个 3 节点上限、随机抽 10 个子集重测：

| 子集 | worst P_D |
|---|---|
| 事后排序 (0,1,11) | 0.7894 |
| 随机 (3,9,11) | **0.8305** |
| 随机 (1,5,9) | **0.8704** |
| 随机中位数 | **0.7216** |
| 随机最差 (1,8,14) | 0.3957 |
| **参考（不协调）** | **0.2050** |

- **10 个随机子集全部**优于参考（最差的 0.3957 也 +0.19）；
- 中位数 0.7216（+0.517）；
- **有 2 个随机子集比我手挑的还好**（0.8305 / 0.8704）。

⇒ **起作用的不是"哪 3 个节点"，而是"上限本身"**。事后排序不但不是必需，甚至不是最优。
这一条把「K 上限」从可疑的 warm-start 提升为**稳健机制**。

### 自审后的策略修正

| 项 | 原计划 | 自审后 |
|---|---|---|
| step 2 迭代反馈 | 作为主机制，2 轮 | **降级为可选精修**（不保证收敛，需环路检测 + 轮数上限） |
| 主机制 | — | **改为一次性约束式：`#TX ≤ K` 选链路，不做反馈迭代**（无收敛问题，robust） |
| step 3 | 把 #TX 代价项写进迭代贪心 | 写进**一次性**目标（`gain − λ·[新照射机]` 或对偶 `min #TX s.t. 检测要求`） |

⇒ **不收敛的迭代不该当主线**。K 上限的稳健性 + 无迭代，才是可发表的形态。

---

## 9. 正式实验：不协调 vs 稀疏照射（真实 MC）

> **🛑 本节数字已作废**（A7 未修 + 粗表评分）。本节的**机制讨论仍然有效**
> （尤其「代理高估约一倍，论文只能用 MC 的数」这一条），但**数值一律换成 §13.1**
> （§11.2 是同一格的 200-trial 版，亦已被 §13.1 取代）。
> 本节原引用的数据文件 `coordination_experiment/coordination_experiment.csv`
> 与 `coordination_experiment_L256.csv` **已删除**（作废产物，指纹与作废原因留档在
> `results/coordination_experiment/RUN_LOG.md`）。有效数据只有 `coordination_experiment_L64.csv`。

运行器 `tools/run_coordination_experiment.py`；数据台账 `results/coordination_experiment/RUN_LOG.md`。

```bash
python tools/run_coordination_experiment.py --mc 40 --seeds 4 --penalty 0.2
```

**为什么必须换掉代理**：此前所有结论都出自 `predicted_pd_for_links`，而它在深低 SNR
区实测偏低约 24%。论文要引用的数字必须来自实现中的检测器。

**用的就是发布口径的检测器**：`simulate.evaluate_detection` —— 精确 LLR 统计量、
Cornish–Fisher 标定阈值、丢包混合、蒙特卡洛计数。选择器也是发布主线
`proposed_c2f_adaptive_pd`（不是简化版 greedy）。

**三条臂，trial 内严格配对**：

| 臂 | 选择 | 评估时的辐射 |
|---|---|---|
| `uncoordinated` | 无 `tx_penalty` | 全员 15 个（参考） |
| `mask_only` | 同上（**同一份选择**） | 只用它的照射机（≈9.5） |
| `sparse` | 有 `tx_penalty=0.2` | 只用它的照射机（≈4.8） |

- `mask_only` 隔离出「静默」单独的贡献；`sparse` 是「静默 + 稀疏重选」。
- **配对是精确的**：`evaluate_detection` 把每一次物理抽样都用 `trial_index` 键控
  （`simulate.py:182-189`），与传入的 generator 无关 ⇒ 同一个 `(q,i,j)` 观测在三条臂上
  看到完全相同的原语，虚警流也共享。**差异因此归因于调度，不归因于 RNG。**
- 报告统计量是**每个部署种子的最差目标 P_D** 及其跨种子分布，
  不是池化平均——池化平均无法区分「均值上移」与「方差压缩」。

**冒烟结果**（mc=3, 2 seeds）：机制生效（#TX 15 → 9.5 → 4.8），P_FA 实测 0.042–0.070（标定正常），
平均 P_D 提升（0.833→0.900→0.933 / 0.733→0.733→0.833），但 mc=3 时最差 P_D 量化为 1/3，
不足以判定，故跑正式规模。

### 正式结果（160 trials/臂：40 trials × 4 部署种子）

| 臂 | 最差种子 P_D | 最差 P_D 均值 | std | #TX | #links | P_FA |
|---|---|---|---|---|---|---|
| `uncoordinated` | **0.6000** | 0.6375 | 0.0375 | 15.0 | 24.1 | 0.044–0.050 |
| `mask_only` | 0.6500 | 0.7188 | 0.0446 | 10.1 | 24.1 | 0.045–0.050 |
| **`sparse`** | **0.7500** | **0.7937** | **0.0272** | **4.7** | 24.0 | 0.045–0.049 |

```
uncoordinated worst-seed 0.6000 -> sparse 0.7500  (+0.1500)
mean over seeds          0.6375 -> 0.7937          (+0.1562)
cross-seed std           0.0375 -> 0.0272
every seed improved?     True   per-seed deltas [0.10, 0.125, 0.225, 0.175]
```

**四个控制点，缺一个结论都不成立**：
1. **每一个种子都改善**（+0.10 … +0.225），不是靠一个好种子拉动；
2. **P_FA 三条臂几乎相同（≈0.045–0.050，标称 0.05）** ⇒ 增益**不是**阈值漂移或虚警率换来的；
   这是最关键的一条控制，因为"提高检测"太容易靠放松门限做到；
3. **#links 基本不变（24.1 → 24.0）** ⇒ 增益来自干扰下降，不是砍掉观测数换来的；
4. 配对严格（同一 `trial_index` 键控同一批物理抽样）⇒ 差异归因于调度而非 RNG。

**分解**：`mask_only`（只静默、不重选）拿到均值 +0.081，`sparse` 再补 +0.075
⇒ **静默与稀疏重选各贡献约一半**。

### ⚠️ 代理高估了约一倍（必须在论文里说明）

| | 代理 `predicted_pd_for_links` | 真实 MC |
|---|---|---|
| 平均提升 | +0.322 | **+0.156** |
| 最差种子提升 | +0.368 | **+0.150** |

代理**高估约 2 倍**，方向与已知偏差一致：它在深低 SNR 区偏低约 24%，
而本机制的增益恰恰集中在最弱目标上，于是被放大。
⇒ **论文只能用 MC 的数**；代理只能用来排序，这一条已在正式实验中验证。

---

## 10. 剩余空间在哪：协调改变了 looks 的**边际效率**（而非复活 power）

> **🛑 本节全部数字已作废**（A7 未修 + 粗表评分）。
> - 「协调**不**复活 power」⇒ ✅ 方向成立，保留。
> - 「协调提升 looks 边际效率」⇒ 代理给的 ×3 **已被 MC 证伪**（见本节末）。
> - 本节末尾的「时间 × 协调完整前沿」表**已于 2026-09-17 18:22 按 §11.1 口径重测完毕**，
>   权威数字全部移到 **§12**，本节不再承载任何可引用数值。

**原本的假说（被证伪）**：协调把工作点推到噪声受限 ⇒ 分母类的 power 旋钮复活。
`tools/probe_post_coordination_levers.py` 实测（500 m / 0.2，同一条链路 (5,10)、同一目标 q=3）：

| 臂 | 辐射节点 | 中位 rinr | 判定 |
|---|---|---|---|
| uncoordinated | 15 | **+9.92 dB** | 干扰受限 |
| coordinated | 4 | **+3.64 dB** | **仍然干扰受限** |

协调确实把 rinr 压低了 6.3 dB、把该链路的 γ 从 0.0580 抬到 0.1288（+3.5 dB），
但**并没有跨过 r=1**（自身照射机的近远比直射项仍是主导）。
⇒ **power 没有复活**，假说证伪。实测也确认：

| 杠杆 | uncoordinated | coordinated |
|---|---|---|
| looks L=64 | +0.0475 | +0.1419 |
| **looks L=256** | **+0.1677** | **+0.5020** |
| power ×1.25 | +0.0009 | +0.0064 |
| power ×2 | +0.0024 | +0.0176 |
| power ×10 | +0.0045 | +0.0366 |
| power1.25 + L256 | +0.1741 | +0.5357 |

**真正的发现（比原假说更有用）**：

> **协调把 looks 的边际价值提高约 3 倍**（L=256：+0.168 → +0.502）。
> 机制：γ 被抬高后，工作点落到 `P_D(γ)` 更陡的区段，同样的 √L 增益换来更多 P_D。

推论（对论文的直接影响）：
1. **协调与积分时间是互补品，不是替代品**——协调提高的是时间预算的**效率**；
2. 剩余缺口应当用**时间（looks）**去补，**不是功率**（后者在两条臂上都几乎无效）；
3. `power1.25 + L256` 相对 `L256` 只多 +0.034 ⇒ power 即便叠加也是边角料。

⚠️ 数值来自代理，**只用于排序**。代理已证实高估约一倍，故组合臂需 MC 复核（见下）。

### MC 复核：协调 × 时间 的交互（🛑 数字作废，结论见 §12）

| 臂 | L=16 最差种子 | L=64 最差种子 | 时间带来的 Δ |
|---|---|---|---|
| `uncoordinated` | ~~0.6000~~ | ~~0.7750~~ | ~~+0.1750~~ |
| `mask_only` | ~~0.6500~~ | ~~0.8500~~ | ~~+0.2000~~ |
| **`sparse`** | ~~0.7500~~ | ~~0.9000~~ | ~~+0.1500~~ |

上表为**作废口径**（粗表评分）。新口径的同一张表见 §12.1。

**方向性结论**（已按 800-trial 复核，成立）：**协调与时间近似可加、略次可加**——
时间 1×→16× 值 **+0.1750**、协调@1× 值 **+0.1150**，相加 **0.2900** vs 实测 **0.2050**
⇒ 明显次可加（200-trial 的 +0.160 / +0.100 / 0.240 作废，§13.5）。

### ⚠️ 代理第二次被证伪：它声称的"协调把 looks 放大 3 倍"不存在

| | 代理 | 真实 MC |
|---|---|---|
| 协调对 looks 边际价值的放大 | **×3**（+0.168 → +0.502） | **不存在**（800-trial 九档增益 +0.1150/+0.1050/+0.1100/+0.1100/+0.0850/+0.0850/+0.0450/+0.0350/+0.0300，**无放大且递减**；§13.3） |

代理把交互项凭空造了出来。**凡涉及交互/组合的结论，一律以 MC 为准。**

### 效率陈述（旧口径版已作废，新口径见 §12.3）

> ~~**协调（1× 时间，最差种子 0.750）≈ 不协调的四倍积分时间（0.775）**~~
> ⇒ 新口径下这句话**要弱化**：协调 1×（0.8400）落在不协调 1×（0.7400）与
> 4×（0.8600）之间，**略低于** 4×。正确说法见 §12.3。

### 🛑 时间 × 协调的完整前沿（**本节版本已作废**，新口径见 §12）

| 积分时间 | 臂 | 最差种子 P_D | 4 种子均值 | std | #TX | P_FA |
|---|---|---|---|---|---|---|
| L=16（1×） | uncoordinated | ~~0.6000~~ | ~~0.6375~~ | ~~0.0375~~ | 15.0 | ~0.045–0.050 |
| L=16 | **sparse** | ~~0.7500~~ | ~~0.7937~~ | ~~0.0272~~ | 4.7 | ~0.045–0.049 |
| L=64（4×） | uncoordinated | ~~0.7750~~ | ~~0.8000~~ | ~~0.0250~~ | 15.0 | ~0.049–0.053 |
| L=64 | **sparse** | ~~0.9000~~ | ~~0.9125~~ | ~~0.0125~~ | 4.7 | ~0.048–0.054 |
| L=256（16×） | uncoordinated | ~~0.8750~~ | ~~0.8812~~ | ~~0.0108~~ | 15.0 | ~0.049–0.051 |
| **L=256** | **sparse** | ~~0.9750~~ | ~~0.9750~~ | ~~0.0000~~ | 3.6 | ~0.049–0.051 |

**作废原因**：粗表评分（`dd_frac_loss`），系统性压低了两侧，尤其发布选择器侧；
且产自修复前一代运行器（固定点默认 `lagrangian_selector`、轮数未声明）。
详见 `results/coordination_experiment/RUN_LOG.md`。

**旧三条结论的存活判定**：

| 旧结论 | 判定 |
|---|---|
| 不协调即使 16 倍时间也摸不到 0.95 | 🛑 **作废**（800-trial：L=256 uncoordinated **0.9550，四种子全 ≥0.95**）⇒ 不协调**能**达标，代价就是 16× 时间（§13.3） |
| 协调 + 16 倍时间四个种子全 ≥0.95、**std = 0** | ⚠️ **前半成立、后半不成立**（std = 0.0025 ≠ 0，别再写"方差压到零"） |
| 协调增益随时间**递减**（+0.150 → +0.125 → +0.100） | ✅ **方向成立**（800-trial：+0.1050/+0.0850/+0.0450/+0.0350/+0.0300 单调递减；绝对值作废） |
| 效率陈述「协调 1× ≈ 不协调 4× 时间」 | ✅ **反转后成立**（800-trial：`sparse`@L16 = **0.8950** > 4× 的 0.8850 ⇒ **≈4.4×**，略高于 4×；200-trial 的"略低于 4×"作废，§13.5） |
| 时间预算越大，达标所需 #TX/#links 越少 | ✅ 成立（#TX 15.0 → 3.4/2.1/1.9/1.6/1.3） |

**留白（已补，见 §12/§13）**：L=96 / L=144 已于 2026-09-17 19:49–19:50 补跑；随后
**全部九个档位都用 `--mc 200` 复核完毕**（九档，`20:47`–`23:10`，**800-trial 表已无缺口**）
⇒ **达标门槛已定案**（`sparse` **L=48（3×）**、`mask_only` L=96、`uncoordinated` L=256，§13.4）。

---

## 11. ✅ 权威数字（细表评分 + 发布方法 + 跑满轮数，2026-09-17 17:47）

**§11.1（口径契约）、§11.3–§11.6（方法与判定）与 §13（九档 800-trial）是本文可引用的来源；
L=64 的数值以 §13.1 为准（§11.2 是同一格的 200-trial 版）。**
§1–§10 的数字见文首作废警告；§12 是 §13 的 200-trial 粗精度前身，只作追溯用。

### 11.1 口径契约（与 `results/coordination_experiment/RUN_LOG.md` 一致）

| 项 | 值 |
|---|---|
| 预设 / 场景 | `target-local-v1` / 500 m / RCS 0.2 m² / 15 UAV / 10 目标 |
| 方法 | `proposed_c2f_adaptive_pd`（= `select_c2f_adaptive`） |
| 门控 | `interference.sense_gate_by_active_tx=True` |
| **评分表** | **full fine table**（`dd_gain=base.eta_fine`）——与 `simulate.py` 喂检测器的表一致 |
| **轮数预算** | **`--rounds 12`**（见 11.3；**≥8 是硬要求**） |
| 统计量 | **跨部署 worst-seed 的 worst-target P_D**（不是 pooled 平均） |

### 11.2 L=64 结果（`--mc 50 --seeds 4`，200/200 收敛）｜⚠️ **该格已被 §13.1 的 800-trial 取代**

| 臂 | worst-seed | worst-P_D MIN | std | #TX | #links | P_FA |
|---|---|---|---|---|---|---|
| uncoordinated | 0.8850 | **0.8600** | 0.0166 | 15.0 | 18.4 | 0.049–0.051 |
| mask_only（同调度只静默） | 0.9150 | 0.8800 | 0.0218 | 8.1 | 18.4 | 0.049–0.051 |
| **sparse（代价 + 固定点）** | **0.9350** | **0.9200** | 0.0166 | **2.1** | 11.9 | 0.047–0.053 |

> ⚠️ **本表是 200-trial 口径**。同一格（L=64）的 800-trial 真值见 §13.1：
> uncoordinated **0.8850**、mask_only **0.9300**、sparse **0.9700**——**三格全部高于本表**
> （`min`-of-noisy-estimates 向下偏）。⇒ 下文"可引用的三条"与"机制分解"的**绝对值以 §13.1 为准**。

**可引用的三条：**

1. **最坏种子 0.860 → 0.920（+0.060）**，4/4 种子改善（+0.04/+0.02/+0.06/+0.08）。
2. **辐射机 15.0 → 2.1（86% 节点静默）**，链路 18.4 → 11.9 ⇒ 增益不是靠加资源换的。
3. **P_FA 三臂 0.047–0.053，无任一侧系统性偏高** ⇒ 增益不是拿虚警换的。

**机制分解**（800-trial 见 §13.1）：只静默不改调度值 **+0.0450**；重新调度再值 **+0.0400** ⇒
「静默」与「重新选择」两条机制都在起作用，**L=64 处静默略大**（200-trial 版读 +0.020/+0.040，绝对值作废）。

### 11.3 与旧口径的对照（用来说明口径错配的量级，不要当结果用）

| 代 | 条件 | L=16 | L=64 | L=256 | 判定 |
|---|---|---|---|---|---|
| 旧（§10 表） | 粗表评分 + 修复前一代运行器 | 0.6000 → ~~0.7500~~ | 0.7750 → 0.9000 | 0.8750 → 0.9750 | **作废** |
| 中（§11.2 / §12） | 细表评分 + 发布方法 + 跑满轮数（`--mc 50`） | 0.7400 → 0.8400 | 0.8600 → 0.9200 | 0.9000 → 0.9800 | **§13 取代（保守下界）** |
| **新（§13.1）** | 同口径但 **`--mc 200`**（每臂 800 trials） | **0.7800 → 0.8950** | **0.8850 → 0.9700** | **0.9550 → 0.9850** | **★ 权威** |

⇒ 两代**不可比**：旧口径把两侧**都**系统性压低了（尤其发布选择器侧）。
⚠️ 这个差异**与 A7 无关**——本运行器的掩码恒由调度自身导出，A7 分支触发 **0 / 1206** 次
（审计 A9）；差异来源是**评分表**。早期把虚高归因于 A7 的说法**已撤回**。
✅ 三个时间档现已**全部**按 11.1 口径重跑完毕（2026-09-17 18:18 / 18:22），
**§10 表的数字整体作废**，不要再按"哪一档可引用"挑着用。

### 11.4 固定点收敛：`--rounds ≥ 8`（新增硬约束）

拆成三类计数（`cycle` / `fixed` / `budget`）并扫两个轮数上限：

| 轮数上限 | fixed | **cycle** | budget | 平均轮数 |
|---|---|---|---|---|
| 4 | 166 | **0** | 34 | 3.55 |
| 12 | **200** | 0 | 0 | 3.73 |

- ⇒ **没有周期振荡**（cycle 0/400），未收敛纯粹是预算不足。§8.B 里由 8 个种子推出的
  「不动点不保证存在 / 存在周期振荡」**作废**——**小样本不可外推**。
- ⚠️ **截断抬高上界**：`rounds=4` 把最好种子（303）从真值 0.960 虚高到 **0.980**，
  而 worst-seed（0.920）与 headline（+0.060）**一字不变**。
  ⇒ 只报最坏情况可接受（须声明）；报最好种子 / std / cv 必须跑满。

### 11.5 代价接不接 coarse：**不接**（`tools/probe_price_stage.py`）

FINE（代价只作用 fine replay）vs PLAN（先定辐射集再选），4 种子 / 18 个匹配 `#TX` 单元：
per-seed 平均差 **+0.0024、中位 0.0000**、matched-K **−0.0058**，
PLAN 只在 5/18 领先，**2/4 种子两设计给出逐条相同的调度**。预注册阈值 +0.05 ⇒ **不接线**。
（§7 那条「代价只作用于 fine replay、约束输出不约束搜索」的 WARN 因此**降级为已声明的设计边界**，
不再是待办缺陷。）

### 11.6 仍然成立 / 需要重测

| 项 | 状态 |
|---|---|
| 「协调有用，且根因在目标函数（逐目标独立最优从未问会新唤醒几个照射机）」 | ✅ 方向成立（§6） |
| 「协调与 κ_dc 是替代品，不可叠加计功」 | ✅ **已测（800 trials/臂/格，§13.6）**：协调增益 κ=40 **+0.1150** → κ=80 **+0.0000**，交互 **+0.1150**；κ≥60 不协调臂已被驱入噪声受限（rinr −4.2 dB） |
| 「协调是最坏情况 / 方差压缩，不是平均提升」 | ✅ 成立（新数均值也升，但 headline 仍按最坏种子报） |
| 「不协调即便 16× 时间也摸不到 0.95」 | 🛑 **作废**（800-trial：L=256 uncoordinated **0.9550**、四种子全 ≥0.95，§13.3） |
| 「协调增益随时间递减」 | ✅ **方向成立**（800-trial **L≥48 后**单调递减 **+0.1100 → +0.0300**；L=16–48 平坦，§13.3） |
| 「协调 + 16× 时间 ⇒ std = 0」 | ❌ **已证伪**（std = 0.0025） |
| 「协调 1× ≈ 不协调 4× 时间」 | ✅ **反转后成立**：800-trial sparse@L16 = **0.8950 > 4× 的 0.8850** ⇒ **≈4.4×**（§13.5） |
| K 上限前沿（`probe_coordination_aware_selection.py`，K=3 = 0.7894） | ✅ 不受 A7 影响（掩码覆盖自身调度） |
| 协调信令开销 / 失效机制 | ❌ **未建模** ⇒ 论文必须声明为假设 |

---

## 12. 时间 × 协调前沿（200-trial 版，取代 §10 表）｜⚠️ **已被 §13 取代，只作追溯**

**2026-09-17 17:47–19:50 补完（五档）**：均 `--mc 50 --seeds 4 --penalty 0.1 --rounds 12`，
每格 200 trials，**逐格 200/200 收敛**。数据
`results/coordination_experiment/coordination_experiment_L{16,64,96,144,256}.csv`
（sha256 `e1540498…` / `e3491d24…` / `4b1c6a33…` / `3e4144ff…` / `9259cf61…`），台账 `RUN_LOG.md` §3。

> ⚠️ **本节已被 §13 全面复核，请直接读 §13。**
> 2026-09-17 20:47–23:10 用 `--mc 200`（格 0.005）对 **L=16/32/40/48/56/64/96/144/256 九档**复核后：
> **15/15 个可比格全部抬升**（+0.005 ~ +0.055），无一例外（`worst-seed` 是
> min-of-noisy-estimates，小样本下向下偏）。⇒ **跨档比较、门槛定位、以及本节所有
> 「从未达标」「硬顶在 0.90」「摸不到 0.95」的断言，一律以 §13 为准**；
> 本节数字只能当**保守下界**用。

### 12.1 前沿表（最差种子 = 跨部署 worst-target P_D 的最小值）

| 积分时间 | 臂 | worst-seed | 4 种子均值 | std | #TX | #links |
|---|---|---|---|---|---|---|
| **L=16（1×）** | uncoordinated | **0.7400** | 0.7550 | 0.0166 | 15.0 | 24.0 |
| L=16 | mask_only | 0.8000 | 0.8050 | 0.0087 | 10.0 | 24.0 |
| **L=16** | **sparse** | **0.8400** | 0.8700 | 0.0300 | **3.4** | 17.1 |
| **L=64（4×）** | uncoordinated | **0.8600** | 0.8850 | 0.0166 | 15.0 | 18.4 |
| L=64 | mask_only | 0.8800 | 0.9150 | 0.0218 | 8.1 | 18.4 |
| **L=64** | **sparse** | **0.9200** | 0.9350 | 0.0166 | **2.1** | 11.9 |
| **L=96（6×）** | uncoordinated | **0.9000** | 0.9150 | 0.0087 | 15.0 | 16.9 |
| L=96 | mask_only | 0.9000 | 0.9300 | 0.0173 | 7.5 | 16.9 |
| **L=96** | **sparse** | **0.9600** | 0.9700 | 0.0100 | **1.9** | 11.2 |
| **L=144（9×）** | uncoordinated | **0.9000** | 0.9250 | 0.0166 | 15.0 | 15.6 |
| L=144 | mask_only | 0.9200 | 0.9500 | 0.0224 | 6.8 | 15.6 |
| **L=144** | **sparse** | **0.9400** | 0.9650 | 0.0166 | **1.6** | 10.7 |
| **L=256（16×）** | uncoordinated | **0.9000** | 0.9300 | 0.0173 | 15.0 | 13.8 |
| L=256 | mask_only | 0.9400 | 0.9650 | 0.0166 | 6.0 | 13.8 |
| **L=256** | **sparse** | **0.9800** | 0.9850 | 0.0087 | **1.3** | 10.3 |

逐种子最差 P_D（顺序 = `20260917 / 101 / 202 / 303`），末列 = sparse 四种子中 ≥0.95 的个数：

| L | uncoordinated | mask_only | sparse | 达标种子 |
|---|---|---|---|---|
| 16 | 0.74 / 0.78 / 0.76 / 0.74 | 0.80 / 0.80 / 0.80 / 0.82 | 0.86 / 0.86 / 0.84 / 0.92 | 0 / 4 |
| 64 | 0.90 / 0.90 / 0.86 / 0.88 | 0.92 / 0.92 / 0.88 / 0.94 | 0.94 / 0.92 / 0.92 / 0.96 | 1 / 4 |
| 96 | 0.92 / 0.92 / 0.90 / 0.92 | 0.94 / 0.94 / 0.90 / 0.94 | 0.98 / 0.96 / 0.96 / 0.98 | **4 / 4** |
| 144 | 0.92 / 0.94 / 0.90 / 0.94 | 0.96 / 0.94 / 0.92 / 0.98 | 0.94 / 0.98 / 0.96 / 0.98 | 3 / 4 |
| 256 | 0.94 / 0.94 / 0.90 / 0.94 | 0.96 / 0.98 / 0.94 / 0.98 | 0.98 / 0.98 / 0.98 / 1.00 | **4 / 4** |

> ⚠️ **精度边界（引用前必读）**：`worst-seed` 由「50 trials 检测计数均值 → 最差目标」构成
> ⇒ **量化格 = 1/50 = 0.02**，**且小样本下 `min` 向下偏**。
> 本表里 L=96 的 sparse 读 0.9600、L=144 读 **0.9400**，差"正好一格"——**这个差已于 §13 被证伪**：
> 800-trial 真值是 96 **0.9650** vs 144 **0.9750**，**方向相反**（144 反而更高，逐种子 3 胜 1 平）。
> ⇒ **不要用本表做跨档比较**，也不要据此说"144 比 96 差"。

### 12.2 六条可引用结论

1. ⚠️ **【§13 已定案】** 达标最小档落在 L=64 与 L=96 之间（原判定）。
   → **§13 定案（800-trial，四点夹逼）**：`sparse` 门槛 = **L=48（3×）**——达标种子数
   L=32 **1/4** → L=40 **2/4** → L=48 **4/4** → L=56 **4/4** ⇒ 门槛落在 **(40, 48]**。
   原判定、"L=144 比 L=96 差"**均作废**（后者真值 144 **0.9750** > 96 **0.9650**，方向相反）。
2. 🛑 **【§13 整条推翻——本节最严重的一条】** 原判定：不协调有硬顶 0.90、
   且任何一档的最好种子都没到 0.95 ⇒ 时间单独用**封顶在 0.95 以下**、**只能靠协调跨过去**。
   → **§13 定案（已含 L=256）**：0.9000 是 min 偏差在低 P_D 处的**饱和假象**。真值随 L
   **单调升**：0.7800（16）→ 0.8300（32）→ 0.8350（40）→ 0.8450（48）→ 0.8700（56）
   → 0.8850（64）→ 0.9200（96）→ 0.9400（144）→ **0.9550（256）**。
   **L=256 的 uncoordinated 四个种子全部 ≥0.95** ⇒ 不协调**能**达标，代价是 16× 时间。
   ⇒ 正确表述：**协调把达标所需时间从 16× 压到 3×（省 5.3 倍）**，
   **不是**"跨过一道本来跨不过的墙"。后者是 200-trial 的假象，**不得再写**。
3. ⚠️ **【§13 部分修正】** 原判定：只静默不够，重调度是达标必需（`mask_only` 从未达标）。
   → **§13 定案**：`mask_only` 门槛 = **L=96（6×）**（L=40 = 0.8850、L=48 = 0.9150、
   L=56 = 0.9250、L=64 = 0.9300 均不达标；L=96 = 0.9500 达标、L=144 = 0.9600、
   L=256 = 0.9750）⇒ "从未达标"**作废**，但**在 L≤64 它确实不够**。
   重调度增量（800-trial）：**+0.0750（32）/ +0.0600（40）/ +0.0400（48）/ +0.0300（56）/
   +0.0400（64）/ +0.0150（96、144）/ +0.0100（256）** ⇒ 低时间档**重调度主导**，
   L≥48 后**静默主导**（翻转点在 L=40→48 之间）。
4. **协调 + 时间可跨到确定性达标**：800-trial 下 L=48/56/64/96/144/256 的 `sparse`
   **四种子全部 ≥0.95**（48：0.955–0.970；56：0.955–0.970；64：0.970–0.975；
   96：0.965–0.980；144：0.975–0.995；256：0.985–0.990）。
   ⚠️ 但 **std ≠ 0**（0.0022–0.0074）⇒ 只能写"跨部署确定性达标"，**不能写"方差压到零"**。
5. ⚠️ **【§13 已推翻"非单调"，改判为 L≥48 后递减】** 原判定：协调增益非单调
   （+0.100/+0.060/+0.060/+0.040/+0.080），旧口径"单调递减"作废。
   → **§13 定案**：L=256 的 +0.080 反弹是**伪影**。800-trial 九档增益
   **+0.1150（16）/ +0.1050（32）/ +0.1100（40）/ +0.1100（48）/ +0.0850（56）/ +0.0850（64）/
   +0.0450（96）/ +0.0350（144）/ +0.0300（256）**
   ⇒ **L≥48 后单调不增（仅 L=56/64 持平，其余严格下降）**（L=16–48 四点平坦，跨度 0.010 = 2 格）。
   仍成立：时间自己的边际收益也在掉（未协调 1×→4× **+0.105**，4×→16× **+0.070**）。

6. **（§13 新增，可直接写论文）** **三条达标路径的门槛**：未协调 **L=256（16×）**、
   仅静默 **L=96（6×）**、静默 + 重调度 **L=48（3×）**
   ⇒ **协调买到的正是"5.3 倍积分时间节省"**，且它不花时间。

**控制**：九档二十七臂 P_FA 全在 **0.046–0.053**（标称 0.05），无任一侧系统性偏高
⇒ 增益**不是**拿虚警换来的。

### 12.3 效率陈述（精确版）

> **★ 权威版（§13，800-trial）：协调把达标所需积分时间从 16× 压到 3×。**
> 未协调要 **L=256**、仅静默要 **L=96**、静默 + 重调度只要 **L=48**
> ⇒ **协调买到的正是"5.3 倍积分时间节省"，而它本身不花积分时间。**

> ⚠️ **原"粗精度"版本已由 §13.5 的 800-trial 复核取代，且结论被反转**：
> 200-trial 版称"协调 1× = 0.8400，**略低于** 4×（0.8600）⇒ ≈3.3×"；
> **800-trial 真值 0.8950，高于** 4×（0.8850）⇒ **≈4.4×，略高于 4×**（详见 §13.5）。
> ⚠️ **"5.3 倍"与"4.4 倍"是两个不同问题**：前者是**门槛比值**（0.95 处 16×→3×），
> 后者是**等值点**（0.895 处 1× 协调 ≈ 4.4× 不协调）。不可混用，§13.5 有说明。

**加和检验（800-trial 定案，见 §13.5）**：时间 1×→16× 值 **+0.175**、协调@1× 值 **+0.1150**，
相加 0.290 vs 实测 **0.205** ⇒ **明显次可加**（旧口径的 0.240 作废）。

**效率证据**：#TX 15.0 → 3.4 / 2.1 / 1.9 / 1.6 / 1.3（**L≥96 后已压到 ≈2**）；
#links 不协调 24.0 → 18.4 → 16.9 → 15.6 → 13.8、协调 17.1 → 11.9 → 11.2 → 10.7 → 10.3
⇒ 时间预算越大，达标所需的观测与辐射节点越少。

### 12.4 仍留白（§13 定案后）

- **门槛精确位置** ⇒ ✅ **已闭合于 (40, 48]**（§13.4）。
- ~~L=16 未做 800-trial~~ ⇒ ✅ **已补完**（§13.5），并**反转了**本节 §12.3 的"不及 4×"。
- **协调 × κ_dc 的替代性**（§11.6）仍未按新口径重测。
- 协调信令开销 / 失效机制**未建模** ⇒ 论文须声明为假设。

---

## 13. ✅ 精度复核（`--mc 200`，量化格 0.005，九档）｜2026-09-17 20:47–23:10 ★权威

产物目录 **`results/coordination_experiment_precision/`**（独立目录，**不与 §12.1 混表**）。
旗标与 §12 完全一致，只把 `--mc` 由 50 提到 200 ⇒ 每臂 **800 trials**。台账 `RUN_LOG.md` §5。

### 13.1 结果

| L | 臂 | worst-seed | 4 种子均值 | std | #TX | #links | 逐种子（`20260917`/`101`/`202`/`303`） |
|---|---|---|---|---|---|---|---|
| 16 | uncoordinated | **0.7800** | 0.7988 | 0.0114 | 15.0 | 23.8 | 0.800 / 0.805 / 0.810 / 0.780 |
| 16 | mask_only | 0.8150 | 0.8425 | 0.0175 | 9.9 | 23.8 | 0.840 / 0.855 / 0.860 / 0.815 |
| **16** | **sparse** | **0.8950** | 0.9075 | 0.0083 | **3.4** | 17.0 | 0.915 / 0.905 / 0.895 / 0.915 |
| 32 | uncoordinated | **0.8300** | 0.8575 | 0.0160 | 15.0 | 21.1 | 0.865 / 0.865 / 0.870 / 0.830 |
| 32 | mask_only | 0.8600 | 0.8937 | 0.0204 | 9.0 | 21.1 | 0.900 / 0.915 / 0.900 / 0.860 |
| **32** | **sparse** | **0.9350** | 0.9413 | 0.0054 | **2.8** | 14.3 | 0.940 / 0.950 / 0.935 / 0.940 |
| 40 | uncoordinated | **0.8350** | 0.8700 | 0.0203 | 15.0 | 20.2 | 0.880 / 0.880 / 0.885 / 0.835 |
| 40 | mask_only | 0.8850 | 0.9038 | 0.0124 | 8.7 | 20.2 | 0.905 / 0.905 / 0.920 / 0.885 |
| **40** | **sparse** | **0.9450** | 0.9512 | 0.0065 | **2.6** | 13.5 | 0.955 / 0.945 / 0.945 / 0.960 |
| 48 | uncoordinated | **0.8450** | 0.8787 | 0.0216 | 15.0 | 19.5 | 0.875 / 0.895 / 0.900 / 0.845 |
| 48 | mask_only | 0.9150 | 0.9250 | 0.0079 | 8.5 | 19.5 | 0.920 / 0.935 / 0.930 / 0.915 |
| **48** | **sparse** | **0.9550** | 0.9625 | 0.0056 | **2.4** | 13.0 | 0.965 / 0.970 / 0.955 / 0.960 |
| 56 | uncoordinated | **0.8700** | 0.8888 | 0.0129 | 15.0 | 18.9 | 0.885 / 0.895 / 0.905 / 0.870 |
| 56 | mask_only | 0.9250 | 0.9363 | 0.0074 | 8.3 | 18.9 | 0.925 / 0.945 / 0.940 / 0.935 |
| **56** | **sparse** | **0.9550** | 0.9650 | 0.0061 | **2.3** | 12.4 | 0.955 / 0.970 / 0.965 / 0.970 |
| 64 | uncoordinated | **0.8850** | 0.8900 | 0.0050 | 15.0 | 18.4 | 0.885 / 0.885 / 0.895 / 0.895 |
| 64 | mask_only | 0.9300 | 0.9387 | 0.0054 | 8.1 | 18.4 | 0.930 / 0.940 / 0.940 / 0.945 |
| **64** | **sparse** | **0.9700** | 0.9713 | 0.0022 | **2.2** | 12.2 | 0.970 / 0.970 / 0.970 / 0.975 |
| 96 | uncoordinated | **0.9200** | 0.9250 | 0.0035 | 15.0 | 17.0 | 0.920 / 0.925 / 0.930 / 0.925 |
| 96 | mask_only | 0.9500 | 0.9600 | 0.0079 | 7.4 | 17.0 | 0.950 / 0.970 / 0.955 / 0.965 |
| **96** | **sparse** | **0.9650** | 0.9738 | 0.0054 | **1.9** | 11.5 | 0.975 / 0.980 / 0.965 / 0.975 |
| 144 | uncoordinated | **0.9400** | 0.9487 | 0.0054 | 15.0 | 15.6 | 0.950 / 0.955 / 0.940 / 0.950 |
| 144 | mask_only | 0.9600 | 0.9700 | 0.0061 | 6.8 | 15.6 | 0.970 / 0.975 / 0.960 / 0.975 |
| **144** | **sparse** | **0.9750** | 0.9838 | 0.0074 | **1.6** | 10.9 | 0.975 / 0.995 / 0.985 / 0.980 |
| 256 | uncoordinated | **0.9550** | 0.9563 | 0.0022 | 15.0 | 13.9 | 0.955 / 0.960 / 0.955 / 0.955 |
| 256 | mask_only | 0.9750 | 0.9825 | 0.0056 | 5.8 | 13.9 | 0.985 / 0.990 / 0.975 / 0.980 |
| **256** | **sparse** | **0.9850** | 0.9875 | 0.0025 | **1.4** | 10.4 | 0.985 / 0.990 / 0.985 / 0.990 |

九档均 **800/800 收敛**（轮数均值／最大见 `RUN_LOG.md` §5.1）。

### 13.2 本次最重要的发现：**200-trial 读数系统性偏低**

| L | 臂 | 200-trial | 800-trial | Δ |
|---|---|---|---|---|
| 16 | uncoordinated | 0.7400 | 0.7800 | +0.040 |
| 16 | mask_only | 0.8000 | 0.8150 | +0.015 |
| 16 | sparse | 0.8400 | **0.8950** | **+0.055** |
| 64 | uncoordinated | 0.8600 | 0.8850 | +0.025 |
| 64 | mask_only | 0.8800 | 0.9300 | **+0.050** |
| 64 | sparse | 0.9200 | **0.9700** | **+0.050** |
| 96 | uncoordinated | 0.9000 | 0.9200 | +0.020 |
| 96 | mask_only | 0.9000 | 0.9500 | **+0.050** |
| 96 | sparse | 0.9600 | 0.9650 | +0.005 |
| 144 | uncoordinated | 0.9000 | 0.9400 | +0.040 |
| 144 | mask_only | 0.9200 | 0.9600 | +0.040 |
| 144 | sparse | 0.9400 | 0.9750 | +0.035 |
| 256 | uncoordinated | 0.9000 | **0.9550** | **+0.055** |
| 256 | mask_only | 0.9400 | 0.9750 | +0.035 |
| 256 | sparse | 0.9800 | 0.9850 | +0.005 |

**15/15 全部抬升，无一例外** ⇒ 方向性偏差，不是抖动。

机制：`worst-seed` = 「每目标 mc 次 trial 均值」再跨目标取 **min** ⇒ **min-of-noisy-estimates
在小样本下向下偏**；估计方差越大、偏得越多。本批 `mask_only` 五档偏 **+0.015~+0.050**
（#TX≈6–10，方差最大），`uncoordinated` 偏 +0.020~+0.055，`sparse` 偏 +0.005~+0.055。
⇒ **偏差量随臂与档位而变，不能用统一常数校正**。

> **最刺眼的两格**：L=256 的 `uncoordinated`（0.9000 → **0.9550**）与 L=16 的 `sparse`
> （0.8400 → **0.8950**），均抬 **+0.055**。前者恰好跨过 0.95 ⇒ **直接翻掉 §12.2 第 2 条**
> （"不协调摸不到 0.95"）；后者把"协调 1× ≈ 几倍时间"的插值位置整个抬高（§13.5）。
> 低 P_D 臂的 min 偏差最大，而"能不能够到 0.95"正是被这条偏差压在门槛两侧的。

**口径规则（新增，三条）**：

1. 跨档**比较**、及任何「某臂从未达标」「硬顶在某值」的断言 ⇒ **必须 800-trial**；
2. 200-trial 表只当**保守下界**用（否定类结论"达不到 0.95"用它只会更强）；
3. 两套量化格**不得进同一张表**（分目录存放即为此）。

### 13.3 逐条定案（每条都对应 §12.2 的一个断言）

| 原判定（§12，200-trial） | 800-trial 定案 | 处置 |
|---|---|---|
| **「不协调即便 16× 时间也摸不到 0.95」** | L=256 uncoordinated = **0.9550，四种子全部 ≥0.95** | **🛑 推翻（本轮最重）** |
| **协调增益非单调**（256 反弹 +0.080） | 九档增益 **+0.1150/+0.1050/+0.1100/+0.1100/+0.0850/+0.0850/+0.0450/+0.0350/+0.0300** ⇒ **L≥48 后单调不增（仅 L=56/64 持平，其余严格下降）**（L=16–48 平坦，跨度 0.010 = 2 格） | **推翻**（旧口径"递减"方向反而成立） |
| **达标最小档落在 L=64 与 L=96 之间** | 五点夹逼：L=16 **0/4** → L=32 **1/4** → L=40 **2/4** → L=48 **4/4** → L=56 **4/4** ⇒ 门槛 **(40, 48]** | **推翻**（门槛远低于 64） |
| L=144（0.9400）比 L=96（0.9600）差一格 | 144 **0.9750** > 96 **0.9650**，逐种子 **3 胜 1 平** | **推翻** |
| `mask_only` **从未**达到 0.95 | L=16 **0.8150**、L=40 **0.8850**、L=48 **0.9150**、L=56 **0.9250**、L=64 **0.9300**（均不达标）；96 **0.9500**、144 **0.9600**、256 **0.9750** | **部分推翻**（L≥96 贴边达标） |
| 不协调硬顶 0.90、**任何种子**没到 0.95 | 16→256：**0.7800 → 0.8300 → 0.8350 → 0.8450 → 0.8700 → 0.8850 → 0.9200 → 0.9400 → 0.9550**（**单调升**），L=256 **4/4 达标** | **🛑 整条作废** |
| 重调度是达标**必需**（+0.040） | **+0.0800（16）/ +0.0750（32）/ +0.0600（40）/ +0.0400（48）/ +0.0300（56）/ +0.0400（64）/ +0.0150（96、144）/ +0.0100（256）** | **修正**（只在 L≤64 显著） |
| 「协调 1× ≈ 3.3× 时间、**不及** 4×」 | sparse@L16 = **0.8950**，**高于** uncoordinated 4×（0.8850）⇒ **≈ 4.4×，略高于 4×** | **反转**（§13.5） |
| L=96 sparse 四种子全 ≥0.95 | 0.975 / 0.980 / 0.965 / 0.975 | **成立** |
| L=144 sparse 3/4 达标 | 0.975 / 0.995 / 0.985 / 0.980（**4/4**） | **上修** |

**800-trial 下的新事实（可直接引用）**：

> **1. 三条达标路径的门槛**：未协调 **L=256（16×）**、仅静默 **L=96（6×）**、
> 静默 + 重调度 **L=48（3×）** ⇒ **协调 = 5.3 倍积分时间节省**，而它本身不花积分时间；
> 只静默只能省 2.7 倍。门槛精确位置夹逼于 **(40, 48]**。
>
> **2. 增益在 L≥48 后单调不增（仅 L=56/64 持平，其余严格下降）**：+0.1100（48）→ +0.0850（56/64）→ +0.0450（96）
> → +0.0350（144）→ +0.0300（256）——与"协调省时间"一致：**时间越少，省的越多**
> （L=16–48 四点平坦 0.105–0.115，跨度 0.010 = 2 格，不可分）。
>
> **3. 两臂的饱和形状不同**：`sparse` 在 L≥48 后平坦（0.9550–0.9850，跨度 0.030，含 1 格逆向差），
> 时间从 3× 加到 16×（5.3×）总共只买 **0.030**；`uncoordinated` 同期仍 **+0.110**
> ⇒ 时间的边际价值**只在未协调时不饱和**。
>
> **4. 机制权重随档位翻转**（翻转点在 **L=40→48** 之间）：L=16/32/40 **重调度 ≥ 静默**
> （+0.0800 vs +0.0350；+0.0750 vs +0.0300；+0.0600 vs +0.0500 已接近持平）；
> L≥48 **静默主导**（+0.0700 vs +0.0400 起，此后 +0.0550/+0.0300、+0.0450/+0.0400…）。

### 13.4 门槛定案（2026-09-17 22:52 五点夹逼闭合）

| 达标途径 | 最小达标档 | 依据 |
|---|---|---|
| `sparse`（静默 + 重调度） | **L=48（3×）** | **L=40 只 2/4**（0.9450）；**L=48 4/4**（0.9550） |
| `mask_only`（只静默） | **L=96（6×）** | L=56（0.9250）、L=64（0.9300）均 **0/4**；L=96 **4/4**（0.9500） |
| `uncoordinated` | **L=256（16×）** | L=144 只 **3/4**（0.9400）；L=256 **4/4**（0.9550） |

⇒ **协调把达标所需积分时间从 16× 压到 3×（省 5.3 倍）**；只静默只能压到 6×（省 2.7 倍）。
门槛精确位置 **(40, 48]**（夹逼路径：16→0/4、32→1/4、40→2/4、48→4/4、56→4/4，单调无逆向）。

### 13.5 效率陈述（L=16 补齐后**反转**，2026-09-17 23:10）

200-trial 那一版（"协调 1× ≈ **3.3×**、**不及** 4×"）**作废**——它建立在 L=16 的粗读数上：

| 量 | 200-trial（作废） | **800-trial（定案）** |
|---|---|---|
| `sparse`@L16 | 0.8400 | **0.8950** |
| 不协调 4×（L=64） | 0.8600 | 0.8850 |
| 落位判断 | 低于 4× ⇒ "≈3.3×" | **高于 4×、落在 4×–6× 之间** ⇒ **≈4.4×** |

> **★ 协调（1× 时间）= 最差种子 0.8950 ≈ 不协调的 4.4 倍积分时间**，
> 而它**本身不花积分时间**。旧表述"不及四倍"是 200-trial 低估造成的假象
> （sparse@16 抬 +0.055、uncoordinated@64 只抬 +0.025 ⇒ **低估幅度不同，插值必偏**）。

> ⚠️ **不要与 §13.4 的"省 5.3 倍"混淆**——两个倍数回答的是**不同问题**，不矛盾：
> - **4.4×（本节）** = **等值点**：在 ≈0.895 这个水平上，1 份协调时间 ≈ 4.4 份不协调时间；
> - **5.3×（§13.4）** = **门槛比值**：跨过 **0.95** 所需的时间 16× → 3×，比值 16/3 = 5.3。
>
> 差异来自**不协调臂在高时间档饱和更快**：目标水平越往上抬，比值越大
> （0.895 → 4.4×；0.95 → 5.3×）。写论文时**必须点明是哪一个**，否则两个数看起来会互相打架。

**加和检验（800-trial）**：时间 1×→16× 值 **+0.175**、协调@1× 值 **+0.1150**，
相加 0.290 vs 实测（sparse@16× − uncoord@1× = 0.9850 − 0.7800）**+0.205**
⇒ **明显次可加**（方向与旧结论一致，程度更强）。

**剩余缺口**：门槛若要写到单档需补 **L=44**（但 40→48 是 2/4→4/4 的**跃迁**，细分边际价值有限）；
协调信令开销 / 失效机制**未建模** ⇒ 论文须声明为假设。

---

### 13.6 协调 × 对消深度（κ_dc）：替代品**定案**（2026-09-18 00:33，800 trials/臂/格）★

**工具**：`tools/probe_coordination_kappa.py`（本轮新增）。
它与 `run_coordination_experiment.py` **逐种子逐位等价**——探针直接 `import` 运行器并复用其
`make_cfg` / `run_trial` / `ARMS`，只在 `make_cfg` 之上叠一个
`apply_overrides({"interference.direct_cancellation_db": κ})`；`--mc 5 --seeds 2` 下
`worst_pd` **6/6 逐位一致**。旗标与 §13 同：`--mc 200 --seeds 4 --penalty 0.1 --looks 16 --rounds 12`。

> ⚠️ **标签纪律**：`interference.direct_cancellation_db` 在发布门禁的 **94 条冻结键**内
> （**不在** 16 条可标定键里）⇒ **κ=40 是发布值本身**，κ=50/60/80 是**带标签的变体**
> （走 override，默认路径逐位不变）。跑前后 `check_release_identity.py --check` 均 **CLEAN**。
>
> ✅ **与 §13.1 的交叉校验**：本扫描 κ=40 档与 §13.1 的 **L=16 行逐格重合**
> （uncoord **0.7800** / mask_only **0.8150** / sparse **0.8950**；`rounds 3.3925` vs 前沿 `3.39`）
> ⇒ 两批实验同口径，可直接拼表。

| κ_dc (dB) | 臂 | worst-P_D **MIN** | 4 种子均值 | std | mean P_D | #TX | #links | P_FA |
|---|---|---|---|---|---|---|---|---|
| **40（发布）** | uncoordinated | **0.7800** (0/4) | 0.7988 | 0.0114 | 0.8395 | 15.0 | 23.8 | 0.0481–0.0490 |
| | mask_only | 0.8150 | 0.8425 | 0.0175 | 0.8816 | 9.9 | 23.8 | 0.0481–0.0491 |
| | sparse | **0.8950** (0/4) | 0.9075 | 0.0083 | 0.9326 | 3.4 | 17.0 | 0.0482–0.0504 |
| 50 | uncoordinated | 0.9400 (1/4) | 0.9437 | 0.0041 | 0.9614 | 15.0 | 13.6 | 0.0484–0.0493 |
| | mask_only | **0.9550** | 0.9662 | 0.0089 | 0.9790 | 5.6 | 13.6 | 0.0485–0.0493 |
| | sparse | 0.9500 (4/4) | 0.9525 | 0.0025 | 0.9709 | 2.4 | 12.5 | 0.0480–0.0496 |
| 60 | uncoordinated | **0.9500 (4/4)** | 0.9600 | 0.0061 | 0.9770 | 15.0 | 11.6 | 0.0479–0.0493 |
| | mask_only | **0.9550** | 0.9637 | 0.0054 | 0.9794 | 4.3 | 11.6 | 0.0479–0.0493 |
| | sparse | 0.9500 (4/4) | 0.9575 | 0.0056 | 0.9728 | 2.1 | 11.9 | 0.0481–0.0491 |
| 80 | uncoordinated | 0.9500 (4/4) | 0.9538 | 0.0041 | 0.9754 | 15.0 | 11.3 | 0.0477–0.0492 |
| | mask_only | 0.9500 (4/4) | 0.9538 | 0.0041 | 0.9754 | 4.1 | 11.3 | 0.0477–0.0492 |
| | sparse | 0.9500 (4/4) | 0.9575 | 0.0083 | 0.9735 | 2.1 | 11.8 | 0.0473–0.0485 |

四档全部 **800/800 收敛**；`P_FA` 全档落在 **0.0473–0.0504**（标称 0.05）⇒ 控制干净。
⚠️ `#links` **随 κ 强响应**（uncoord 23.8 → 13.6 → 11.6 → 11.3）：选择器打分含残余 ⇒
链路变好就不需要那么多条。⇒ **`#links` 是扫描结果、不是控制量**，本表里只有 `P_FA` 算控制。

**机制读数**（`uncoordinated` 臂感知 `rinr` 中位，按 4 个扫描种子取中位；>0 dB = 干扰受限）：

| κ_dc | uncoordinated | mask_only | sparse | 逐种子（uncoordinated） | 区间 |
|---|---|---|---|---|---|
| 40 | **+10.87** | +8.67 | +2.40 | +9.7 / +10.4 / +11.5 / +11.4 | 干扰受限 |
| 50 | **+1.64** | −2.46 | −4.89 | +0.7 / +1.2 / +2.1 / +2.1 | 干扰受限（贴边） |
| 60 | **−4.19** | −5.46 | −5.73 | −4.5 / −4.4 / −4.0 / −4.0 | **噪声受限** |
| 80 | **−5.82** | −5.83 | −5.84 | −5.8 ×4 | 深噪声受限 |

⇒ **跨界发生在 κ=50→60，且四个种子完全一致**（κ=50 全正、κ=60 全负）
⇒ 判定的 `dead` 条件**不依赖单个部署**（初版只用 1 个部署测这个量，已升级为按种子取中位）。

**预注册判定（写在跑之前，防事后反推）**——两条全部触发：

| 判据 | 阈值 | 实测 | 结果 |
|---|---|---|---|
| 增益塌缩 `g(40) − g(80)` | ≥ 0.05 | **+0.1150** | ✅ |
| 存在 κ>40 使 `r(k) < 0`（噪声受限跨界） | 至少一处 | κ = 60、80 | ✅ |

- 交互量 = `Δ_uncoord − Δ_sparse` = (+0.1700) − (+0.0550) = **+0.1150** ⇒ **强烈次可加**
- **VERDICT：SUBSTITUTES** —— 对消越深、协调买到的越少；而单靠加深对消本身已把系统
  带出干扰受限区。**不可把两者的增益相加。**

**一条比"增益小"更硬的证据**：κ=80 时 `mask_only` 与 `uncoordinated` 在 **4/4 种子上
`worst_pd` 与 `mean_pd` 逐位完全相同**，而 `#TX` 差 **+10.9**（15.0 → 4.1）
——**关掉 15 台里的 11 台辐射机，检测统计一字不变**。残余干扰项在 κ=80 已是惰性的。
这不依赖 0.95 天花板（`worst MIN = 0.9500`，未触发 `[[CEILING]]`）。

**两个副产物（报告时要分开写，别混）**：

1. **连续的增益塌缩比二值的达标翻转更早**：κ=50 时增益只剩 **+0.0100**，
   但 `uncoordinated` 此时仍只 **1/4** 达标（到 κ=60 才 4/4）⇒ 拐点附近两视角不一致。
2. **机制权重的反转**：κ=40 时 `sparse`(0.8950) > `mask_only`(0.8150) > `uncoordinated`(0.7800)
   （重调度>静默）；κ≥50 时 **`mask_only` ≥ `sparse`**（0.9550 vs 0.9500）
   ——**对消一旦压低残余，"重新选择照射机"这条机制就归零**。

**遗留**：κ ∈ {40,50,60,80} 四点足以定案（塌缩 0.1150 ≫ 阈值 0.05，且跨界点被两点夹住）；
若要写拐点单档需补 κ=55，边际价值低。协调信令开销 / 失效机制**仍未建模** ⇒ 论文须声明为假设。

