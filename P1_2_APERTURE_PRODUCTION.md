# P1-2（第一步）：把接收阵列接进**生产**碰撞项

角度探针测的是旁路观测模型里的 `rho`。论文数字不来自那里——它来自
`build_base_gains → dd_collision_count → gamma_sense → 选择 → evaluate_detection`。
本步把阵列接在**这条链唯一今天能被阵列影响的地方**：延迟-多普勒碰撞惩罚。

## 一、改了什么

新增 `isac_sim/aperture.py`（**阵列模型的唯一实现**，探针与生产共用，避免两份公式漂移）
与 `Config.Aperture`（`enable=False`、`m_rx=1`、`axis=0` ⇒ 默认逐位不变）。

碰撞项从**整数计数**改成**软计数**：

```
count_q  =  1 + Σ_{r 与 q 同 DD 格, r≠q} |A(Δu_qr)|²
```

`|A|²` 是掩蔽系数（`1 - rho`）：单阵元时**恒等于 1** ⇒ 软计数逐位退化为现有整数计数；
阵列分开一对，它才从 1 往下掉。**没有阈值可调**——这是刻意的，避免事后调门限。

两个刻意的"不做"：

1. **不收集增益**。`10log10(m)` 是链路预算量，与 `radar_net_gain_db` 同类；
   单测钉住 `target_gain / direct_gain / dd_frac_loss` 在开关阵列时**逐位不变**。
   把预算当成机理，就是 `G_hw = 15 dB` 那次的老错。
2. **不挑"更好的轴"**。一维阵只分辨一条体轴且有前后模糊（`Δu = 2` 处 `|A|` 回到 1），
   轴写死在配置里，不看数后选。

## 二、⚠️ 这一步只覆盖了一条通道（必须说清）

`rho` 是**检测器层**的量；而生产检测器是 SINR 软检测，阵列今天只能通过
**掩蔽**这条通道进来。**空间-DD 流形进旁路观测模型（`cancellation.py`）、
进而进 `Ξ`（P1-3）仍未做**——那是 P1-2 的剩余部分。
所以本表的增益是**下界**：它只算了"同格目标不再互相完全掩蔽"，
还没算"模板本身变得可分"。

## 三、结果（20 trials，600 m / RCS 0.1，配对几何与种子）

`m_rx = 1` = 已发布的 DD-only 模型；每一行同一几何、同一种子，只有口径在动。

| m_rx | 口径 | 碰撞均值 | 碰撞最大 | 碰撞链路占比 | mean γ（相对） | n_selected | P_D | P_FA |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | — | 1.149 | 3.00 | 0.138 | 0.000 dB | 32.9 | 0.595 | 0.050 |
| 4 | 8 cm | 1.058 | 2.59 | 0.138 | **+0.124 dB** | 32.9 | 0.595 | 0.051 |
| 8 | 18 cm | 1.030 | 2.27 | 0.138 | **+0.188 dB** | 32.8 | 0.595 | 0.051 |
| 16 | 38 cm | 1.015 | 2.07 | 0.138 | **+0.219 dB** | 32.8 | 0.595 | 0.051 |

**逐 trial 配对**：`P_D(16) − P_D(1)` 均值 **+0.0000**，20 个 trial 里只有 **4 个**发生变化。

## 四、判读：这是本步最重要的结论

**接线是通的**（mean γ 确实 +0.22 dB），但 **P_D 不动**。两个原因，都指向同一件事：

1. **掩蔽本来就不是这个工作点的瓶颈**：只有 13.8% 的链路存在碰撞，均值 1.149 ⇒
   把它**全部**消掉也只值约 0.2 dB（实测 +0.219 dB，吻合）。
2. **ρ 是检测器层的量，而生产检测器是 SINR 软检测**。阵列今天只能通过掩蔽这条通道
   进生产；它带来的"模板本身可分"（`rho_median` 4.1e-04 → 0.742/0.941/0.978）
   **在这条链上完全没有被兑现**。

⇒ **不要把 +0.22 dB 当成"阵列的价值"**。它是"阵列在**现有**检测器里能拿到的部分"。
真正的量在 §二那条通道上。因此：

* **P1-2 的剩余部分（空间-DD 流形进 `cancellation.py` / GLRT）不是可选项，是主战场**；
* **P1-3 的 `Ξ` 必须两条通道一起进**（掩蔽 + 可辨识性），只进前者 ≈ 白做；
* 若将来有人拿"加了阵列只涨 0.2 dB"来质疑方向，本表就是回答：**涨的是掩蔽通道，
  不是可辨识性通道**。

## 六、下半：空间-DD 流形进观测模型（已落地 + 已测）

`aperture.enable` 时观测空间变成 `DD ⊗ 阵列`：字典、保护基、噪声、残差协方差整体升到
`K·m` 维（Kronecker 展开），其后的消除器、白化、统计量、`rho` 全是与维度无关的线性代数，
于是**一次提升，处处生效**。`m_rx=1` 与门控关闭**逐位相同**（已断言）。

**20 trials，配对 (H1,H0)，600 m / RCS 0.1：**

| m_rx | 口径 | `rho_weighted` | P_D | P_FA | **AUC（门限无关）** | E[t_h0] |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | — | 0.182 | 0.550 | 0.450 | **0.557** | 23.8 |
| 4 | 8 cm | 0.692 | 0.350 | 0.150 | **0.568** | 15.7 |
| 8 | 18 cm | 0.893 | 0.150 | 0.050 | **0.540** | 14.4 |

（`tp_uic_full` 同趋势：AUC 0.555 → 0.562 → 0.512。）

**⚠️ 不要读 P_FA 那一列。** `dof = 28`、`E[t_h0] = 14–24`、门限 `20.67 < dof`
⇒ 统计量**不是** χ²(28)，固定门限下的 P_FA 读数没有意义——m=8 那个漂亮的 0.050 很可能是
**统计量整体变小**落到门限以下，不是标定变好。可信的只有门限无关的 AUC。

⇒ **结论（与上半一致，现在在检测器侧也站住了）**：

* 流形**确实进去了**：`rho` 0.182 → 0.692 → 0.893（与探针的 0.742/0.941 同量级）；
* 但**可检测性没有变**：AUC 全程 0.51–0.57 ≈ 随机；
* ⇒ **600 m / 0.1 这个工作点上，可分性不是约束**。把 `rho` 从 0.18 提到 0.89
  （几乎完全消除 DD 拥挤）**换不来任何检测增益**。

（口径提醒：旁路 GLRT 不是生产检测器——生产是 SINR 软检测、主口径 P_D 0.7169。
本表是**旁路检测器**的证据，不能直接外推成"生产里加阵列也没用"。）

**对 P1-3 的含义（重要）**：`Ξ` 要优化的东西在这个工作点上**不约束结果**。
在投入 P1-3 之前应先确认约束到底是什么——现有三条独立证据都指向**回波能量 / 几何 / 跟踪精度**
（`sigma_p = 150 m ≈ 1.9 个距离单元`），而不是 DD 拥挤或掩蔽。

## 七、复现与门禁

```
PY=E:/anaconda/3_11_python/python.exe
$PY tools/run_aperture_check.py --trials 20 --out results_aperture_check   # ~2 min
$PY tools/run_aperture_detection.py --trials 20 --m-list 1,4,8 \
    --out results_aperture_det                                             # ~24 min
$PY -m pytest tests/test_aperture_production.py tests/test_aperture_manifold.py -q
```

上半（生产碰撞项）单测 6 条：默认门控关闭；`m_rx=1` 时计数与**整张链路表**逐位等于 DD-only；
`m` 增大只减不增且必须真的起作用；阵列**不许**改 `target_gain/direct_gain/dd_frac_loss`；
`rho` 是相干性（0…1、单阵元恒 0、1 个波束宽 ≈ 0.5）；软计数对间隔连续、无阈值。

下半（观测模型提升）单测 5 条：`m=1` 与门控关闭**逐位相同**（`y / X / A` 全等）；
维度 = `K × m`；导向向量单位模且诱导的内积**等于** `aperture.array_factor`（Kronecker 恒等式的
阵列半边——若它漂移，阵列会被静默忽略，**表现得像负结果**）；提升**不改模板范数**
（⇒ 匹配滤波 SNR 不变，收集增益没有被偷偷塞进来）；`rho` 必须随 `m` 上升。
