# advice/002 整合结论：先把 audit 的三处硬伤补掉，再谈 selective rescue

**日期**：2026-09-17
**输入**：`advice/002.md`（selective rescue 主线）+ `SYSTEM_INTEGRITY_AUDIT_2026-09-16.md`（11 项 FAIL）
**可执行证据**：`tools/probe_rescue_levers.py`，日志 `results/probe_rescue_levers.log`

> **⚠️ 初版本文有两个主张是错的，已更正**（2026-09-17 复核）：
> 1. 「瓶颈是送达、只有 49% 视图能送到」——**污染结果**。我给 `predicted_pd_for_links` 传了
>    `plan=None`，那意味着 **legacy「报告回传给感知发起方 i」路由**（`reporting.py:79-100`），
>    而 V1 是 `fusion.mode="explicit"` + `nearest_target`。改用 `assign_fusion_nodes` 后覆盖率
>    49% → **64.7%**。
> 2. 「`P̂_D < P_FA` ⇒ 代理非物理」——**判据用错**。名义 P_FA 不是正确的比较基准。
>    实测 CF 阈值下的真虚警率 **0.0494**（标定是对的，去掉 CF 反而 0.0619），真 P_D **0.0490**，
>    P_D ≈ P_FA 符合物理。代理只是**偏低约 24%**（0.0371 vs 0.0490）。
>
> 结论主线不变（looks 第一 / power 理由错误 / §12 实验无判别力），但**绝对数值不可用于论文**。

结论一句话：**advice 002 的主线可以走，但它在发布工作点上的可用收益很小；而且任何结论都必须
先在「正确的 reporting plan + 多链路融合」下复核，不能用单链路代理的数值当系统结论。**

---

## 零、锁定场景：600 m / RCS 0.1 m²（本线路以此为准）

```bash
python tools/probe_rescue_levers.py --area 600 --rcs 0.1
```

| 项 | 值 |
|---|---|
| 基线 rinr | **+11.05 dB**（r = 12.7）→ **这里确实干扰受限** |
| 送达覆盖率 | 68.0 % |
| 基线 worst P_D（L=16） | **0.1146** |
| 已达标目标 | **5 / 10**（P_D ≥ 0.95）⇒ 重新分配**有预算可挪** |

**杠杆实测（最差目标，单条最佳可送达视图）**

| 杠杆（成本） | worst P_D | Δ | 等效 γ |
|---|---|---|---|
| L=32（预算 ×2） | 0.1569 | +0.042 | +1.32 dB |
| L=64（×4） | 0.2256 | +0.111 | +2.78 dB |
| L=144（×9） | 0.3661 | +0.252 | +4.71 dB |
| L=256（×16） | 0.5222 | **+0.408** | +6.28 dB |
| power ×1.25 | 0.1165 | +0.002 | +0.06 dB |
| power ×2 | 0.1196 | +0.005 | +0.16 dB |
| power ×10 | 0.1242 | +0.010 | +0.30 dB |

**这一组把 advice 002 的排序完整证实了**，而且这次是「理由正确的证实」：

- **§6 looks 是第一资源**：✓ ×16 的 CPI 换 +0.408，是本场景唯一强杠杆。
- **§7 power 是二级**：✓ 而且**这次理由成立**——rinr = +11 dB 真的 ≫1，
  `m(1+r)/(1+mr)` 在 r=12.7 时饱和（×10 只买 +0.30 dB）。
  （对比：4 km 发布点 r = −0.06 dB 时 ×10 能买 +3.65 dB，结论不能跨工作点搬。）
- **§3 易目标停止投入**：本场景 5/10 已达标，**这个机制第一次真正有东西可挪**。
  校准曲线显示到达 0.95 需要约 **+12 dB 纯 γ**，而 256 looks 只买到 6.28 dB。

**预算换算（这是给论文的关键一句）**

- 现实预算 ×2（总 320 looks，易目标各留 16）→ 弱目标各得 48 looks → worst P_D ≈ **0.19**；
- 想摸到 0.52，需要约 **8.5 倍总预算**（5×256 + 5×16 = 1360 vs 基线 160）。

⇒ 诚实表述：**600 m / 0.1 m² 下，selective rescue 在 2 倍预算内把 worst P_D 从 0.115 抬到约 0.19；
剩余缺口是链路预算边界，不是调度能补的。**

### 与 600 m / 0.1 m² 对照（用户已补齐 500 m / 0.2 m²）

| | 600 m / 0.1 m² | **500 m / 0.2 m²** |
|---|---|---|
| rinr | +11.05 dB | **+11.52 dB**（同样干扰受限） |
| 送达覆盖率 | 68.0 % | 69.3 % |
| 基线 worst P_D | 0.1146 | **0.1740** |
| L=32 / 64 | 0.157 / 0.226 | **0.255 / 0.385** |
| L=144 / 256 | 0.366 / 0.522 | **0.624 / 0.819** |
| power ×10 | +0.010 | +0.014（同样饱和） |
| 已达标 | 5/10 | **6/10** |
| 到 0.95 需要 | ~+12 dB γ | **~+10 dB γ** |
| 预算 ×2 → 弱目标各得 | 48 looks → P_D ≈ **0.19** | 56 looks → P_D ≈ **0.36** |
| 预算 ×4 → 弱目标各得 | 112 looks → P_D ≈ **0.33** | 136 looks → P_D ≈ **0.60** |

**500 m / 0.2 m² 明显更适合做小目标主实验**：基线已经 0.174（不是贴着虚警地板），
looks 256 能摸到 **0.819**，且 ×4 预算就能到 ~0.60——rescue 机制有可见的收益曲线可画。
600 m / 0.1 那组则全程在低位爬（×2 只有 0.19），说服力弱很多。

⇒ 建议：**主实验用 500 m / 0.2 m²**，600 m / 0.1 m² 留作"更难"的对比点。
（两者都仍须配混合 RCS，否则「抠易目标」的机制在 0/10 达标时无处发力。）

---

## 一、先把三个工作点的结论摆出来（对比用，非锁定场景）

所有数字来自 `probe_rescue_levers.py`，口径 = V1 预设 + `target-local-v1`，单链路最佳可送达视图，
最差目标（`min P_D`），`P_FA = 0.05`。

| 场景 | rinr | **送达覆盖率** | 基线 worst P_D | looks 16→256 | power ×10 |
|---|---|---|---|---|---|
| **4 km（发布）RCS 0.05** | **−0.06 dB** | **49.0 %** | 0.0371 | 0.0470 (+0.0099) | 0.0373 (+0.0002) |
| **4 km（发布）RCS 0.2** | −0.06 dB | 49.0 % | 0.0376 | 0.0487 (+0.0111) | 0.0384 (+0.0008) |
| 400 m RCS 0.05 | +12.4 dB | 58.6 % | 0.0668 | 0.2031 (**+0.1363**) | 0.0708 (+0.0039) |

「送达覆盖率」= 候选视图里 `received_moments().m1 > 0` 的比例；其余的视图**报告从未送达**。

---

## 二、advice 002 十二条逐条裁决

| 节 | 主张 | 裁决 | 依据 |
|---|---|---|---|
| §3 | min-max 缺口 `d_q = [P_D^req − P̂_D]_+` | **需改写** | 弱目标区 `P̂_D < P_FA`（非物理），缺口被夸大到超范围 |
| §4 | 用 `predicted_pd_for_links` 做边际评估 | **⚠️ 不可直接用** | 见第四节：它会把零证据链路排到真链路之上 |
| §6 | looks 是第一资源 | **✅ 结论成立**，**❌ 理由错误** | 见第三节 |
| §7 | power 降为二级，理由是 `r ≫ 1` | **❌ 理由失效** | 发布点 rinr = −0.06 dB，不是 ≫1。结论仍对，但必须换论据 |
| §8 | DD refinement 只给值得救的链路 | **已达一半** | V1 预设已 `refine.enable=True`（`config.py:771`），再做成选择性只省算力、不涨 P_D |
| §9 | fusion 降级为「保住已形成的证据」 | **❌ 低估了 severity** | 真实瓶颈是「证据没到」，不是「证据丢失」，见第四节 |
| §11 | min-max 问题表述 | **需两级化** | 见第五节 |
| §12 | 全部目标 RCS 0.05 跑 5 方法链 | **❌ 没有判别力** | 0/10 达标 ⇒ 没有可从易目标抠出的预算，见第六节 |

---

## 三、looks 赢，但不是因为「干扰受限」

advice §7 的核心论据是一个前提：`r = I_residual/N_0 ≫ 1`。
审计 C4 已经证明这个前提在发布工作点**不成立**（rinr = −2.8 dB，本次探针 min-target 支路上测到 −0.06 dB）。
而探针显示 power 在发布点**确实变强了**：×1.25 买 **+0.50 dB**（400 m 只有 +0.07 dB），
×10 买 **+3.65 dB**（400 m 只有 +0.36 dB）——相差一个数量级，和 `m(1+r)/(1+mr)` 的预测一致。

那为什么 looks 仍然排第一？**因为 looks 是样本数杠杆，压根不经过那个分母。**
它对所有工作点都是同一个 √L 关系，而 power 的杠杆强度取决于 `r`。
⇒ **结论保住了，但论据必须换**。论文/文档里若写「因干扰受限故不用功率」，审稿人一算 r 就穿。

⚠️ **不要引用上表「equivalent gamma dB」的绝对值**（+16 dB 之类）。
在发布点校准曲线近乎平坦，一个微弱 P_D 变化反解出的 dB 会爆表。它只能用来**排序**。

---

## 四、~~真正的瓶颈：一半视图没送到~~ → 更正：那是 legacy 路由造成的

初版这句我判错了。原因是我给 `predicted_pd_for_links` 传 `plan=None`，
按 `reporting.py:79-100` 的语义那是 **legacy「回传给感知发起方 i」**路由，χ 取 `chi_comm[j,i]`
——4 km 上这条腿大面积失败，于是 m1 ≡ 0。而 V1 是 `fusion.mode="explicit"`、
`rule="nearest_target"`，必须走 `assign_fusion_nodes()`。

改成正确 plan 后：**送达覆盖率 49.0% → 64.7%**。所以「report 可行性」仍然值得在候选生成里
显式考虑（毕竟还有 ~35% 视图送不到），但它**不是那个压倒性的瓶颈**，也不足以改写 advice §9。

⇒ 实操价值仍在：候选集里带一遍 `report_chi(...) > 0` 过滤是廉价的，只是别把它吹成主线结论。

---

## 五、~~§4 的评估器是坏的~~ → 更正：它没坏，只是偏保守（我判据用错了）

我原先用「`P̂_D <` 名义 `P_FA`」当非物理证据 —— **这个判据本身就是错的**。
用代码自带的精确 LLR 采样复核（γ = 3.7e-4，L = 16，4 km）：

```
H0 经验偏度 0.5079   vs  CF 采用的 2/sqrt(L) = 0.5000      ✓ 公式对
真实 95% 分位 z = 1.7788 vs CF 阈值 z_cf = 1.7870           ✓ 标定好
CF 阈值实测： P_FA = 0.0494（名义 0.05）  → 标定是对的
              P_D  = 0.0490                → P_D ≈ P_FA，符合物理
去掉 CF 反而：P_FA = 0.0619                → CF 确实在起作用
代理公式：     P_D = 0.0371                → 比真值低约 24%
```

所以：**检测器标定没问题，`predicted_pd_for_links` 也没崩，它在深低 SNR 区只是偏保守。**
对 advice §4 的真实影响只有一条——**绝对缺口 `d_q` 被夸大**，用它做 max-min 权重会失真；
用于**排序**相对安全。建议：跑排序用它，报绝对值前用一次 MC 校准。

---

## 六、§12 的实验设计在这个配置下测不出东西

advice §12 提议「10 个目标全部 RCS 0.05」跑 5 方法链。
实测在发布点：**0/10 目标达标，per-target best-view P_D spread 只有 0.169**。

⇒ 「容易目标一旦够了就停止投入」这一步**一分钱也释放不出来**——没有容易目标。
整个 reallocation 机制退化成「在同样难的目标之间挪预算」，净收益趋零（这正是既有
「算法只能重分配预算，造不出预算」结论的翻版）。

**必须改成有判别力的矩阵**：一组**混合 RCS**（例如 3 个 5 m² + 7 个 0.05 m²），
这样「易目标释放预算 → 喂给难目标」才有自由度，也才是这个算法真正主张的价值。
否则跑出来的 5 方法链会是平的，且你会错误地归因成「算法无效」。

---

## 七、advice 末尾那个 boxed question 的答案（很难看，但这就是答案）

> 完全不增加额外硬件增益时，现有可控 sensing operations 能把 0.05 m² 的 worst-P_D 从多少提到多少？

**发布工作点：从 0.037 到 0.047（looks 提到 256，即 16 倍 CPI 时间）。**
仍然**低于虚警率本身**，也就是统计意义上仍不可检。power 拉满（×10）只到 0.0373。

对照 400 m 几何确实能到 0.203（+0.136），但那是另一个工作点，论文主结果不能搬。

⇒ 诚实的结论：**在发布工作点，算法侧的喘息空间 ≈ ΔP_D 0.01，且跨不过可检门槛。**
剩余缺口不是算法问题，是链路预算边界。这和 advice 建议「暂时拿掉 17.5 dB hardware gain」
的方向一致，但量级比它期望的「0.21 → 0.75」严峻得多——需要按这个数字重写叙事，
不能再假设「算法还能救」。

---

## 八、推荐的执行顺序（含我看其中的东西）

**先修 audit 里那两条会被静默吞掉的（否则所有实验建立在不确定口径上）**

1. ~~`tools/audit_coupling_changes.py:255,333` 丢弃返回值~~ **撤回**——初版 H3 的行正则误报，
   两处分别是「作为实参传入」和「try 内负向测试」，已改用 AST 判据，H3 现为 PASS。
   `apply_overrides` 返回 deepcopy 这件事依然是真的、依然危险，**但目前没有真实踩坑点**。
2. `model.py:613` 前加口径枚举硬断言（`config.py:987` 校验 `.lower()` 但不写回）→ 关掉 A1/A2。
3. 决定 D1：要么像 v1.1 显式钉 `comm_error_model=erasure`，要么改论文为 CF 描述。**当前状态是"论文说的检测器分支是死代码"。**

**再修本线路本身的三个前置（缺一个都别启动大扫描）**

4. 候选生成加 `report_chi(...) > 0` 过滤（廉价、无害）。**不需要** clamp 到 P_FA——
   代理没坏，只是偏保守；clamp 反而会把真差异抹平。
5. ~~送达过滤吃掉 51% 免费收益~~ → 更正为 35%（正确 plan 下覆盖率 64.7%）。收益在，但没那么戏剧性。
6. 实验矩阵改用**混合 RCS**（§12 的坑）。
6b. **任何数值结论都必须在「多链路融合 + 正确 plan」下复核**：本文所有 P_D 都是单链路，
   与发布系统（小目标融合后 0.18–0.52）不是一个口径，**不可直接引用数值**。

**然后才值得实现 selective rescue 本身**

7. 两级形式：Level 1 保证每个弱目标 ≥ K_min 条**可送达**视图；Level 2 在可行集上分配 `Σ L_q ≤ L_total`。
8. 在此之前**不要**动 power main 变量：它在发布点只值 +0.5 dB @ ×1.25，且在 r 随几何翻转时不稳。

---

## 九、本次过程中的四次自欺（都已修正，注明以防重蹈）

1. **`sensing_processing_gain` 是绝对值覆盖，不是倍数。** 传 1.0 等于把处理增益从 4096 打到 1（−36 dB），
   整条校准曲线被压到地板。已改为按 `N*L` 缩放，并加了 self-gate 断言「传自然值必须逐位复现默认」。
2. **按 γ 挑"最佳视角"会挑到报告送不到的链路**，导致所有杠杆读成 0 dB。已改为在 `m1 > 0` 的链路内排。
3. **按 P_D 挑更糟**：零证据链路停在地板 0.05，真链路被算成 0.037，**argmax 反向挑选**。
4. **用「`P̂_D <` 名义 P_FA」判非物理——判据错了。** 名义 P_FA 不是正确的比较基准：
   CF 阈值下实测 P_FA=0.0494、P_D=0.0490，检测器标定良好；代理只是偏低 24%。
   正确不变量是「MC P_D ≥ MC P_FA」，不是拿代理值去比一个标称常数。

（注：`equivalent dB` 在准平坦区会失真，只用于排序，不引用绝对值。）
⇒ **总教训**：我这轮的"离谱结果"里，四次有三处是我自己的评估口径错，**没有一处是你的仿真代码错**。
下次看到反直觉数字，先怀疑探针的调用契约（plan / 覆盖语义 / 比较基准），再怀疑物理。
