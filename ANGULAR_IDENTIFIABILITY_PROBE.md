# 角度维存在性探针：DD 重叠是不是「本征」的？

> 判据在跑之前就写死（`tools/probe_angular_identifiability.py` 的模块 docstring + 常量），
> 结论按当时写的规则给出，**没有事后调阈值**；规则本身的一处设计缺陷也原样保留并标注。
> 日期 **2026-09-19**。口径：`paper-canonical`，600 m，RCS 0.1，seed 2026，24 trials。

---

## 一、要回答什么

`TP_UIC_V12.md` §3.3 记下的结论是：十个散射体共享少数 DD 单元 ⇒ 互不可辨识
（`rho` 1.00 → 0.381 → 0.019），且**完美对消与完美跟踪器都救不回来**。那句话当时的措辞是
「**在当前 DD-only 接收机抽象下**不可辨识」——因为模板只在时延—多普勒里。真实 UAV 载荷带阵列，
两个同 DD 单元但不同方位的目标，其模板是

```
a(τ, ν, φ) = a_DD(τ, ν) ⊗ a_arr(φ)
```

DD-only 抽象把它们**按构造**压成同一个向量。本探针测：这个缺失维是不是全部故事。

## 二、预注册判据（跑之前写死）

`BW_u(M) = 0.886 / M`（半波长 ULA 的 3 dB 波束宽度，`u = sinφ` 单位）。所有间隔**只用波束宽度报**，
不用度——因为波束宽度随 `M` 变，这正是机理所在。

| | 判据 |
| --- | --- |
| PASS | 某个 `M` 同时满足：`rho(0.5 BW) ≥ 0.9`，且 ≥50% 共位目标对的间隔 ≥0.5 BW |
| FAIL | `rho(0.5 BW) < 0.5`，或达标比例 <20%，或达到 `rho ≥ 0.9` 所需间隔 ≥2.0 BW |
| 其他 | 中间，两个数一起报 |

> ⚠️ **规则自身的缺陷（跑完才发现，保留原样并标注）**：`RHO_PASS = 0.9` 对应的间隔恰好是
> **2.0 BW**，也就是 `BW_FAIL` —— 第 2 条 PASS 条件和第 3 条 FAIL 条件其实是**同一个条件**。
> 这条规则无法区分「所需口径不现实」与「需要给一个有限损耗预算」，它的结论字符串**结构上必然是
> FAIL**。因此下面同时给出两种损耗预算下的读数，并把判决写清楚，而不是让规则字符串替我说话。

## 三、方法（三件事必须先说清）

1. **不许把阵列增益算成可辨识性。** 阵列同时买两样东西：**口径增益**（`+10log10 M` 的收集功率，
   属预算杠杆，与 `G_hw` 同级）和**空间选择性**（波束变窄，原本落在一个模板上的两个方位分开了，
   属可辨识性杠杆）。本探针只测后者：**导向矢量单位模**，总收集功率与 `M` 无关，
   模板尺度与 `cancellation.kernel_vector` 完全一致。没有这条守卫，曲线会因为"什么都更强了"而上升
   ——这正是 `G_hw = 15 dB` 那一行犯过的错。
2. **白化用 oracle `σ²I`**（`perfect_channel` 臂的 `C_res`，低秩项 rank 0）。本探针测**几何**；
   `C_res` 标定（belief 误差那一项）是另一件事，不许它影响可辨识性判决。
3. **模板与偏移全部来自生产函数**：`cancellation.kernel_vector`、`cancellation.target_link_offset`、
   同一条回波功率式 `P_sense·g·G_proc·G_hw`、同一份 **belief** 几何（接收机用自己的信念导向）。
   模板**只用中心列**（无有限差分切向列），所以投影是精确正交投影、不涉及秩裁剪。

计算走 **Gram 域**（等价而非近似）：`<a_DD1⊗a_arr1, a_DD2⊗a_arr2> = <a_DD1,a_DD2> · A(u1,u2)`，
于是 `K·M = 65536` 维投影变成 150×150 的矩阵运算。

## 四、结果

### 4.1 A 段：同 DD、只有 Δφ 不同（确定性，无随机）

`rho` 对 Δφ/BW 的曲线：

| M | 0.10 BW | 0.20 BW | **0.50 BW** | 0.75 BW | **1.00 BW** | 1.50 BW | **2.00 BW** | 3.00 BW |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 4 | 6.0e-3 | 2.4e-2 | **0.143** | 0.299 | **0.479** | 0.810 | **0.981** | 0.938 |
| 8 | 6.3e-3 | 2.5e-2 | **0.149** | 0.310 | **0.495** | 0.823 | **0.983** | 0.954 |
| 16 | 6.4e-3 | 2.6e-2 | **0.151** | 0.313 | **0.499** | 0.826 | **0.984** | 0.957 |
| **1（DD-only）** | 0 | 0 | **0** | 0 | **0** | 0 | **0** | 0 |

- **解析核验**：曲线与 Dirichlet 核 `1 − |A|²`（`A = sin(MπΔu/2)/(M sin(πΔu/2))`）
  最大偏差 **2.2e-16**，即阵列那一半模板是被独立公式钉住的。
- **曲线只依赖 `M·Δu`**，所以在**波束宽度**单位下与 `M` 无关（渐近；`M=4` 处最大偏差 1.6e-2）
  ⇒ 用度报会把结论绑到口径选择上，这正是必须用波束宽度的理由。
- **半个波束宽度不够**：`rho(0.5 BW) ≈ 0.15`，即 85% 的模板能量被掩掉（约 −8.2 dB）。
  达到 `rho ≥ 0.9`（0.5 dB 损耗预算）需要 **2.0 个波束宽度**；3 dB 预算（`rho ≥ 0.5`）需要 **1.0 个**。
- 3 BW 处 `rho` 反而略降（0.94）：Dirichlet 旁瓣 ⇒ **间隔更大不一定更好**。

### 4.2 B 段：场景里实际发生的间隔（184 对共位目标，24 trials）

| M | 口径 (m) | 中位间隔 (BW, 最优轴) | ≥1 BW | ≥2 BW |
| --- | ---: | ---: | ---: | ---: |
| 4 | **0.08** | **1.73** | 67% | 43% |
| 8 | **0.18** | **3.46** | 82% | 67% |
| 16 | **0.38** | **6.93** | 91% | 82% |

「共位」定义：**至少存在一个照明机**把该目标对映射到 ≤0.5 DD 单元内（对 DD-only 接收机最有利的
情况都分不开，才算共位——保守口径）。绝对 DD 坐标取 `bin + frac`（`*_frac` 是 bin 内带符号偏移）。

⇒ **中位共位对在 8 cm 口径下就有 1.73 个波束宽度的角间隔**。「同 DD 单元」远不等于「同方位」：
一个 delay bin 在 B = 1.92 MHz 下是 156 m 的**距离**，所以同距离窗内两个目标可以相隔几十度。

### 4.3 C 段：真实场景的 `rho`，有阵列 vs 无阵列（这是判决量）

噪声项=其余目标的**信念**回波，幅度=生产回波功率，平均=生产功率加权：

| M | `rho_weighted`(x) | `rho_weighted`(y) | `rho_median` |
| --- | ---: | ---: | ---: |
| **1（DD-only）** | 2.279e-01 | 2.279e-01 | **4.10e-04** |
| 4 | **7.299e-01** | 7.451e-01 | 7.417e-01 |
| 8 | **8.715e-01** | 8.705e-01 | 9.406e-01 |
| 16 | **9.328e-01** | 9.441e-01 | 9.783e-01 |
| 归档 DD-only 审计（对照） | 0.0080 | — | 1.83e-04 |

> ⚠️ **本表于 2026-09-19 重跑并修正**（上一版数字由 `escape_from_gram` 的共轭缺陷产生，见 §4.5）。
> 旧值：M=1 `0.000`，M=4/8/16 `0.470 / 0.656 / 0.800`。
> 新值如上。**结论方向不变、量级更清楚**，但两处口径必须分开说：
>
> * **`rho_median`（典型模板）**：`4.1e-04 → 0.742 / 0.941 / 0.978`。
>   这才是「DD-only 下典型目标不可分」的证据——比 M=1 高了 3 个数量级。
> * **`rho_weighted`（功率加权）**：`0.228 → 0.730 / 0.872 / 0.933`。
>   M=1 就已经是 0.228，因为**加权平均被少数强回波主导**——少数功率大、DD 上孤立的目标
>   在 M=1 也有 `rho ≈ 1`，把均值拉起来。所以「DD-only 退化」这句话要用**中位数**说，
>   用加权均值会被那几个强目标盖住。
> * 与归档 `0.0080` 的关系：那是生产审计对**单个被测目标**、按其 nuisance 模板加权的值，
>   与本表「对全部 140 个模板加权」不是同一聚合，**不可直接比较**；只有量级参照意义。

### 4.4 C2 段：口径证书

同一 trial、同一对生产字典列：生产 `_project_out` 得 `rho = 9.976e-01`，Gram 捷径得 `9.976e-01`，
**最大相对差 2.33e-15**。这是「Gram 捷径 = 生产函数」的证书，也是它先后抓出前两个缺陷的地方
（DD 偏移顺序写反、原始幅度 Gram 让 `pinv` 返回 `rho = −1.4e6`）。

### 4.5 C3 段：为什么 2.33e-15 的证书没抓出第三个缺陷（**2026-09-19 修**）

`escape_from_gram` 原来算的是 `conj(u) @ G_ss⁺ @ u`，正确是 `u @ G_ss⁺ @ conj(u)`
（`u = corr[c, s]`）。差别是 `G_ss⁺` 的**转置**：

* **单列 nuisance 时 `G_ss⁺` 是标量**，两个式子都等于 `|u|²` ⇒ 完全正确；
* 而 C2 证书取的正是**两模板**用例（1 个 nuisance 列）——**错误在这个 regime 里不可见**；
* `|others| ≥ 2` 且复数据、Gram 非对角时，偏差实测：n=2 **0.0**、n=3 **2.3e-01**、
  n=6 **2.1e-01**、n=20 **7.3e-02**，并且会**变负**（被 clip 成 0，读成"完全不可逃逸"）。

⇒ 上一版 §4.3 的 `M=1 → 0.000` 有一部分是这个缺陷造成的（负值被截断），不是物理结论。
修复后已重跑（见 §4.3 表），并补了 5 条断言：`n_col ∈ {2,3,6,20}` 与向量域逐位比对
（`n_col=2` **故意保留**，它正是缺陷不可见的那一档），以及 Schur 补 `rho = 1/inv(corr)[c,c]`
这条**没有转置自由度**的第二意见。

教训（已进技能）：**等价实现的互证必须落在二者可能分歧的 regime 上**；
且测试里"未查清的 1e-6 残差"不要以"低于阈值"归档 —— 那个残差就是缺陷本身。

## 五、判决

**规则字符串：FAIL（三个 M 全部 FAIL）。** 触发条件是第 1 条 PASS 条件（`rho(0.5 BW) ≥ 0.9`）
与第 3 条 FAIL 条件（`need ≥ 2.0 BW`）——而这两条按 §二 的标注是同一个条件。

**按两种损耗预算的实际读数**：

| M | 口径 | 3 dB 预算（需 1.0 BW） | 0.5 dB 预算（需 2.0 BW） |
| --- | ---: | ---: | ---: |
| 4 | 8 cm | **67%** 的共位对达标 | 43% |
| 8 | 18 cm | **82%** | 67% |
| 16 | 38 cm | **91%** | 82% |

**结论（这是用户给的两条分支之外的情形）**：

> 用户给的两条分支都没干净地命中。
> **「半个波束宽度就能分开」不成立**（那只有 `rho = 0.15`，反而被强掩），
> 但**「需要几何上不现实的 Δφ」也不成立**——所需是 2 个波束宽度，而场景中位间隔是 1.73–6.93 个，
> 对应 **8–38 cm** 的口径。
> 所以这是一个**第三条**：**重叠确实主要是 DD-only 抽象的产物，可以用一块很小的阵列大幅救回，
> 但不是全部救回**（8 cm 下 43% 的共位对在 0.5 dB 预算内可分，18 cm 下 67%）。

## 六、对项目意味着什么

1. **`TP_UIC_V12.md` §3.3 的措辞必须改**：从「受 DD 域分辨率限制的**本征**重叠」改为
   **「DD-only 接收机抽象下的重叠」**，并补上本探针的量化（§四、§五）。这不是措辞修饰：
   §3.3 那句支撑了「别再加深对消器」的判断，而本探针说明**真正的瓶颈是接收机有没有空间维**。
2. **P1-2 / P1-3 升为主线**，且顺序明确：先做空间-DD 流形（P1-2），再做以可辨识性为目标的选择器
   （P1-3）。P1-3 的 `Ξ` 目标函数此时才有可操作的东西——
   在 DD-only 下 `rho ≈ 0` 对所有链路成立，选择器无物可选。
3. **口径是必须声明的硬件假设**（与 `G_hw` 同级，不是免费午餐）：`M` 元半波长 ULA、
   指向可调、只看一条体轴；8–38 cm 的量级要写进 assumptions 并给出实物依据。
4. **本探针没有建模的代价（必须写进 limitations）**：
   - 一维阵列有**前后模糊**（`Δu = 2` 处 `|A| = 1` 混叠）与方位覆盖限制；要全向需二维阵；
   - **方位角需要被估计**（本探针给了信念方位，理由是协作几何共享位置；
     真实系统还要有空间谱估计这一步，且它的误差未被建模）；
   - 互耦、阵元校准、幅相不一致全部忽略；
   - 白化取了 oracle `σ²I`，因此**没有**把 `C_res` 标定误差算进去（那会**降低**可分辨性）。

## 七、另一轴（分辨率）的前置条件：`N`/`L` 不是分辨率旋钮

本探针回答的是**空间维**这一轴。同一问题的**分辨率轴**（原清单 #2）在开跑前先做了一次数值探针
（`tools/probe_waveform_mapping.py`）。结论比原判断更强：**`N ≠ L` 时，模型里同时存在两个「带宽」。**

映射（`isac_sim/model.py`）：

| 量 | 表达式 | 取决于 |
| --- | --- | --- |
| `delay_bin` | `round(τ·L·Δf)`，有效 `0 ≤ l < L` | **L** |
| `doppler_bin` | `round(ν·N·T)`，有效 `−N/2 ≤ k < N/2` | **N** |
| `bandwidth` → `noise_power` | `N·Δf` | **N** |
| `G_proc` | `N·L`（= `B·CPI`，物理正确） | N、L |
| `K` | `N·L` | N、L |

⇒ 延迟轴的**量化尺度**是 `L·Δf`，而噪声带宽（以及 `1/B` 给出的**物理**延迟分辨率）是 `N·Δf`。
**默认 `N = L = 64` 让二者恰好相等** —— 与本项目其它「默认值掩盖不一致」（`G_hw`、
`EPS` 守卫）同源：模型开箱自洽，一旦独立扫其中一个就不再是。

实测（`--area 600`，`Δf = 30 kHz`）：

| N×L | B (MHz) | 噪声 × | G_proc | 延迟格 (m) | 物理延迟格 `1/B` (m) | 多普勒格 (m/s) | 自洽 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 64x64 | 1.92 | 1.00 | 4096 | 78.1 | 78.1 | 11.9 | yes |
| 64x128 | 1.92 | 1.00 | 8192 | 39.1 | 78.1 | 11.9 | **NO** |
| 64x256 | 1.92 | 1.00 | 16384 | 19.5 | 78.1 | 11.9 | **NO** |
| 128x64 | 3.84 | 2.00 | 8192 | 78.1 | 39.1 | 6.0 | **NO** |
| 256x64 | 7.68 | 4.00 | 16384 | 78.1 | 19.5 | 3.0 | **NO** |
| 128x128 | 3.84 | 2.00 | 16384 | 39.1 | 39.1 | 6.0 | yes |
| 256x256 | 7.68 | 4.00 | 65536 | 19.5 | 19.5 | 3.0 | yes |

以及同一几何、同一 seed 下的**生产碰撞惩罚**（`dd_collision_count`，`α = 1.00`）：
碰撞链路占比 64x64 **11.9%** → 64x256 **2.3%** → 256x64 **2.7%**；最差 4.77 dB（count = 3），
**中位 1.0000**。

三条读数：

1. **只加 L（64→256）：B 与噪声完全不动**，`G_proc` ×4，延迟格 78.1→19.5 m，
   碰撞链路 11.9%→2.3% ⇒ **纯重量化**，物理分辨率没变。
2. **只加 N（64→256）：B ×4 ⇒ 噪声 −6 dB**，多普勒格 11.9→3.0 m/s（真实改善，CPI 变长），
   但延迟格不动 ⇒ 碰撞只到 2.7%：**4× 真实分辨率的碰撞收益不超过纯重量化**。
3. ⇒ **碰撞统计跟随取整格，不跟随带宽**（生产的 `dd_collision_count` 与旁路的 `distance_bins`
   都以取整格为基础）。所以 #2 的原始判据「扫 N/L 找可辨识性转折点」在**先裁定延迟轴映射之前
   不可解释** —— 比原清单里那句「必须同时报 Δτ/Δν」更强。

顺带一条独立证据：600 m 主工作点下生产碰撞惩罚只作用于 **11.9%** 的有效链路、中位 1.0000、
最差 −4.77 dB ⇒ **它不是当前瓶颈**。这与「继续加深对消器收益递减」是同一个方向的证据。

**未裁定（必须显式决定，不能默认）**：延迟分辨率到底读 `1/(L·Δf)` 还是 `1/(N·Δf)`。
当前行为已用断言钉住（`tests/test_waveform_mapping.py`，6 条），任何改动都要**先改断言** ——
这样「改映射」是一个可见的决定，而不是一次扫不出结果的扫描。

## 八、复现与门禁

```
PY=E:/anaconda/3_11_python/python.exe
$PY tools/probe_angular_identifiability.py --trials 24 --scene-trials 24 \
    --anchor-trials 6 --out results_angular_probe      # 约 12 分钟
$PY -m pytest tests/test_angular_probe.py -q          # 10 passed
$PY tools/probe_waveform_mapping.py --area 600 --collisions \
    --out results_waveform_mapping                     # §七，约 10 s
$PY -m pytest tests/test_waveform_mapping.py -q       # 6 passed
```

映射探针单测钉住的是**记账事实**（不是物理定律）：默认 `N == L`（即那个把不一致藏起来的
取值）；`bandwidth` 只由 `N` 决定、`L` 无关；延迟格随 `L` 缩放、多普勒格随 `N` 缩放；
两个「延迟格」读数只在 `N == L` 时相等；`G_proc = B·CPI`；
**重量化比 4× 带宽买到更多碰撞改善**（这条是「碰撞跟随取整格」的回归）。

单测钉住的不变量：波束宽度定义随 `M` 收敛到 3 dB 点；曲线对 `M·Δu` 的渐近不变性；
主瓣内单调而旁瓣处非单调（**故意允许**，避免声称"越大越好"）；
`rho` 对幅度缩放不变（原始幅度 Gram 那个事故的回归）；
`escape_from_gram` 与向量域一致；Kronecker 恒等式（若它失效，阵列项会被静默忽略，
**表现得和负结果一模一样**）。

**本探针不动主链路**：没有新增 config 段、没有改 `isac_sim/` 任何文件，只新增一个工具与一个测试文件。

⚠️ 一处**未查清**的数值项已如实记录在测试里：合成的良态两模板用例中，两条等价路径只吻合到
**1e-6**（真实生产向量上是 2.3e-15）。它比任何判决阈值低 5 个数量级（判决在 0.1 量级移动），
没有结论依赖它，但原因未知。
