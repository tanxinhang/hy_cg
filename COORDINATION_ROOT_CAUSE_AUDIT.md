# 根因审计：性能缺口的成因、来源、与数字准确性

**日期**：2026-09-17　**场景**：500 m / RCS 0.2 m²　**口径**：V1 发布（正交 + nearest_target）

这是宣布结果前的归因审计，回答三个问题：**为什么有缺口、性能从哪来、数字准不准。**

---

## 1. 根因：双站近远比 + 自泄漏（不是噪声，不是"干扰太多"）

对最差目标（q=3，最佳链路 i=5→j=1，`d_ij = 230 m`）把感知分母逐项拆开：

| 项 | 值 | 相对 n0 |
|---|---|---|
| 回波信号 `signal` | 3.99e-14 W | **+0.97 dB**（几乎贴着噪声底） |
| 噪声 `n0` | 3.83e-14 W | 0 dB |
| 残留自干扰 `res_self` | 8.0e-15 W | −6.8 dB |
| **残留直射 `res_direct`** | **3.54e-13 W** | **+9.6 dB（主导）** |
| 残留合计 | 3.62e-13 W | rinr = **+9.75 dB** |
| γ_sense | 0.098 | **−10.1 dB** |

**关键数字**：
- 照射机 i 的直射路径比目标回波强 **74.24 dB**（近远比）；
- κ_dc 对消 40 dB 后，仍剩 **34.24 dB** 压在回波之上。

⇒ **根因一句话：这是一个双站近远比问题。** 接收机 j 离照射机 i 只有 230 m，
i 的直射波比 i→目标→j 的双程回波强 74 dB；40 dB 的对消把 74 压到 34，
但这 34 dB 残留仍比噪声高 9.6 dB，把 γ 压到 0.098（−10 dB），
于是单目标 P_D ≈ 0.6、融合也够不到 0.95。

---

## 2. 性能从哪来（每个结果的机制）

| 观察 | 机制 |
|---|---|
| 基线 P_D ≈ 0.6（单链路口径） | γ ≈ 0.1：回波只比噪声高 1 dB，残留直射又压掉 9.6 dB |
| **协调增益 +0.1150 / +0.1050 / +0.1100 / +0.1100 / +0.0850 / +0.0850 / +0.0450 / +0.0350 / +0.0300**（L=16/32/40/48/56/64/96/144/256，**800-trial 最差种子**） | 静默其余 13 个节点 → 去掉它们的直射泄漏 → `res_direct` 从 9.6×n0 降到约 1–2×n0 → γ 翻倍（0.058→0.129）；**增益在 L≥48 后单调不增**（仅 L=56/64 持平）（时间越少，省的越多；L=16–48 四点平坦，跨度 0.010 = 2 格） |
| **协调的极限** | 照射机 i **自己的**直射近远比（34 dB）无法靠协调消除（i 必须辐射）——这是地板 |
| **power 无效** | `res_direct ∝ P`，信号也 ∝ P；r=9.75 dB ⇒ `m(1+r)/(1+mr)` 饱和，×10 只买 0.04 |
| **looks 有效** | 样本数杠杆，与 γ 无关，√L 直接进 d′ |
| 🛑 **「不协调摸不到 0.95」——整条作废**（2026-09-17 23:10 定案） | 800-trial 实测 uncoordinated **随 L 单调升**：**0.7800（16）→ 0.8300（32）→ 0.8350（40）→ 0.8450（48）→ 0.8700（56）→ 0.8850（64）→ 0.9200（96）→ 0.9400（144）→ 0.9550（256，4/4 达标）** ⇒ 不协调**能**达标，代价是 **16× 时间**。旧口径"恒为 0.9000 / 任何种子没到 0.95"是 200-trial 的 **min 偏差饱和假象**。正确表述：**协调把达标门槛从 16× 压到 3×**（省 5.3 倍），**不是**"跨过一道本来跨不过的墙" |
| **协调 + 时间可确定性达标，且 L=48 起进平台** | 800-trial 下 **L=48 / 56 / 64 / 96 / 144 / 256 的 sparse 均 4/4 ≥0.95**（**0.9550 / 0.9550 / 0.9700 / 0.9650 / 0.9750 / 0.9850**，六档跨度仅 0.030）⇒ 协调先把 γ 从 0.06 抬到 0.13，时间再在更陡的 `P_D(γ)` 区段兑现 √L；**协调一旦到位，时间就是冗余的**——对照 uncoordinated 同期仍 **+0.110**（0.8450→0.9550） |
| **🎯 三条达标路径的门槛**（最可引用的一条） | 未协调 **L=256（16×）**、仅静默 **L=96（6×）**、静默 + 重调度 **L=48（3×）** ⇒ **协调 = 5.3 倍积分时间节省**（只静默 2.7 倍）；门槛精确位置 **(40, 48]**（五点夹逼：16→0/4、32→1/4、40→2/4、48→4/4、56→4/4） |
| **协调 1× ≈ 4.4× 时间**（旧口径"不及 4×"**已反转**） | sparse@L16 = **0.8950**，**高于** uncoordinated 4×（0.8850）⇒ 一条协调信令 ≈ 4.4 倍积分时间，而它不花时间。（200-trial 版读 0.8400 才显得"不及 4×"——各档低估幅度不同，插值必偏） |
| **🎯 协调 × 对消深度（κ_dc）是替代品**（2026-09-18 00:33 定案，800 trials/臂/格，权威表 `COORDINATION_INTERFERENCE_ROUTE.md` **§13.6**） | **VERDICT: SUBSTITUTES**。两者攻击**同一个直射泄漏项**，且以**乘积**进分母（`residual_direct = κ_dc · I_sense_field[j]`，`I_sense_field = P_sense @ 掩码后 direct_gain`）⇒ 预测**饱和/负交互**。实测协调增益 **+0.1150（κ=40）→ +0.0100（50）→ +0.0000（60/80）**；κ 40→80 买 **+0.1700**（不协调臂）⇒ 交互 **+0.1150**，**不可叠加计功**。机制：不协调臂中位 rinr **+10.87 → +1.64 → −4.19 → −5.82 dB**，跨界 κ=50→60（四种子一致）。旁证：κ≥50 时 **`mask_only` ≥ `sparse`** ⇒「重新选择照射机」机制归零。 |
| **对消深度是协调的"备选路线"**（同上，反向的工程价值） | **κ=60 时 1× CPI 的不协调单臂就 4/4 达标**（0.9500，rinr −4.19 dB）⇒ 「+20 dB 对消」可替代「协调 + 3× 时间」。而 κ=40（发布值）下 1× CPI 两条路都不达标（0.7800 / 0.8950）⇒ 论文里该把**协调写成机制**、把**加深对消写成备选**（或反之），取决于硬件是否支持 60 dB —— **不能两个都当贡献**。 |

> ⚠️ **本表 P_D 数字口径**：正文数字优先取 **800-trial 精度复核**（`--mc 200`，格 0.005），
> 即 `COORDINATION_INTERFERENCE_ROUTE.md` **§13**；200-trial 前沿见该文 §12（**只作保守下界**）。
> 更早的 `0.875 / 0.975`、`+0.15` 属**粗表评分旧口径，已作废**。
> **⚠️ 200-trial 系统性低估**：2026-09-17 20:47–23:10 九档复核，**15/15 个可比格全部抬升**（+0.005~+0.055）；
> 本表凡涉及跨档比较、门槛定位、否定式断言的表述**一律以 §13 为准**。
> 第 1 节的双站近远比（74 dB / 34 dB）与 γ 分解**不受口径影响**，仍然有效。

**为什么缺口还剩 ~12 dB（不可再靠时间/功率）**：缺口是**相干信号**的缺口。
要跨过去只能靠三类：① 更深的直射对消 κ_dc（34 dB 残留 → 压到噪声下，需 κ≥75 dB）；
② 空域零陷（把 i→j 直射方向陷掉）；③ 更多相干处理增益 G_proc。
这三者都不是"调度"能给的——这正呼应 memory 里「算法只能重分配预算，造不出预算」。

---

## 3. 数字准确性校验（针对修改后的代码）

**发布路径未动**（已复验）：`check_release_identity --check` CLEAN、`pytest` 180 passed、
`tx_penalty=0/max_tx=None` 时选择结果逐位相同 ⇒ 所有已发布数值 bit-exact。

**MC 实验用的是真检测器**：`evaluate_detection`（精确 LLR + CF 阈值 + 丢包混合），
不是代理。三条臂 P_FA 实测 ≈0.044–0.054（标称 0.05）⇒ 修改后检测器标定依然正确，
增益**不是**阈值漂移换来的。

**归因可分离**（🔴 一律用 **800-trial** 值，最差种子；权威表
`COORDINATION_INTERFERENCE_ROUTE.md` §13.3 / §13.4）：

| L | 16 | 32 | 40 | 48 | 56 | 64 | 96 | 144 | 256 |
|---|---|---|---|---|---|---|---|---|---|
| 静默 `mask_only` − `uncoordinated` | +0.0350 | +0.0300 | +0.0500 | **+0.0700** | +0.0550 | +0.0450 | +0.0300 | +0.0200 | +0.0200 |
| 重调度 `sparse` − `mask_only` | **+0.0800** | +0.0750 | +0.0600 | +0.0400 | +0.0300 | +0.0400 | +0.0150 | +0.0150 | +0.0100 |
| **合计（协调增益）** | +0.1150 | +0.1050 | +0.1100 | +0.1100 | +0.0850 | +0.0850 | +0.0450 | +0.0350 | +0.0300 |

⇒ **「静默」与「稀疏重选」两条机制都在起作用，但权重随档位翻转**（翻转点 **L=40→48**）：
L=16/32/40 **重调度 ≥ 静默**（+0.0800 vs +0.0350；+0.0750 vs +0.0300；+0.0600 vs +0.0500）；
L≥48 **静默主导**（+0.0700 vs +0.0400 起）。物理上说得通——预算紧张时必须精挑"看哪些"，
预算一宽，关掉多余照射机就够。

> ⚠️ **达标必需性（800-trial 定案）**：`mask_only` 门槛是 **L=96（6×）**——L=16/40/48/56/64 的
> `mask_only` = 0.8150 / 0.8850 / 0.9150 / 0.9250 / 0.9300（**均 0/4 不达标**，L=64 需重调度补
> **+0.0400** 才到 0.9700）；L=96 起它自己就能到 0.9500。
> ⇒「`mask_only` 从未达到 0.95」**作废**。正确表述：**重调度在 L≤64 显著，L≥96 降为补稳**。
>
> 📌 旧表（200-trial）的分解 `+0.06/+0.02/+0.00/+0.02/+0.04`（静默）与
> `+0.04/+0.04/+0.06/+0.02/+0.04`（重调度）**一律作废**——200-trial 各臂低估幅度不同，
> 分解比总量更敏感。

**⚠️ 五个必须声明的精度局限**：
1. **量化格**：**800-trial** ⇒ P_D 量化到 **1/200 = 0.005 一格**（200-trial 前沿表是 0.02，只作下界）。
2. **⚠️ 200-trial 的 `worst-seed` 系统性偏低**（本次最危险的一条）：
   `worst-seed` = 「每目标 mc 次 trial 均值」再跨目标取 **min** ⇒ min-of-noisy-estimates
   在小样本下**向下偏**。九档复核实测 **15/15 个可比格全部抬升**（+0.005 ~ +0.055）,
   偏差量随臂与档位而变（低 P_D 臂偏得最多）⇒ **不能用统一常数校正**。
   ⇒ **跨档比较、门槛定位、与「从未/硬顶/摸不到」类否定断言必须用 800 trials**。
   本次因低估而翻掉的断言共 3 条（硬顶 0.90、`mask_only` 从未达标、增益非单调）。
3. **代理数字一律不可引用**：代理高估增益约一倍（+0.32 vs MC +0.16），
   还虚构了「协调放大 looks ×3」的交互。**只有 MC 表格是论文可用的。**
4. **最差目标的定义**是「mc 次 trial 平均 P_D 最低的那个目标」，不是逐 trial 取 min；
   这是标准做法，但要在论文方法节写明。
5. ✅ **~~`L=16` 尚未做 800-trial~~ 已补完**（`23:10`）⇒ 该档与"协调 1× ≈ 几倍时间"的
   表述现在都是 800-trial 口径：**sparse@L16 = 0.8950 ⇒ ≈4.4× 时间，略高于 4×**
   （200-trial 版"≈3.3×、不及 4×"**已反转**）。**800-trial 表已无档位缺口。**
6. ⚠️ **`worst-seed` 这个列名在同一脚本里存在两种含义（新发现，2026-09-18）**：
   `run_coordination_experiment.py` 的**表格列**打 `w.mean()`（4 种子的**均值**），
   而正下方那行**头条增益**打 `w.min()`（4 种子的**最小值**）。L=16 的日志可直接对照：
   `0.7800 → 0.8950 (+0.1150)`（MIN）vs `0.7988 → 0.9075 (+0.1087)`（MEAN）。
   ⇒ **本库全部已发布的门槛/增益都是 MIN 口径**（九档表已逐档复核），引用时**必须说明是哪一个**。
   κ 扫描的探针已强制拆成 `worst_min` / `worst_mean` 两个独立键，判定一律走 **min**。

**结论**：修改本身 bit-exact、检测器标定正确、归因可分离、机制解释完整——
**数字可信，但要带量化误差与"只信 MC"这两条脚注。**
