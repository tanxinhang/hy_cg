# 系统审计：2026-09-17（工作树）

> ## 📌 本文状态（2026-09-17 17:50 更新）
> 本文 **§1–§5 记录的是审计当刻（上午）的发现**，其中 **A7 / A8 / C3 已在当天下午修复或决断**，
> 见新增的 **§7 修复落地**、**§8 正式 MC**、**§9 评分口径错配**、**§10 轮数收敛诊断**。
> 引用本文任何 FAIL 条目时**先看 §7 的状态表**——「未修」的不要当既有事实用，
> 「已修」的不要当未修用。

**对象**：`D:\Desktop\conference` 工作树 = HEAD `acdcacc` + **未提交的协调改动**
（`isac_sim/config.py` +15、`model.py` +21/-2、`selection.py` +41）
**可执行证据**：
```
python tools/check_release_identity.py --check
python tools/parity_check.py --baseline --strict
python -m pytest tests/ -q
python tools/audit_system_integrity.py --deep
python tools/audit_coordination_gate.py          # 本次新增
```
唯一被信任的东西是 assert 与实测数字。读代码得到的印象不算证据。

---

## 0. 结论摘要

| 层 | 结果 | 判定 |
|---|---|---|
| **发布门禁（三道）** | 全部 **CLEAN** | 未提交改动没有动到发布身份与默认路径数值 |
| **系统完整性（深探针）** | **11 PASS / 10 FAIL** | 与 2026-09-16 完全相同，**无新增退化** |
| **未提交的协调改动** | **15 PASS / 2 WARN / 2 FAIL** | 默认路径 bit-exact；但**发现两条新缺陷**，且都属于「会被静默吞掉」那一类 |

**发现的两条缺陷（核心结论）**

1. **门控只关干扰、不关回波**（A7）。`compute_link_tables` 的 `signal` 由
   `effective_sensing_power * target_gain` 构造，**从不查 `active_tx_mask`**。
   实测：把**所有节点静默**后，2250 个感知 SINR 里仍有 **2100 个为正**（剩下的 150 个是
   对角线）。⇒ 掩码遗漏的照射机，其观测**不会作废，只是没了干扰**，性能被系统性高估。
2. **发布管线自己的掩码约定（报告机 `{j}`）踩中上一条**（A8）。`simulate.py:872-874`
   的 `active_tx[j]=True` 标的是**报告机**，而调度实际需要的是**照射机 `{i}`**。
   实测：该掩码静默了 **14 个仍被调度使用的照射机**，worst P_D 读作 **0.4575**，
   而物理上安全的照射机掩码只有 **0.3831** ⇒ **+0.0744 是不可实现的虚高**。

> **两条均已于当天下午修复**（修法与复测见 §7）。A7 修后：全员静默 2250 格中为正的
> `gamma_sense` **0 个**（原 2100），未门控对照仍 2100 ⇒ 不是靠「全归零」过关。

**同时确认已修好的部分**（本次改动的正向价值）：默认路径与 HEAD **逐位一致**（A1/B2），
门控全 True 为 no-op（A3），旧门控式对直射项的膨胀**精确等于 1/ρ = 1.25**（A5），
`rinr` 与解析门控式**误差 0.00e+00**（A4）。

---

## 1. 发布门禁（三道，全绿）

| 门禁 | 结果 |
|---|---|
| `check_release_identity.py --check` | **RESULT: CLEAN** — 94 冻结键 0 violated；19 default / 9 experimental；归档交叉 110 键比完；`archived-main[run.num_mc]` 1000→200 仍是「由入口供给」的可标定项 |
| `parity_check.py --baseline --strict` | **RESULT: CLEAN** — 1596 格比对，**0 differing**、0 below tolerance、0 unattributed |
| `pytest tests/ -q` | **180 passed, 6 subtests passed**（13.1 s） |

⇒ 新增的 `selector.tx_penalty` / `selector.max_tx_nodes` 既没进冻结键，也没改变发布路径数值。
**默认关（`tx_penalty=0.0`、`max_tx_nodes=None`）已实测确认**（B1）。

---

## 2. 未提交改动的逐条审计

### 2.1 `model.py` 门控口径修复 —— 修对了

| 编号 | 断言 | 结果 |
|---|---|---|
| A1 | 默认路径（gate off）与 **HEAD 快照**（`git show HEAD:isac_sim/model.py` 独立加载）**逐位相同** | PASS |
| A2 | gate off 时传任意掩码 → 结果不变（掩码在不开口控时是装饰） | PASS |
| A3 | gate on + 全 True 掩码 == 未门控发布表，**逐位相同** | PASS |
| A4 | `rinr` == 解析门控式 `(resid_self + κ·(P_sense⊙m)G)/(n0+eps)`，**误差 0.00e+00** | PASS |
| A5 | 修复前表达式对直射项的膨胀 **= 1/ρ = 1.250000**（ρ=0.8）⇒ 注释里的「+25% 干扰 / −0.89 dB」**成立** | PASS |
| A6 | 静默单调性：max rinr 全员 **22.65** / 子集 **9.25** / 全静默 **0.261** | PASS |

判据用的是 **HEAD 快照当独立参照实现**（不是「我觉得没改」），A4 用的是**解析式逐项对账**
（不是「数值看起来对」）。这是本次改动最扎实的一块。

### 2.2 `selection.py` 的 `tx_penalty` / `max_tx_nodes`

| 编号 | 断言 | 结果 |
|---|---|---|
| B2 | 默认关时 `select_lagrangian` 与 HEAD **逐位相同**（26 条链路，`D_sum=3247.885026`） | PASS |
| B3 | AST：`_greedy_lagrangian` 内 6 处 `active_tx` 读取**全部**处在两个守卫之下 | PASS |
| B4 | 硬上限对**返回的调度**生效：cap=None/1/2/3/5 → #TX = 11/1/2/3/5 | PASS |
| B6 | 软代价单调：penalty 0→0.2 ⇒ #TX 11→3 | PASS |
| B7 | 非法值**抛异常**而非静默返回空调度（`max_tx_nodes=0`、`tx_penalty=5.0`） | PASS |
| B8 | 合法代价（0.02）仍产出非空调度 —— 防护栏没有假阳性 | PASS |

### 2.3 新发现的两条（**均已于当天下午修复**，修法与复测见 §7）

#### ✅ FAIL A7 —— 门控只关干扰、不关回波　【已于 2026-09-17 下午修复】

```
全部静默 (mask = 全 False) 时：2100 / 2250 个 gamma_sense 仍 > 0（max 2.823e+03）
未静默的 150 个恰好是 i == j 的对角线
```

`signal` 不含掩码，`residual_direct = κ·I_sense_field[j]` 含掩码。后果是：**掩码让干扰消失、
却让回波保留**。因此任何「掩掉一个仍在被使用的照射机」的掩码，都会白拿一份该照射机的观测。
正确做法是 `signal *= mask[i]`（或整行置零），使被静默的照射机**不可观测**。

**实测修法**：`model.py` 增 `gate_echo` 分支——被静默照射机的 `effective_sensing_power = 0.0`
（`signal` 与 `raw_signal` 同源，一处覆盖两处）。复测见 §7 表。

#### ✅ FAIL A8 —— 发布管线自己的掩码约定踩中 A7　【已于 2026-09-17 下午结构性关闭】

```
illuminators {i}  #TX=11  worst P_D=0.3831   被静默却仍被使用的链路 = 0
reporters   {j}   #TX= 5  worst P_D=0.4575   被静默却仍被使用的链路 = 14
```
`simulate.py:872-874` 标的是**报告机 j**；`coordination.illuminator_mask` 标的是**照射机 i**。
两者不是同一集合，而 A7 让 `{j}` 这一侧凭空多出 **+0.0744** 的 worst P_D。

⚠️ 因此 **memory 里「接收机 j 不需要辐射，mask={i} 与 {i,j} 等价」这句需要限定条件**：
实测 **`{i}` ≡ `{i,j}`（确实逐位等价）**，但 **`{j}` 单独使用既不安全也不等价**。
「等价」只在**已经包含全部照射机**的前提下成立。

**结构性关闭**（而非打补丁）：① 掩码语义统一为「本 CPI 辐射的**照射机**集合」，
`simulate.py` 那个 `{j}` 掩码改名 `payload_tx` 并注明它**不是**协调掩码；
② `validate_config` 拒绝「门控 + 非 orthogonal / 非 shared_spectrum」——因为
`compute_link_tables` 门控干扰场却**从不门控通信信号**，在有并发载荷的口径下
「被静默的报告机」不是一个辐射集能描述的系统；③ `require_mask_covers_schedule`
在掩码静默了调度仍在用的照射机时**直接抛错**（实测：报告机掩码就是被这条守拒掉的）。

#### WARN C2 / C3 —— 代价只接进了 `_greedy_lagrangian`

AST 扫描（按函数体切作用域，不用行正则）结果：

- **接入了**：`_greedy_lagrangian`
- **未接入**：`select_lagrangian`、`select_c2f`、**`select_c2f_adaptive`**、
  `select_all_neighbor`、`select_topk_baseline`、`select_budget_ranked_baseline`、
  `select_global_budget_baseline`

**`select_c2f_adaptive` 就是发布方法** `proposed_c2f_adaptive_pd` 的入口
（`simulate.py:746`）。它有自己的 **coarse rollout**（`selection.py:778-900`），
而代价只作用于**fine replay**（`selection.py:943` 调用 `_greedy_lagrangian`）。
⇒ 上限约束住了**输出**，却没有约束**搜索**（候选池由未计价的 coarse 阶段决定）。
**任何协调效果必须走 `simulate.py` 实测，不能从 probe 外推。**

#### ⚠️ B4 的实测反证：上限**单独**用会变差

```
cap=None -> #TX=11  worst P_D=0.2050
cap=  3  -> #TX= 3  worst P_D=0.1472      <-- 变差
cap=  5  -> #TX= 5  worst P_D=0.1879      <-- 仍变差
```
因为**选择期用的表是未门控的**（审计 F1：掩码在选择之后才生成），上限只砍掉可选照射机、
拿不到任何干扰收益。它必须在**掩码同时反馈进表**的前提下才有意义
（`run_coordination_experiment.py` 的 `sparse` 臂正是「代价 + 评测时门控」的组合，
`--mc 2 --seeds 1` 冒烟 4.7 s，成本可忽略）。

⇒ **不要把 `probe_coordination_aware_selection.py` 的「K=3 → 0.789」当成
`selector.max_tx_nodes=3` 的预期效果**：那个 probe 在**选择期就重建了带掩码的表**，
是一个更强、且当前未接线的机制。

---

## 3. 未收口的旧缺陷（与 09-16 相同，未变）

`tools/audit_system_integrity.py --deep` 10 条 FAIL：

| 层 | 编号 | 一句话 |
|---|---|---|
| 模型 | **A1/A2** | 干扰口径枚举被静默忽略：`"Orthogonal"`、`active_set` 无掩码，都退化成 `full_concurrent` |
| 模型 | **C3/C4** | 发布工作点 rinr = **−2.81 dB**（噪声受限），「干扰受限」是几何条件量 |
| 算法 | **F1** | 选择器永远看不到 `active_tx_mask` ⇒ 选择期感知 SINR 是几何常数 |
| 算法 | **F2** | `_greedy_lagrangian` 只 append 无 swap ⇒ 「greedy gap」被系统性高估 |
| 算法 | **F3** | 迭代预算写死 `rounds/passes=2`，无收敛判据 |
| 算法 | **H1/H2** | `ABLATION_VARIANTS["full"]`/`DD_VARIANTS["full_dd"]` 是空覆盖；`legacy` 只钉 2 键 |
| 理论 | **D1** | V1 预设永远进不到精确 LLR 分支（`comm_error_model` 继承为 `gaussian_replacement`，`fusion.py:351-356` 用 `return` 静默退回 CF） |

PASS 的 11 条里较有价值的是：`B1a/B1b` 数值保护（0.0043 dB，未知模式抛异常）、
`B2` 表健康、`G1/G1b/G2` 理论闭式自洽（残差 0.00e+00）、`G3` N==L==64 巧合、
`H3` 返回值捕获（AST 判据，无假失败）。

---

## 4. 建议的处置顺序（**已执行，见 §7–§10**）

| # | 项 | 状态 |
|---|---|---|
| 1 | **A7**（`signal *= mask[i]`）：一行量级，但决定了「静默」是不是真的静默。**先修再跑任何协调 MC。** | ✅ 已修，§7 |
| 2 | **A8 + F1**：统一掩码语义——**只承认照射机掩码**，并让选择期就看到它。 | ✅ 已修，§7 |
| 3 | **C3**：把代价接进 `select_c2f_adaptive` 的 coarse rollout，**或**明确声明「本机制只作用于 fine replay」。 | ✅ 已决断：**不接**，§8.4 |
| 4 | **B4 的教训写进文档**：`max_tx_nodes` 不是 K=3 probe 的等价物，两者必须分开陈述。 | ✅ 见 §2.3 末与 memory |
| 5 | 旧缺陷按 09-16 的顺序继续：A1/A2 的枚举硬断言 → D1 二选一 → C4 绑定工作点改写杠杆文档 → `bandwidth` 的 N/L 断言 → F1/F2 先改论文措辞 → 清理项放最后。 | ⬜ 未动 |

---

## 5. 审计脚本自己踩的坑（防自欺，本次 5 次）

1. **A4 报了 9.25 的假 FAIL**：`rinr` 是 `(M,M)` 矩阵但直射项只依赖接收机 `j`，
   解析式是 `(M,)` 向量；直接相减等于把**对角线（`compute_link_tables` 恒为 0）**也算进去。
   修法：广播到 `(M,M)` 并**显式排除对角线**。两次都把「模型没错」判成 FAIL。
2. **HEAD 快照的相对导入**：`selection.py` 有**函数体内**的 `from .fbl import ...`，
   只替换行首的 `from .` 会漏掉 ⇒ 改成 `^(\s*)from +\.+`。快照文件跑完即删。

⇒ 结论：**修 FAIL 之前先确认是「被测物」错了还是「断言」错了。** 这两次都是断言错了。

**下午修复期间又发生 2 次，同类第 3、4 例**：

3. **评分口径错配**（最危险的一次）：我用粗表评分，据此得出「发布选择器在门控下输 0.126」，
   而实际两选择器在细表上**并列**。假象由评分表造成，与模型无关。详见 §9。
4. **把「未收敛」当单一状态**：34/200 未收敛被我记为一条 WARNING，实际拆开后
   **cycle 严格为 0**，纯粹是轮数预算不足。详见 §10。

**第 5 例（归因错误，已撤回）**：我把旧 MC 数字的虚高归因于 **A7**，
实测 1206 条被评估链路中 A7 分支触发 **0 次** ⇒ 归因错误，真实原因是**评分表**。见 §8.2。
该不变式已固化为 **A9**。⇒ 教训升级：**不仅「修 FAIL 前先查断言口径」，
「解释差异前也要先证明该机制真的作用在这里」**——有机制 ≠ 对该数据生效。

⇒ 统一教训：**结论异常时，先查断言的「口径」，再查被测物。** 五次里有五次是断言/归因错，不是被测物错。

---

## 6. 本文（§1–§5）覆盖不到的

- 未提交改动的**跨部署种子鲁棒性** ⇒ 已由 §8.1 的 4 种子 × 50 trial MC 补上。
- 新字段的**端到端效果** ⇒ 已由 §8.1 补上（结论：有效，且必须走 `simulate.py` 实测，不能用 probe 外推）。
- 时间档 **L=16 / L=64 / L=96 / L=144 / L=256** 的前沿 ⇒ 已由 §8.3 按同一口径补完
  （2026-09-17 18:18–19:50，五档同口径）。
- **门槛精度** ⇒ 已由 **§8.5** 用 `--mc 200`（格 0.005）复核 **全部九个档位**
  （L=16/32/40/48/56/64/96/144/256，2026-09-17 20:47–23:10），**门槛已五点夹逼闭合**：
  | 达标途径 | 最小达标档 |
  |---|---|
  | `sparse`（静默 + 重调度） | **L=48（3×）** |
  | `mask_only`（只静默） | **L=96（6×）** |
  | `uncoordinated` | **L=256（16×）** |

  ⇒ **协调 = 5.3 倍积分时间节省**（只静默 2.7 倍）；精确门槛位置 **(40, 48]**。
  同时确认 **200-trial 系统性低估**（15/15 格全部抬升 +0.005~+0.055），并**推翻四条断言**：
  硬顶 0.90、`mask_only` 从未达标、协调增益非单调、以及"协调 1× 不及 4×"（实为 ≈4.4×）。
- **仍缺**：门槛若要写到单档需补 L=44（但 40→48 是 2/4→4/4 的跃迁，边际价值有限）。
  **800-trial 表已无档位缺口。**
- **协调 × 对消深度（κ_dc）的替代性** ⇒ 已由 **§8.6** 用**专用探针 + 800 trials/臂/格**补完
  （2026-09-18 00:11–00:33，四档），**判定 SUBSTITUTES**，为全库最后一个旧口径结论收口。
- 协调信令开销 / 失效机制**未建模**，论文须声明为假设。

---

## 7. 修复落地与逐条复测（2026-09-17 下午）

改动文件：`isac_sim/{model,config,coordination,selection,simulate}.py`（`coordination.py` 为新增）。

| 修复 | 位置 | 修前实测 | 修后实测 |
|---|---|---|---|
| **A7 回波门控** | `model.py` `gate_echo` | 全员静默：**2100 / 2250** 个 `gamma_sense` > 0 | **0 / 2250**；`raw_gamma_sense` 同样 0 |
| A7 反假阳性对照 | 同上 | — | 未门控表仍 **2100** ⇒ 不是靠「全归零」过关 |
| A7 无害性 | 同上 | — | gate on + 全 True 掩码 == 未门控表，**逐位相同** |
| **掩码语义唯一化** | `config.py` `validate_config` | 门控可与任意口径组合 | 拒绝门控 + 非 `orthogonal` / 非 `shared_spectrum`（抛 `ValueError`） |
| **唯一构造器** | `coordination.illuminator_mask(selected, n_uav=None)` | 掩码在 `simulate.py` 事后拼 | `n_uav` 可由 `selected` 推断、越界抛错 |
| **覆盖性守卫** | `coordination.require_mask_covers_schedule` | 无 | 掩码静默了在用照射机 → **抛错**（报告机掩码即被此拒） |
| **F1 两阶段闭合** | `selection.select_c2f_adaptive(..., active_tx_mask=)` | fine 用 `dd_gain` 单独重建 ⇒ **静默丢门控** | 掩码喂给 coarse **与** fine 重建；gate off 或上游粗表非同一掩码 ⇒ 抛错 |
| **A8 结构性关闭** | `simulate.py` `active_set` 分支 | `{j}` 掩码被当门控用 | 改名 `payload_tx` + 注明非协调掩码；配置层已不可能误用 |
| **固定点默认选择器** | `coordination.select_with_coordination(selector=, base=)` | 默认 `lagrangian_selector` | 默认**发布方法**；可传预建 `base` 做配对臂 |

### 复测（`tools/audit_coordination_gate.py`）

**27 PASS / 1 WARN / 0 FAIL**（新增 D 段：半接线抛错、门控选择不排静默照射机、
全静默 ⇒ 空调度、掩掉首选照射机 ⇒ 调度确实改变；新增 **A9**：A7 的作用域不变式）。

### 回归

三道发布门禁仍 **CLEAN**：identity 94 冻结键 0 violated、parity 1596 格 0 differing、
`pytest` 180 passed。⇒ 修复**没有动发布身份与默认路径数值**。

`tools/audit_system_integrity.py --deep` 由 11 PASS/10 FAIL 变为 **12 PASS/10 FAIL(+WARN)**：
原 F1 是**字符串检查**（「`selection.py` 里出现 `active_tx_mask` 吗」），加了参数就会翻成**假 PASS**，
已重切为行为检查——**F1a PASS**（协调路径确实能吃掩码）+ **F1b 保留 WARN**
（发布路径仍不传掩码 ⇒ 选择仍是干扰盲的；这是铁律 1 保护下的**冻结行为**，不是回归）。

`tools/audit_coupling_changes.py`：T2b 的非法口径配置改为 `orthogonal` 后 **AUDIT PASSED**
（T10 标签真实性成立：legacy 比 coupled+guard 低约 12 dB）。

---

## 8. 正式协调 MC（权威新口径：细表评分 + 发布方法 + 跑满轮数）

### 8.1 结果

`--mc 50 --seeds 4 --looks 64 --penalty 0.1 --rounds 12`，200/200 收敛，8 min 25 s。
500 m / RCS 0.2 / 15 UAV / 10 目标，三臂逐 trial 配对。

| 臂 | worst-seed | worst-P_D MIN | std | #TX | #links |
|---|---|---|---|---|---|
| uncoordinated | 0.8850 | **0.8600** | 0.0166 | 15.0 | 18.4 |
| mask_only（同调度只静默） | 0.9150 | 0.8800 | 0.0218 | 8.1 | 18.4 |
| sparse（代价 + 固定点） | 0.9350 | **0.9200** | 0.0166 | **2.1** | 11.9 |

- 最坏种子 **0.860 → 0.920（+0.060）**，4/4 种子改善（+0.04/+0.02/+0.06/+0.08）。
- 辐射机 **15.0 → 2.1**（86% 节点静默）；链路 18.4 → 11.9。
- P_FA 三臂 **0.047–0.053**，无任一侧系统性偏高 ⇒ 增益不是拿虚警换的。
- **机制分解**：只静默不动调度 +0.020；重新调度再 **+0.040** ⇒ 两条机制都在起作用。

台账与作废判定：`results/coordination_experiment/RUN_LOG.md`。

### 8.2 旧数字的处置

| 代 | 口径 | headline（最坏种子） | 判定 |
|---|---|---|---|
| 旧 | **粗表评分** + 旧运行器（固定点默认用 `lagrangian_selector`、轮数未声明） | L=16 0.600→0.800；L=256 0.875→0.975 | **作废，禁止引用** |
| 新 | 细表评分 + 发布方法 + 跑满轮数 | L=16 **0.740→0.840**；L=64 **0.860→0.920**；L=96 **0.900→0.960**；L=144 **0.900→0.940**；L=256 **0.900→0.980** | **唯一可引用（五档同口径）** |

### 8.3 时间 × 协调前沿（200-trial 粗口径版，**权威版见 §8.5**）

五档同口径：`--mc 50 --seeds 4 --penalty 0.1 --rounds 12`，每格 **200 trials**，
**逐格 200/200 收敛**。数据 `results/coordination_experiment/coordination_experiment_L{16,64,96,144,256}.csv`，
台账 `RUN_LOG.md` §3。

| 积分时间 | 臂 | worst-seed | 4 种子均值 | std | #TX | #links |
|---|---|---|---|---|---|---|
| **L=16（1×）** | uncoordinated | **0.7400** | 0.7550 | 0.0166 | 15.0 | 24.0 |
| L=16 | mask_only | 0.8000 | 0.8050 | 0.0087 | 10.0 | 24.0 |
| **L=16** | **sparse** | **0.8400** | 0.8700 | 0.0300 | **3.4** | 17.1 |
| **L=64（4×）** | uncoordinated | **0.8600** | 0.8850 | 0.0166 | 15.0 | 18.4 |
| L=64 | mask_only | 0.8800 | 0.9150 | 0.0218 | 8.1 | 18.4 |
| **L=64** | **sparse** | **0.9200** | 0.9350 | 0.0166 | **2.1** | 11.9 |
| **L=96（6×）** | uncoordinated | **0.9000** | 0.9150 | 0.0087 | 15.0 | 16.9 |
| L=96 | mask_only | 0.9000 | 0.9300 | 0.0173 | 7.5 | 16.9 |
| **L=96** | **sparse** | **0.9600** | 0.9700 | 0.0100 | **1.9** | 11.2 |
| **L=144（9×）** | uncoordinated | **0.9000** | 0.9250 | 0.0166 | 15.0 | 15.6 |
| L=144 | mask_only | 0.9200 | 0.9500 | 0.0224 | 6.8 | 15.6 |
| **L=144** | **sparse** | **0.9400** | 0.9650 | 0.0166 | **1.6** | 10.7 |
| **L=256（16×）** | uncoordinated | **0.9000** | 0.9300 | 0.0173 | 15.0 | 13.8 |
| L=256 | mask_only | 0.9400 | 0.9650 | 0.0166 | 6.0 | 13.8 |
| **L=256** | **sparse** | **0.9800** | 0.9850 | 0.0087 | **1.3** | 10.3 |

逐种子最差 P_D（`20260917 / 101 / 202 / 303`），末列 = sparse 四种子中 ≥0.95 的个数：

| L | uncoordinated | sparse | 达标种子 |
|---|---|---|---|
| 16 | 0.74 / 0.78 / 0.76 / 0.74 | 0.86 / 0.86 / 0.84 / 0.92 | 0 / 4 |
| 64 | 0.90 / 0.90 / 0.86 / 0.88 | 0.94 / 0.92 / 0.92 / 0.96 | 1 / 4 |
| 96 | 0.92 / 0.92 / 0.90 / 0.92 | 0.98 / 0.96 / 0.96 / 0.98 | **4 / 4** |
| 144 | 0.92 / 0.94 / 0.90 / 0.94 | 0.94 / 0.98 / 0.96 / 0.98 | 3 / 4 |
| 256 | 0.94 / 0.94 / 0.90 / 0.94 | 0.98 / 0.98 / 0.98 / 1.00 | **4 / 4** |

> ⚠️ **精度边界（已由 §8.5 实测修正）**：`worst-seed` 的量化格 = 1/50 = **0.02**，
> **且小样本 `min` 向下偏**（min-of-noisy-estimates）。§8.5 用 800-trial 复核后
> **15/15 个可比格全部抬升**（+0.005 ~ +0.055）⇒ **本节数字只能当保守下界，
> 跨档比较 / 门槛定位 / "从未·硬顶·摸不到"类断言一律以 §8.5 为准。**

**五条结论**

1. ⚠️ **【§8.5 已定案】** 原判定：达标最小档落在 L=64 与 L=96 之间，且 L=144 差一格。
   → **§8.5**：门槛 = **L=48（3×）**——四点夹逼 32 **1/4** → 40 **2/4** → 48 **4/4** → 56 **4/4**
   ⇒ 落在 **(40, 48]**。"144 比 96 差"亦作废（真值 144 **0.9750** > 96 **0.9650**，方向相反）。
2. 🛑 **【§8.5 整条推翻】** 原判定：不协调有硬顶 0.90、**任何种子**没到 0.95 ⇒ 只能靠协调跨过。
   → **§8.5**：0.9000 是 min 偏差在低 P_D 处的**饱和假象**；真值随 L 单调升
   **0.7800 → 0.8300 → 0.8350 → 0.8450 → 0.8700 → 0.8850 → 0.9200 → 0.9400 → 0.9550**，
   **L=256 的 uncoordinated 四种全部 ≥0.95**。
   ⇒ 不协调**能**达标（代价 16× 时间）；协调把门槛压到 **3×** ⇒ 是**省时间**，不是"跨不过的墙"。
3. ⚠️ **【§8.5 部分推翻】** 原判定：`mask_only` 从未达到 0.95、重调度是达标必需。
   → **§8.5**：`mask_only` 门槛 = **L=96（6×）**（L=16 = 0.8150、40 = 0.8850、48 = 0.9150、
   56 = 0.9250、64 = 0.9300 均 0/4）⇒ "从未"作废，但 **L≤64 时它确实不够**。重调度增量：
   **+0.0800（16）/ +0.0750（32）/ +0.0600（40）/ +0.0400（48）/ +0.0300（56）/ +0.0400（64）/
   +0.0150（96、144）/ +0.0100（256）** ⇒ 低时间档重调度 ≥ 静默，L≥48 静默主导。
4. **协调 + 时间可跨到确定性达标**：800-trial 下 L=48/56/64/96/144/256 的 sparse **四种子全 ≥0.95**。
   ⚠️ 但 **std ≠ 0**（0.0022–0.0083）⇒ 旧口径的「std = 0 / 方差压到零」**作废**，
   只能写"跨部署确定性达标"。
5. ⚠️ **【§8.5 已推翻"非单调"】** 原判定：协调增益非单调（+0.100/+0.060/+0.060/+0.040/+0.080）。
   → **§8.5**：L=256 的 +0.080 反弹是伪影；800-trial 九档
   **+0.1150/+0.1050/+0.1100/+0.1100/+0.0850/+0.0850/+0.0450/+0.0350/+0.0300**
   ⇒ **L≥48 后单调不增（仅 L=56/64 持平，其余严格下降）**（L=16–48 平坦，跨度 0.010 = 2 格）。

**控制**：九档二十七臂 P_FA 全在 **0.046–0.053**（标称 0.05），无系统性偏高。
**效率**：#TX 15.0 → 3.4 / 2.1 / 1.9 / 1.6 / 1.3（**L≥96 后 ≈2**）；
#links 不协调 23.8 → 18.4 → … → 13.8、协调 17.0 → 11.9 → … → 10.4。
**★ 效率陈述（§8.5 权威版）**：**未协调要 16× 才达标、仅静默 6×、静默 + 重调度 3×**
⇒ **协调 = 5.3 倍积分时间节省**；
且 **协调@1×（0.8950）= 不协调的 ≈4.4× 时间**（200-trial 版"不及 4×"**已反转**，见 §8.5）。
~~（粗精度，不可写论文） 协调 1×（0.8400）落在不协调 1×（0.7400）与 4×（0.8600）之间、
略低于 4× ⇒ 「≈ 3.3× 时间，不及 4×」~~ ⇒ **该条已于 23:10 按 800-trial 定案，见上。**

#### ⚠️ 归因更正：旧数的虚高**不是 A7 造成的**

我一度把旧数虚高归因于 A7，**该归因错误，已撤回**。A7 的修法是
`if gate_echo and not active_tx_mask[i]: effective_sensing_power = 0.0`，
而 `run_coordination_experiment.py` 两个坐标臂的掩码都由**调度自身**导出
（`illuminator_mask(selected)`）⇒ 每条被评估链路的照射机必在掩码内 ⇒ **A7 分支永不触发**。

**实测**（4 种子 × 10 trial × 2 臂）：**1206 条被评估链路中，照射机被静默的 = 0 条。**
⇒ 该目录的数字与 A7 修复**无关**；差异来源是**评分表**。

A7 真正影响的是**掩码不覆盖被评估集合**的场合：`simulate.py:872` 的 `{j}` 约定（+0.0744 虚高）
以及任何「评估链路 ≠ 掩码所属集合」的探针。**判据：掩码若由调度导出，则天然免疫 A7。**

⇒ 该不变式已固化为可执行检查 **A9**（`audit_coordination_gate.py`）：
对任意调度，断言 `illuminator_mask(selected)` 覆盖其全部照射机，
即「A7 分支在协调口径下触发 0 次」。这防止未来再把差异误归因到 A7。

⇒ **凡使用「不覆盖全部照射机」的掩码测出的协调数字（含旧 `simulate.py:872` 的 `{j}` 约定）
一律虚高**；覆盖式掩码（`probe_coordination_aware_selection.py` 的 K 前沿，K=3 = 0.7894）不受影响。

### 8.4 代价阶段决断：**不接进 coarse**（`tools/probe_price_stage.py`）

门控固定点下比 FINE（代价只作用 fine replay）vs PLAN（先定辐射集再选），4 个部署种子、
18 个匹配 `#TX` 单元：per-seed 平均差 **+0.0024、中位 0.0000**、matched-K **−0.0058**，
PLAN 只在 5/18 单元领先，**2/4 种子两设计给出逐条相同的调度**（逐位核对）。
预注册阈值 +0.05 ⇒ 细阶段已在同一前沿，**不接线**。

副产物：软代价在同 `#TX` 下优于硬上限（#TX=3 时 **0.8591** vs 上限 0.7657）。

### 8.5 精度复核：`--mc 200`（量化格 0.005，九档）｜2026-09-17 20:47–23:10 ★权威

产物目录 `results/coordination_experiment_precision/`（**独立目录，不得与 §8.3 混表**）。
旗标与 §8.3 一致，只把 `--mc` 由 50 提到 200 ⇒ 每臂 **800 trials**，九档均 **800/800 收敛**。

| L | 臂 | worst-seed | 逐种子（`20260917`/`101`/`202`/`303`） | #TX |
|---|---|---|---|---|
| 16 | uncoordinated | **0.7800** | 0.800 / 0.805 / 0.810 / 0.780 | 15.0 |
| 16 | mask_only | 0.8150 | 0.840 / 0.855 / 0.860 / 0.815 | 9.9 |
| **16** | **sparse** | **0.8950** | 0.915 / 0.905 / 0.895 / 0.915 | **3.4** |
| 32 | uncoordinated | **0.8300** | 0.865 / 0.865 / 0.870 / 0.830 | 15.0 |
| 32 | mask_only | 0.8600 | 0.900 / 0.915 / 0.900 / 0.860 | 9.0 |
| **32** | **sparse** | **0.9350** | 0.940 / 0.950 / 0.935 / 0.940 | **2.8** |
| 40 | uncoordinated | **0.8350** | 0.880 / 0.880 / 0.885 / 0.835 | 15.0 |
| 40 | mask_only | 0.8850 | 0.905 / 0.905 / 0.920 / 0.885 | 8.7 |
| **40** | **sparse** | **0.9450** | 0.955 / 0.945 / 0.945 / 0.960 | **2.6** |
| 48 | uncoordinated | **0.8450** | 0.875 / 0.895 / 0.900 / 0.845 | 15.0 |
| 48 | mask_only | 0.9150 | 0.920 / 0.935 / 0.930 / 0.915 | 8.5 |
| **48** | **sparse** | **0.9550** | 0.965 / 0.970 / 0.955 / 0.960 | **2.4** |
| 56 | uncoordinated | **0.8700** | 0.885 / 0.895 / 0.905 / 0.870 | 15.0 |
| 56 | mask_only | 0.9250 | 0.925 / 0.945 / 0.940 / 0.935 | 8.3 |
| **56** | **sparse** | **0.9550** | 0.955 / 0.970 / 0.965 / 0.970 | **2.3** |
| 64 | uncoordinated | **0.8850** | 0.885 / 0.885 / 0.895 / 0.895 | 15.0 |
| 64 | mask_only | 0.9300 | 0.930 / 0.940 / 0.940 / 0.945 | 8.1 |
| **64** | **sparse** | **0.9700** | 0.970 / 0.970 / 0.970 / 0.975 | **2.2** |
| 96 | uncoordinated | **0.9200** | 0.920 / 0.925 / 0.930 / 0.925 | 15.0 |
| 96 | mask_only | 0.9500 | 0.950 / 0.970 / 0.955 / 0.965 | 7.4 |
| **96** | **sparse** | **0.9650** | 0.975 / 0.980 / 0.965 / 0.975 | **1.9** |
| 144 | uncoordinated | **0.9400** | 0.950 / 0.955 / 0.940 / 0.950 | 15.0 |
| 144 | mask_only | 0.9600 | 0.970 / 0.975 / 0.960 / 0.975 | 6.8 |
| **144** | **sparse** | **0.9750** | 0.975 / 0.995 / 0.985 / 0.980 | **1.6** |
| 256 | uncoordinated | **0.9550** | 0.955 / 0.960 / 0.955 / 0.955 | 15.0 |
| 256 | mask_only | 0.9750 | 0.985 / 0.990 / 0.975 / 0.980 | 5.8 |
| **256** | **sparse** | **0.9850** | 0.985 / 0.990 / 0.985 / 0.990 | **1.4** |

#### 最重要的收获：200-trial 是**系统性低估**

15 个格与 §8.3 对比**全部抬升**（+0.005 ~ +0.055），无一例外：

| L | 臂 | 200-trial | 800-trial | Δ |
|---|---|---|---|---|
| 16 | uncoordinated | 0.7400 | 0.7800 | +0.040 |
| 16 | mask_only | 0.8000 | 0.8150 | +0.015 |
| 16 | sparse | 0.8400 | **0.8950** | **+0.055** |
| 64 | uncoordinated | 0.8600 | 0.8850 | +0.025 |
| 64 | mask_only | 0.8800 | 0.9300 | **+0.050** |
| 64 | sparse | 0.9200 | **0.9700** | **+0.050** |
| 96 | uncoordinated | 0.9000 | 0.9200 | +0.020 |
| 96 | mask_only | 0.9000 | 0.9500 | **+0.050** |
| 96 | sparse | 0.9600 | 0.9650 | +0.005 |
| 144 | uncoordinated | 0.9000 | 0.9400 | +0.040 |
| 144 | mask_only | 0.9200 | 0.9600 | +0.040 |
| 144 | sparse | 0.9400 | 0.9750 | +0.035 |
| 256 | uncoordinated | 0.9000 | **0.9550** | **+0.055** |
| 256 | mask_only | 0.9400 | 0.9750 | +0.035 |
| 256 | sparse | 0.9800 | 0.9850 | +0.005 |

机制：`worst-seed` = 「每目标 mc 次 trial 均值」再跨目标取 **min** ⇒ **min-of-noisy-estimates
在小样本下向下偏**；估计方差越大、偏得越多。本批 `mask_only` 五档偏 **+0.015~+0.050**
（#TX≈6–10，方差最大）、`uncoordinated` 偏 +0.020~+0.055、`sparse` 偏 +0.005~+0.055
⇒ **偏差量随臂与档位而变，不能用统一常数校正**。

> **最刺眼的两格**：L=256 的 `uncoordinated`（0.9000 → **0.9550**）与 L=16 的 `sparse`
> （0.8400 → **0.8950**）。前者恰好跨过 0.95 ⇒ **直接翻掉 §8.3 的"不协调摸不到 0.95"**；
> 后者把"协调 1× ≈ 几倍时间"的插值整个抬高（§8.5 末）。

⇒ **新增口径规则**：跨档**比较**与「从未/硬顶/摸不到」类断言必须 800-trial；200-trial 只当**保守下界**；
两套量化格**不得同表**。

#### 逐条定案

| 原判定（§8.3，200-trial） | 800-trial 定案 | 处置 |
|---|---|---|
| **「不协调即便 16× 时间也摸不到 0.95」** | L=256 uncoordinated = **0.9550，四种子全部 ≥0.95** | **🛑 推翻（本轮最重）** |
| **协调增益非单调**（256 反弹 +0.080） | 九档增益 **+0.1150/+0.1050/+0.1100/+0.1100/+0.0850/+0.0850/+0.0450/+0.0350/+0.0300** ⇒ **L≥48 后单调不增（仅 L=56/64 持平，其余严格下降）**（L=16–48 平坦，跨度 0.010 = 2 格） | **推翻**（旧口径方向反而成立） |
| **门槛落在 L=64 与 L=96 之间** | 五点夹逼：16 **0/4** → 32 **1/4** → 40 **2/4** → 48 **4/4** → 56 **4/4** ⇒ 门槛 **(40, 48]** | **推翻** |
| L=144 比 L=96 差一格 | 144 **0.9750** > 96 **0.9650**（逐种子 3 胜 1 平） | **推翻** |
| `mask_only` 从未达到 0.95 | L=16 **0.8150**、40 **0.8850**、48 **0.9150**、56 **0.9250**、64 **0.9300**（均不达标）；96 **0.9500**、144 **0.9600**、256 **0.9750** | **部分推翻** |
| 不协调硬顶 0.90、任何种子没到 0.95 | 16→256：**0.7800→0.8300→0.8350→0.8450→0.8700→0.8850→0.9200→0.9400→0.9550**（单调升），L=256 **4/4 达标** | **🛑 整条作废** |
| 重调度是达标必需（+0.040） | **+0.0800（16）/ +0.0750（32）/ +0.0600（40）/ +0.0400（48）/ +0.0300（56）/ +0.0400（64）/ +0.0150（96、144）/ +0.0100（256）** | **修正**（只在 L≤64 显著） |
| 「协调 1× ≈ 3.3× 时间、**不及** 4×」 | sparse@L16 = **0.8950**，**高于** uncoordinated 4×（0.8850）⇒ **≈ 4.4×，略高于 4×** | **反转** |

#### 门槛定案（2026-09-17 22:52 五点夹逼闭合）

| 达标途径 | 最小达标档 | 依据 |
|---|---|---|
| `sparse`（静默 + 重调度） | **L=48（3×）** | L=40 只 **2/4**（0.9450）；L=48 **4/4**（0.9550） |
| `mask_only`（只静默） | **L=96（6×）** | L=56（0.9250）、L=64（0.9300）均 **0/4**；L=96 **4/4** |
| `uncoordinated` | **L=256（16×）** | L=144 只 **3/4**；L=256 **4/4** |

⇒ **协调把达标所需积分时间从 16× 压到 3×**（省 5.3 倍；只静默只能省 2.7 倍），
**不是**"跨过一道本来跨不过的墙"。门槛精确位置 **(40, 48]**。

> ⚠️ **「5.3 倍」与本节末「4.4 倍」是两回事**：前者是**门槛比值**（0.95 处 16×→3×）；
> 后者是**等值点**（0.895 处 1× 协调 ≈ 4.4× 不协调）。比值随目标水平上升，
> 因为**不协调臂在高时间档饱和更快**。写论文时须点明用哪一个。

**新增可引用事实**：sparse 在 L≥48 后平坦（0.9550 / 0.9550 / 0.9700 / 0.9650 / 0.9750 / 0.9850，
跨度 0.030，含 1 格逆向差）⇒ 时间从 3× 加到 16×（5.3×）总共只买 **0.030**；
而 uncoordinated 同期仍 **+0.110** ⇒ **时间的边际价值只在未协调时不饱和**。
机制权重随档位翻转（翻转点 L=40→48）：L=16/32/40 重调度 ≥ 静默（+0.0800 vs +0.0350；
+0.0750 vs +0.0300；+0.0600 vs +0.0500），L≥48 静默主导（+0.0700 vs +0.0400 起）。

#### 效率陈述（L=16 补齐后**反转**）

| 量 | 200-trial（作废） | **800-trial（定案）** |
|---|---|---|
| `sparse`@L16 | 0.8400 | **0.8950** |
| 不协调 4×（L=64） | 0.8600 | 0.8850 |
| 落位 | 低于 4× ⇒ "≈3.3×、不及 4×" | **高于 4×** ⇒ **≈4.4×** |

**加和检验**：时间 1×→16× **+0.175**、协调@1× **+0.1150**，相加 0.290 vs 实测 **0.205**
⇒ **明显次可加**。

#### 剩余缺口

800-trial 表**已无档位缺口**（九档全覆盖）。门槛若要写到单档需补 L=44，
但 40→48 是 2/4→4/4 的**跃迁**，细分边际价值有限；协调信令开销 / 失效机制**未建模**。

### 8.6 协调 × 对消深度（κ_dc）：替代品**定案**（2026-09-18 00:11–00:33，800 trials/臂/格）★

全库**最后一个带旧口径的结论**（`ROUTE` §2.C 的旧表：代理 + 单条冻结链路 + `{i,j}` 两点掩码
= 孤立单目标 + 粗表评分）已按 §8.5 同一口径重测收口。装具为**新增** `tools/probe_coordination_kappa.py`
（复用运行器的 `make_cfg`/`run_trial`/`ARMS`，只在 `make_cfg` 上叠
`apply_overrides({"interference.direct_cancellation_db": κ})`；与运行器 **6/6 逐种子逐位一致**）。
逐格值与机制读数全文见 **`COORDINATION_INTERFERENCE_ROUTE.md` §13.6**（本节只留结论）。

**结果（worst-P_D MIN；四档全部 800/800 收敛；P_FA 0.0473–0.0504）**

| κ_dc (dB) | uncoordinated | mask_only | sparse | **协调增益** | uncoord 中位 rinr |
|---|---|---|---|---|---|
| **40（发布值）** | 0.7800 (0/4) | 0.8150 | **0.8950** | **+0.1150** | +10.87 dB（干扰受限） |
| 50 | 0.9400 (1/4) | 0.9550 | 0.9500 | +0.0100 | +1.64 dB（贴边） |
| 60 | **0.9500 (4/4)** | 0.9550 | 0.9500 | **+0.0000** | **−4.19 dB（跨界）** |
| 80 | 0.9500 (4/4) | 0.9500 | 0.9500 | **+0.0000** | −5.82 dB |

**判定（预注册）SUBSTITUTES**：塌缩 `g(40)−g(80)` = **+0.1150** ≫ 阈值 0.05；
交互量 `Δ_uncoord − Δ_sparse` = (+0.1700) − (+0.0550) = **+0.1150** ⇒ **强烈次可加**
⇒ **协调与对消深度不可叠加计功**。跨界 κ=50→60 在**四个种子完全一致**（`dead` 条件不依赖单部署）。

**最硬的一条**：κ=80 时 `mask_only` 与 `uncoordinated` 在 **4/4 种子上 `worst_pd`/`mean_pd`
逐位完全相同**，而 `#TX` 差 **+10.9** ⇒ 关掉 11/15 台辐射机、检测统计一字不变（不靠 0.95 天花板）。
顺带：κ≥50 时 **`mask_only` ≥ `sparse`** ⇒ 「重新选择照射机」这条机制在对消足够深时**归零**。

> ⚠️ **标签纪律**：`interference.direct_cancellation_db` 在发布门禁的 **94 条冻结键**内
> （**不在** 16 条可标定键中）⇒ **κ=40 是发布值本身**，50/60/80 是**带标签的变体**
> （默认路径逐位不变；跑前后 `--check` 均 CLEAN）。
> 交叉校验：κ=40 档与 §8.5 的 **L=16 行逐格重合**（0.7800 / 0.8150 / 0.8950）。

**工程含义（双向）**：能协调 ⇒ 走协调（3× CPI）且**之后别再加深对消**；
不能协调 ⇒ **κ=60 时 1× CPI 不协调单臂即 4/4 达标**，「+20 dB 对消」可替代「协调 + 3× 时间」。

---

## 9. 评分口径错配（本次最危险的一个假发现）

`simulate.py` 把 **full fine table**（`dd_gain=base.eta_fine`，即 `c2f_tables`）喂给判决器，
而 `run_coordination_experiment.py` 与 `probe_price_stage.py` 原先用**粗表** `dd_frac_loss` 评分：

```
同一掩码、同一调度：#TX=3 时  粗表评分 0.6825   vs   细表评分 0.8280   （差 0.15）
```

后果：我据粗表得出「发布选择器在门控下输 0.126」，而两者在细表上**并列**
（差异来自评分表，不是选择器）。**同一探针在两种评分表上会给出不同符号** ——
决策探针的结论也因此在两种口径下从 +0.019/+0.021 变为 +0.0024。

⇒ 修正：两处评分统一为**细表**，并把它写进口径契约。旧粗表口径的数字全部作废。

---

## 10. 轮数收敛诊断（区分「周期振荡」与「预算不足」）

首次 MC（`rounds=4`）打印 `converged on 166/200`。**没有把它当一条 WARNING 糊过去**，
而是拆成三类计数并扫两个轮数上限：

| 轮数上限 | fixed | **cycle** | budget | 平均轮数 |
|---|---|---|---|---|
| 4 | 166 | **0** | 34 | 3.55 |
| 12 | **200** | 0 | 0 | 3.73 |

⇒ **cycle 严格为 0，未收敛纯粹是预算不足。** `coordination.py` 的环路检测在此工作点
**0/400 触发**；memory 里由 8 个种子推出的「存在周期振荡」结论**作废**（小样本不可外推）。
**`--rounds` 定为 ≥ 8。**

⚠️ **截断的偏差方向不对称**：截断会**抬高上界**——最好那个种子（303）的 sparse 由
真实的 0.960 虚高到 **0.980**；但 **worst-seed（0.920）与 headline（+0.060）一字不变**。
⇒ 只报最坏情况时截断可接受（须声明）；**一旦报最好种子 / std / cv 这类上界量，必须跑满轮数。**
