# P1-4 审计报告：离格/信念不确定性搜索——**未达预期，且"落位"这条线已关闭**

**判据（跑前写死）**：`m_rx = 8` 上 AUC 提升 ≥ 0.05 判 PASS；`|ΔAUC| < 0.02` 判 FAIL；其间为 GREY。
**实测：ΔAUC = +0.025 → GREY（未达预期）。**

但本报告的价值不在那个 GREY，而在随后补做的**裁决实验**：它把"灰"变成了确定结论——
**不是搜索没做好，是"落位"根本不是问题所在。**

## 一、做了什么

检测器在**信念**格上建被测模板。`sigma_p = 150 m → 0.96 延迟格`、`1.26 多普勒格`
（距离单元 78.1 m ⇒ 约 **1.9 个单元**），所以模板平均错位约一整格。P1-4 的假设是：
**把模板在信念不确定性范围内搜一遍取最大**，就能把回波找回来。

实现走 `target_conditioned_glrt` 现成的 `template_override` 钩子 ⇒ **未改生产代码**：
对被测目标的每条照射路径，按 `(δk, δl)` 重建 `kron(a_DD(k+δk, l+δl), a(u))`，
在 `±1 格、步长 1 格`（3×3 = 9 点，正好匹配 σ≈1 单元）上取最大统计量。

## 二、结果（20 trials，600 m / RCS 0.1）

| m_rx | 变体 | AUC | P_D @ 经验匹配 P_FA=0.05 |
|---:|---|---:|---:|
| 1 | 无搜索 | 0.557 | 0.150 |
| 1 | **搜索 ±1 格** | 0.525 | 0.050 |
| 1 | **oracle 落位**（真值格） | 0.565 | 0.050 |
| 8 | 无搜索 | 0.540 | 0.150 |
| 8 | **搜索 ±1 格** | 0.565 | 0.200 |
| 8 | **oracle 落位**（真值格） | 0.522 | 0.200 |

**oracle 落位是上界**：它把模板直接放在真值格上（接收机不可能知道，正因如此它才是上界）。
⇒ 搜索（+0.025）与 oracle（−0.018）**都在噪声内，且 oracle 并不比搜索好**。

## 三、裁决：三个正交的 oracle 上界，全部在随机水平

| 干预 | 它修复的是 | AUC（m=8） |
|---|---|---:|
| 无 | — | 0.540 |
| **阵列**（`rho` 0.18 → 0.89） | **可分性** | 0.540 |
| **oracle 落位** | **信念误差（落位）** | 0.522 |
| 搜索 ±1 格（可实现版） | 信念误差 | 0.565 |

⇒ **"信号在哪儿"和"能不能分开"这两件事，都不是这个工作点的瓶颈。**
两个互相独立的上界都停在 AUC ≈ 0.5，那么任何沿着这两条轴的工程都不可能把 AUC 抬起来。
剩下的候选只剩**有多少信号（能量）**，以及检测器自身的统计量构造。

## 四、顺带修正了我上一轮的一个误判（重要）

我上一轮说"门限 20.67 < dof 28，所以统计量不是 χ²(28)、P_FA 不可读"——**这是错的**。
核对 `glrt_threshold`：`T = χ²_dof / 2`，所以 `E[T] = dof/2 = **14**`，门限
`0.5·χ²_{0.95}(28) = 20.669`——**内部自洽**。

于是实测的 `E[t_h0]` 有了明确含义：

| m_rx | E[t_h0] | 理论 14 | 读数 |
|---:|---:|---:|---|
| 1 | 23.8 | 远高于 | **零假设被掩蔽/泄漏抬高 ⇒ 门限失效**（P_FA 0.45） |
| 4 | 15.7 | 接近 | 基本恢复（P_FA 0.15） |
| 8 | 14.4 | ≈ | **零假设回到理论值 ⇒ 门限有效**（P_FA 0.05） |

⇒ **一个此前被我否掉的正面结论成立**：阵列（可分性）**确实修好了虚警标定**——
零假设均值回到 `dof/2`。但它**没有**换来可检测性（AUC 不动）。
即：阵列把一个"失效的检验"换成了"有效但很弱的检验"。

## 五、结论与建议

1. **P1-4 不立项**（按判据 GREY，且 oracle 上界已说明上限就在眼前）。
   连续 DD / 离格 refinement 对**这个工作点**无用；若将来 σ_p 显著小于一个分辨单元，这条轴才重新打开。
2. **不要再把"未标定"归因于模板错格**。真正的顺序是：可分性 ⇒ 标定（已由阵列修复）⇒ 能量。
3. **下一步应直接测能量轴**（唯一能改变结论的轴）：固定 `m_rx=8`（可分性与标定都已到位），
   扫 RCS / 距离 / `n_looks`，找 AUC 何时离开 0.5。这条曲线同时回答两件事：
   ——旁路检测器在这个几何下需要多大 RCS；——P1-3 在哪个区间才有价值。
4. **P1-3 仍不建议先做**：准入测量（spread 0.78、与 SINR 相关系数 ≈0.02）证明它"有事可做"，
   但 P1-2 与 P1-4 两个上界证明**它要优化的东西不约束结果**。等第 3 条的曲线出来再定。

## 六、本轮方法学（可复用）

**修不好时，先做该修复的 oracle 版本。** 若 oracle 也修不好，这条轴就关闭了——
比继续调参数便宜得多，而且结论是**确定**的（"上限如此"），不是"还没调好"。
本轮用一次 oracle 落位把 GREY 变成了确定结论。

## 七、局限

* 20 trials ⇒ AUC 标准误约 ±0.04；`+0.025` 与 `−0.018` 都不可分辨（这正是判据给出 GREY 的原因）。
  **但 oracle 上界不需要大样本**：它只要证明"即使完美落位也上不去"，而这个结论在
  m=1 与 m=8 两个口径、两个不同随机流上都成立。
* 旁路 GLRT ≠ 生产检测器（生产是 SINR 软检测，主口径 P_D = 0.7169）。本报告的"检测不到"
  是**旁路检测器**的结论。
* `P_D@P_FA` 用 20 个 H0 样本的 pooled 分位数，只作指示。

## 八、复现

```
PY=E:/anaconda/3_11_python/python.exe
$PY tools/run_offgrid_search.py --trials 20 --m-list 1,8 --half 1.0 --step 1.0 \
    --out results_offgrid                                                  # ~7 min
```
CSV 含 `base / search / oracle` 三组 H1/H0 统计量，便于重算 AUC 与匹配门限下的 P_D。
