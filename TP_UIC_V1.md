# TP-UIC V1 — 把 κ 从常数换成可执行消除器的输出

**结论先行**：接收端样本级（DD 域 4096 bin）干扰消除器已经落地并能跑通。它不需要人来给
`kappa_dc`：同一套算法在 600 m / RCS 0.1 场景上**实测出 37.1 dB（中位接收机）与 41.7 dB（最差
接收机）**，两次 80-trial MC 各自独立 —— **发布默认的 40 dB 正好落在这个区间内**。这是独立于
`KAPPA_DERIVATION.md` 解析推导的第二条证据，量级自洽。但同一套测量同时给出了固定 κ 完全表达
不了的另一半事实：**不做目标保护的基础最小二乘把干扰压掉 40 dB 量级的同时，吃掉了弱目标回波
能量的 31–33%**（`eta_survive = 0.6915 / 0.6683`）。这就是「消除 dB 很漂亮，P_D 反而没提高」
的量化形态。

**没有改动发布口径。** 四道门禁全绿：发布路径 1653 格逐位 0 差异，冻结层 96 键 0 违规。
单测 16 项（原 11 项 + 5 项 H1/H0 配对回归，见第六节第 4 条）。

**本版还修掉一个让检测口径整体失效的实现错误**：H1/H0 原先由两次独立的构观调用产生，两次
调用消耗不同的随机数流，于是两个假设的**直达场根本不同**（实测 `‖x_direct‖²` 从 `5.59e-10`
漂到 `8.75e-10`）。在这种观测上算出来的 P_D 是纯噪声，与消除器好坏无关。修复后检测量恢复
正确的**排序**，但幅度仍小 —— 原因在场景而不在代码，见第六节第 4 条。

---

## 一、现状与本版范围

发布链路里 κ 的落点是唯一一处（`isac_sim/model.py:765`）：

```python
residual_direct = kappa_dc * float(I_sense_field[j])   # kappa_dc = 10**(-direct_cancellation_db/10)
```

`I_sense_field[j] = Σ_k P_rad[k]·g_kj` 是**聚合**直达场，所以 κ 对所有照明机同等对消、
逐节点不可分辨；`KAPPA_DERIVATION.md` 已证明它是个**需求值**（600 m 场景要 52.2 dB，发布
40 dB 时残余仍在处理后回波之上 10.2 dB），不是某个消除器的输出。全仓 grep
`cancel|subspace|projection|reconstruct` 只命中配置与注释——**库内没有消除器实现**。

本版交付的是这个实现，以及把它的输出送回链路表模型的解析接口。**尚未**把
`model.py` 切到新口径（见第七节）。

| 文件 | 作用 |
| --- | --- |
| `isac_sim/cancellation.py` | TP-UIC V1 核心：字典构建、保护子空间、保护 MAP 估计、保守消除、联合精化、残余不确定度核算、H1/H0 配对构观、解析桥 |
| `isac_sim/config.py` | 新增 `Cancellation` 段，`enable` 默认 `False` |
| `tools/run_tp_uic_v1.py` | 八臂 MC 实验 + 四项扫描，产出 `results_tp_uic_v1_paired{,_median}/` |
| `tools/summarise_tpuic.py` | 把 `arms_mc.csv` 规约成下方结果表（两次运行共用同一套规约） |
| `tests/test_cancellation_tp_uic.py` | 16 项不变量 |
| `tools/probe_tpuic_accounting.py`、`tools/diag_tpuic_{kernel,subspace}.py` | 排查用探针，留档 |

> **旧结果目录已作废**：`results_tp_uic_v1/` 与 `results_tp_uic_v1_median/` 是配对修复**之前**
> 的运行，其 `C_IC`/`eta_survive` 列仍有效（不受影响，见第六节第 4 条），但检测列无效，勿引用。
> 正式数字一律取 `results_tp_uic_v1_paired{,_median}/`。

---

## 二、信号模型与到链路表的桥

接收机 j 在一个 CPI 的 DD 域观测写成列向量（`K = N·L = 4096`）：

```
y_j = X_j h_j + A_j α_j + n_j                                (1a)
X_j = [ √(P_i g_ij)·a(θ^dir_ij) ]_{i∈A}                      (1b)
A_j = [ √(P_i g^tgt_ijq·G_proc·G_hw)·a(θ^tgt_ijq) ]          (1c)
```

`a(θ)` 是**单位范数**的整幅 OTFS 响应，直接复用 `isac_sim.waveform.full_otfs_kernel`，
`θ` 用与 `model.build_base_gains` 完全相同的换算（`l = τ·L·Δf`，`k = ν·N·T`）。因此有两
个恒等式成立，这一点不是类比而是同一件事：

- `‖a(θ)‖² = 1` ⟹ `‖X_j h‖² = Σ_i P_i g_ij = I_sense_field[j]`。**样本级输入干扰功率就是
  模型里那个直接场**，回灌时不需要重新标定单位。
- 回波列范数等于 `P_i·g^tgt·G_proc·G_hw`，与 `model.py` 里 `signal` 的表达式逐项一致
  （`G_proc` 出现是因为 DD 域匹配滤波对整帧相干积累，而直达路径不对自己的波形做匹配滤波）。

实测验证：`‖x_direct‖² / I_sense_field = 0.943`。差的 5.7% 来自不同照明机 DD 核之间的交叉
项（核不是严格正交，且核在 DD 域有旁瓣）。单测
`test_direct_field_matches_the_link_table_field` 把这个比值钉在 15% 容差内，而不是假装它
恰好为 1。

---

## 三、四个模块

**M1 干扰字典** `X_j`。默认**每台活动照明机一列**（`interference_tangent_order = 0`），这是
物理上正确的默认而不是图省事：协作网内 UAV 位置是共享的，直达路径的时延与多普勒是**算出来的**，
只有复增益未知。加切空间列（分数时延/多普勒基）是在建模残留的同步与振荡器失配。

> **实测警示：打开干扰切空间会让式(3) 的保留项失真，`i_res_pred` 不可信。**
> `interference_tangent_order = 1` 时 X 从 14 列涨到 42 列，实测三个量（vs `I_in`）：
>
> | | 保留项 term 1 | 估计项 term 2 | 实测残余 → `C_IC` |
> |---|---|---|---|
> | `order = 0` | −10.72 dB | −48.82 dB | 3.71e-9 → **+10.39 dB** |
> | `order = 1` | **+3.18 dB** | −45.30 dB | 3.71e-9 → **+10.39 dB** |
>
> **保留项单独一项就超过了整个输入直达场**，预测因此失效（`cal_err = 13.65 dB`），而实测残余
> 与 `order = 0` **逐位相同**。机制是：`residual_accounting` 对 X 的**每一列**都给
> `R_h = prior_variance`，包括那些真值系数恒为零的切空间列 —— 于是新增的 28 列被当成
> 「有单位方差的真实干扰」计入保留项，把它抬了 ~25 倍。
> **结论：`order > 0` 时 `i_res_pred` 只能当无效值，必须用实测残余。** 默认 0 就是为了避开
> 这个不一致；真正的修法（只对中心列给先验）留到下一版（见第十节）。

**M2 目标切空间保护**。对每个被保护目标不止保护中心响应，还保护它的一阶变化：

```
B_q = [ a_q , ∂a_q/∂f_delay , ∂a_q/∂f_doppler ]
U_j = orth([B_1 … B_Q]) ,  P = U Uᴴ ,  M = I − P          (2)
```

因为 `a(θ+Δθ) = a(θ) + JΔθ + O(‖Δθ‖²)`，真实目标落在保护空间外的部分满足
`‖M a_true‖ = O(‖Δθ‖²)`——保护是关于一个邻域的一阶陈述，带明确的误差阶，而不是「附近三个
bin 不消」这种没有误差项的说法。这一条有单测：`test_protection_covers_a_neighbouring_offset_to_first_order`
验证泄漏随偏移单调增、且 0.04 bin 偏移仍留在保护内。

**M3 保护 MAP 估计与保守消除**

```
ĥ = (XᴴMX + σ²R_h⁻¹)⁻¹ XᴴMy                                 (3a)
C_h = (R_h⁻¹ + σ_n⁻² XᴴMX)⁻¹                                (3b)
r1 = y − M X ĥ                                              (3c)
```

(3c) 有一个干净到可以当不变量用的性质：既然 `UᴴM = 0`，则

```
P r1 = P y                                                   (3d)
```

实测残差 `5.6e-11`（相对）。含义是：**被接收机定义为「可能载有目标证据」的那部分观测，第一
阶段一个比特都没动**。干扰估计器无法把目标「拟合掉」。这比调一个 target-protection penalty
强得多——后者只能降低概率，前者是恒等式。

**M4 残余不确定度核算**。残余拆成「为保护而主动保留」与「估计误差」：

```
I_res = tr(P X R_h Xᴴ P)  +  tr(M X C_h Xᴴ M)                (4)
        └ 保留项（保护的代价）┘  └ 估计项（参考预算的代价）┘
```

于是「消除得不彻底」和「算法失败」不再是同一句话。两项在样本域里的对应物是
`‖x − f(x)‖²` 与 `‖f(n)‖²`，而且**互相正交**（`f(n) ∈ span(MX)`，`x − f(x) ⊥ span(MX)`），
所以求和是精确的、不是近似。

**阶段二（联合精化）**：一阶段候选支撑确定后

```
(ĥ, α̂) = argmin ‖y − X h − A_Ω α‖² + λ_h‖h‖² + λ_t‖α‖²      (5a)
r2 = y − X ĥ_joint                                          (5b)
```

**目标估计量绝不从 r2 里减掉。**

---

## 四、度量口径（这一步做错会让结论反向）

所有臂都是观测上的**线性映射 f**，于是残余精确分解为

```
y − f(y) = [x − f(x)] + [s − f(s)] − f(n)
```

三项分别是「没消掉的干扰」「活下来的回波」「估计器从噪声里拉进来的东西」。因此：

- **`C_IC = 10log10(I_in / I_res)`，其中 `I_res = ‖x−f(x)‖² + ‖f(n)‖²`。** 只取干扰残余，与
  模型口径一致（`compute_link_tables` 把噪声底和残余直达场写成分母里两个独立项，κ 从来不描
  述噪声）。把 `f(n)` 漏掉会**给每个估计器白送深度**，因为估计误差恰恰是从它拟合的噪声里
  生出来的。
- 不允许用 `‖x − f(y)‖²` 当残余：它把目标泄漏算进「干扰残余」，方向恰好是**美化保护**。
  第一版就是这么写错的，两种写法在同一臂上差 30 dB 量级。
- `eta_survive = ‖s − f(s)‖²/‖s‖²` 才是「目标被吃掉了多少」。文档里的 `‖P s‖²/‖s‖²` 是保护
  **覆盖度**（设计量），两个都报，别混。

**式(4) 的验证**：在无保护的 `plain_ls` 上，预测估计项 `5.255e-13` vs 实测 `5.154e-13`，
**差 2%**；在 `protected_ls` 上预测 `4.767e-13` vs 实测 `4.871e-13`。无先验 LS 下的恒等式也
钉住了：`tr(C_h G) = σ²·rank(X)`。

---

## 五、结果

配置：`paper-canonical` + `--area 600 --rcs 0.1`，`interference.direct_cancellation_db = 40`，
`cancellation.enable = True`，`seed = 2026`，每臂 **80 trial**。两种接收机选择规则各跑一遍：
**worst** = 该 trial 回波-直连裕度最低的接收机（方法瞄准的最难情形，选中处裕度中位 −11.7 dB），
**median** = 裕度居中的接收机（−9.1 dB）。复现命令见第九节。

**`H1/H0` 列的定义**：`10log10(stat_H1 / stat_H0)` 的**逐 trial 配对中位**，其中统计量是弱目标
所在列块子空间上的归一化能量 `‖U_wᴴ·residual‖²/rank(U_w)`。这是比 P_D 稳健得多的口径 —— 它不
需要定阈、不受 80 次采样分辨率限制，因此用它判断「消除器是否真的把回波露出来了」。

### 5.1 worst 接收机

| arm | C_IC [dB] | pred [dB] | cal err [dB] | eta_prot | eta_surv | H1/H0 [dB] | P_D | P_FA | n_det |
|---|---|---|---|---|---|---|---|---|---|
| `no_ic` | 0.00 | 0.00 | 0.00 | 0.4215 | 1.0000 | +0.06 | 0.062 | 0.050 | 5/80 |
| `fixed_kappa` (40 dB) | 40.00 | 40.00 | 0.00 | 0.4215 | 1.0000 | +0.20 | 0.062 | 0.050 | 5/80 |
| `plain_ls` | 41.73 | 41.25 | 0.77 | 0.4215 | 0.6915 | +0.21 | 0.050 | 0.050 | 4/80 |
| `ridge_ls` | 41.78 | 41.25 | 0.78 | 0.4215 | 0.6915 | +0.21 | 0.050 | 0.050 | 4/80 |
| `protected_ls` | 8.30 | 8.20 | 0.71 | 0.4215 | 0.7899 | +0.06 | 0.062 | 0.050 | 5/80 |
| `tp_uic_stage1` | 8.30 | 8.20 | 0.71 | 0.4215 | 0.7911 | +0.06 | 0.062 | 0.050 | 5/80 |
| `tp_uic_full` | 32.08 | 8.20 | 23.82 | 0.4215 | 0.9824 | +0.19 | 0.062 | 0.050 | 5/80 |
| `perfect_channel` | inf | inf | nan | 0.4215 | 1.0000 | +0.21 | 0.062 | 0.050 | 5/80 |

### 5.2 median 接收机

| arm | C_IC [dB] | pred [dB] | cal err [dB] | eta_prot | eta_surv | H1/H0 [dB] | P_D | P_FA | n_det |
|---|---|---|---|---|---|---|---|---|---|
| `no_ic` | 0.00 | 0.00 | 0.00 | 0.5686 | 1.0000 | +0.10 | 0.050 | 0.050 | 4/80 |
| `fixed_kappa` (40 dB) | 40.00 | 40.00 | 0.00 | 0.5686 | 1.0000 | **+0.41** | 0.087 | 0.050 | 7/80 |
| `plain_ls` | 37.12 | 37.41 | 1.00 | 0.5686 | 0.6683 | +0.52 | 0.062 | 0.050 | 5/80 |
| `ridge_ls` | 37.31 | 37.41 | 1.04 | 0.5686 | 0.6697 | +0.52 | 0.062 | 0.050 | 5/80 |
| `protected_ls` | 6.25 | 5.81 | 0.71 | 0.5686 | 0.8099 | +0.10 | 0.062 | 0.050 | 5/80 |
| `tp_uic_stage1` | 6.25 | 5.81 | 0.71 | 0.5686 | 0.8171 | +0.10 | 0.062 | 0.050 | 5/80 |
| `tp_uic_full` | 28.92 | 5.81 | 22.18 | 0.5686 | 0.9415 | +0.33 | 0.050 | 0.050 | 4/80 |
| `perfect_channel` | inf | inf | nan | 0.5686 | 1.0000 | **+0.41** | 0.100 | 0.050 | 8/80 |

**怎么读这张表**

- **`C_IC` 与 `eta_surv` 才是判别口径。** `plain_ls` 在 worst 上拿到 41.73 dB 的全场第二高消除
  深度，代价是 `eta_surv = 0.6915`（吃掉 30.85% 的回波）；在 median 上是 37.12 dB / 0.6683
  （吃掉 33.17%）。`tp_uic_full` 用 32.08 / 28.92 dB 换到 `eta_surv = 0.98 / 0.94` —— 少 10 dB
  深度，换回几乎全部回波。
- **`fixed_kappa` 不是稻草人。** 40 dB 常数在 H1/H0 上与 `perfect_channel` **打平**
  （median 上都是 +0.41 dB），因为它们都只是从观测里减掉一个直连模型，且在本场景下 40 dB 已经
  把残余压到噪声量级以下 —— 再准也没有可压的东西了。**这是 κ=40 dB「不是随手填的」最直接的
  数值证据**，同时也说明：在这个工作点上，「用算法替换 κ」的收益不在深度上，而在
  `eta_surv`（保护回波）和 `cal_err`（知道自己残余多少）上。
- **`protected_ls` / `tp_uic_stage1` 与 `no_ic` 完全同分**（H1/H0 都是 +0.06 / +0.10，逐位相同）。
  这不是巧合，是 `P r1 = P y` 的必然结果：一阶段一个字都没动被保护子空间，而弱目标恰恰是被保护
  的那个，所以它的可检测性与「完全不消除」一模一样。**保护的代价第一次被量化成了检测损失。**
- **`n_det` 列要一起读。** P_D 与 P_FA 在 80 trial 上只差 **1–3 个样本**（4/80 vs 8/80），
  统计上不可分辨。原因是场景而非代码，见第六节第 4 条。因此 P_D 在本版**只作方向性核对**，
  不作结论数字。
- **`cal err` 一列已经把 `tp_uic_full` 的机制说清楚了**：它的 `cal_err = 23.82 / 22.18 dB`
  是**巨大**的，因为它用实测的 `residual` 打分，而式(3) 只承诺一阶段的保守残余 —— 差的这
  23 dB 就是阶段二联合精化买到的深度，是特性不是缺陷。反过来 `plain_ls` 的 `cal_err = 0.77 dB`
  说明对无保护臂，式(3) 的解析预测**准到 1 dB 以内**。

---

## 六、四个机制发现

### 1. 基础 LS 用 41.7 dB 的干扰抑制吃掉 30.9% 的弱目标

`plain_ls`（无保护）在 worst 接收机上实测 `C_IC = 41.73 dB`、`eta_survive = 0.6915`（median 上
37.12 dB / 0.6683）。这正是你文档预言的失败模式，而它**不需要任何复杂机制就会发生**：干扰字典
张成的子空间里本来就含有一大块目标方向的能量，最小二乘会把目标当成需要解释的干扰的一部分。

这也是本版最直接的答案：**发布默认的 40 dB，落在一个真实最小二乘消除器于 600 m / RCS 0.1 上
实测到的 37.1–41.7 dB 之间。** 两次独立运行把常数夹在中间，而 κ 原本是按聚合场定义、还带着
交叉项与先验口径的差异 —— 但「40 dB 不是随手填的」这件事，现在有一条独立于解析推导的数值证据。

### 2. 保护不是免费的：本场景的直达子空间和目标子空间在 DD 域**重叠**

这是本版最重要的发现，而且它一开始是以「算法坏了」的表象出现的。

- 不设保护预算时（`max_protected_targets = 0`），接收机要保护 140 个回波假设（14 台照明机 ×
  10 个目标），切空间秩在 12-trial 扫描里中位 **217 / 4096**（单 trial 实查 169，随几何波动）。
  此刻**14 台照明机的直达核落入保护子空间的比例：中位 99.997%，但最差只有 80.8%** ——
  多数路径几乎被完整「保护」进去了，总有一两台落在外面。
  `‖MX‖_F/‖X‖_F` 在预算 0 / 3 / 1 下分别是 **0.414 / 0.954 / 0.995**：预算一放开，干扰字典的
  可学习部分就被吃掉四成以上，于是实测深度从 16.96 dB（预算 1）塌到 **1.49 dB**（预算 0）
  ——**不是数值问题，是整个干扰学习空间被保护吃掉了**。
- 根因是几何量：600 m 场景的 DD 占用极小。1 个 delay bin = `1/(LΔf)` = 0.52 µs = **156 m**，
  整个 600 m 场景只有 **3.8 个 delay bin**；1 个 Doppler bin = `Δf/N` = 469 Hz，在 5.9 GHz 上
  相当于 **23.8 m/s**，30–150 m/s 的相对径向速度对应 **±6 个 bin**。实测直连偏移落在
  delay ∈ [0.96, 3.51]、doppler ∈ [−0.3, 3.4]，回波偏移同区域——**所有东西挤在 4096 个 bin
  里的大约 33 个里**，再加核旁瓣也只在 ~100 个 bin 量级。

  换句话说：**这个工作点上 DD 域根本没有分辨开直达路径和目标回波。** OTFS-ISAC 文献里
  「DD 域保护」好用的前提（场景在 DD 域可分辨）在这里不成立。

- 因此 `cancellation.max_protected_targets`（默认 3，按该接收机处**回波最弱**的目标优先）
  是承重旋钮，不是参数微调。12-trial 扫描给出的完整代价曲线是一条干净的单调权衡：

  | 保护目标数 | `protect_dim` | `C_IC` [dB] | `eta_surv` |
  |---|---|---|---|
  | 0（= 保护全部） | 217.0 | 1.49 | 0.9975 |
  | 1 | 40.0 | **16.96** | 0.6861 |
  | 2 | 75.5 | 12.43 | 0.7030 |
  | 3（默认） | 107.5 | 8.58 | 0.7302 |
  | 5 | 148.0 | 5.88 | 0.8737 |
  | 10 | 217.0 | 1.49 | 0.9975 |

  ⚠️ **`0` 表示保护全部，不是「关闭保护」**，所以它与 `10` 同值（`Q = 10`，全保护），
  曲线因此在两端非单调。这个反直觉取值是刻意保留的消融臂，不是笔误。
  `eta_surv` 随保护数上升而上升、`C_IC` 随之下滑 —— 两者是同一枚硬币，**保护就是拿深度换回波**。

### 3. 参考预算只买估计项，买不到保留项

`n_cpi`（相干参考 CPI 数）从 1 → 64，`tp_uic_stage1` 的总深度**逐档完全相同：8.58 dB**，
`eta_surv` 也一字不动（0.7302）—— 参考能量对结果**毫无影响**；而估计项本身在解析上应涨
12.04 dB（由单测 `test_reference_budget_scales_the_estimation_term` 钉住）。因为在保护开启、
且保护与干扰重叠的前提下，**保留项主导残余**，而保留项只随先验精度和 DD 重叠变化，不随参考
能量变化。这是「多加导频」这类直觉在本场景失效的量化说明 —— 而且是**极端版本**：不是收益小，
是收益为零。

单测 `test_reference_budget_scales_the_estimation_term` 把这个 12 dB 的比例钉住。

### 4. 「最弱目标检测」这个口径在本场景是**弱定义**的 —— 先修实现错，再承认场景限制

这一条分两半，前半是实打实的 bug，后半是场景的硬限制。

**前半：H1/H0 原先不是配对观测。** 第一版的 `evaluate` 用两次独立的 `build_observation` 调用
造 H1 和 H0。`build_observation` 每次都会消耗传入的 rng 去抽直连相位 `h_true`、回波相位
`alpha_true` 和噪声，所以第二次调用拿到的是**另一份直连场**。实测（trial 0）：
`‖x_direct‖²` 从 `5.59e-10` 变成 `8.75e-10`（差 57%），`‖y‖²` 的 H0 甚至**大于** H1。在这种
观测上比较两个统计量，测到的是照明实现的涨落，与消除器毫无关系 —— 于是所有臂的 `H1/H0` 都挤在
**1.00–1.15**，P_D 全部贴在 CFAR 地板上。当时我一度把它归因成「因为选了最差接收机」，**这个
归因是错的**：换成 median 接收机后 P_D 依旧贴地板，才有了这次追查。

修法是让 H0 **从 H1 派生**（`build_observation_pair`）：两者共享 `x_direct`、`X`、`A` 和其余
所有目标的回波，只把被测目标的列清零，只重抽噪声。这一条现在有 5 项回归单测钉住，其中
`test_h0_is_h1_minus_exactly_the_tested_echo` 在 `share_noise=True` 下设的是**恒等式**而不是
容差比较：`(y1 − x) − (y0 − x) = s_weak`。

> 附带结论：因为 `obs1` 的 rng 位置没有改变，修复后 `C_IC`、`eta_survive`、残余核算**逐位不变**
> —— 第五节的 C_IC / eta_surv 列在修复前后完全一致，这也反过来印证了修复只触碰了检测口径。

**后半：修好之后幅度仍然只有 0.06–0.52 dB，且这是场景决定的。**

- **被测目标的回波本来就弱。** 按构造选的是该接收机处回波-直连裕度最低的那个目标（worst
  −11.7 dB / median −9.1 dB）。实测它的回波在**完美对消之后仅比每 bin 噪声高 +2.7 dB**，而直连
  高 +11.2 dB。理想对消也只能把 SNR 提到这个量级，P_D 自然贴着 P_FA。
- **更要紧的是其余 9 个目标的回波在 DD 域几乎与被测目标共位。** 检测统计量建在被测目标的列块
  子空间上，而第 6.2 节已经证明整个 600 m 场景只占约 4×10 个 DD bin —— 于是「只去掉被测目标」
  的 H0 里，其余目标的回波**仍然落在同一个子空间内**，把 H0 抬到 H1 附近。这是为什么主场景的
  分离度（+0.06~+0.52 dB）比小配置实测（M=6/Q=3，+2~+5 dB）低一个量级。
- 因此「检测最弱目标」在这个工作点上是个**弱定义**的指标：DD 分辨率不足以把目标分开时，
  「该目标不在」这个假设本身就不成立。**这是场景的结论，不是代码的缺陷。**

**所以本版的口径是**：`C_IC` + `eta_survive` + 残余核算（式(3)(4) 的预测 vs 实测）作为判别性
指标；`H1/H0` 只作**方向性核对**（它确实把臂排对了：`perfect_channel` ≈ `fixed_kappa` >
估计器 > 保护臂 = `no_ic`）；**P_D 在 80 trial 下不构成结论数字**。要让 P_D 变成可引用数字，
只有三条路：单目标场景、抬高被测目标回波（RCS/距离）、或改用对最强可信路径的相干匹配滤波
（当前刻意用的是不知道回波方向的子空间能量检测器，见第十节第 4 条）。

---

## 七、与固定 κ 的关系 / 回灌路径

解析桥已实现但**未接入** `model.py`：

```
predict_cancellation(cfg, I_sense_field, n_illuminators) -> (residual_fraction, retention_fraction)
kappa_from_budget(cfg, ...) -> 等效 κ (dB)
```

用法是把 `model.py:765` 的 `kappa_dc * I_sense_field[j]` 换成
`residual_fraction[j] * I_sense_field[j]`，分母结构一行不改。**但接口里有一处已知不可靠**：
保留项用的是**维数比** `n_targets·(M−1)·(1+2·order)/K`，它假设保护子空间与干扰子空间处于
一般位置。实测（worst 接收机、seed 2026、`interference_tangent_order = 0`）：

| | 维数比（规划用） | 实测保留 / `I_in` | 乐观倍数 |
|---|---|---|---|
| 预算 3（发布默认） | 3.08% | **9.15%** | **2.97×** |
| 预算 0（保护全部） | 10.25% | **82.51%** | **8.05×** |

即**维数比在默认档就已经乐观 3 倍，放开保护后恶化到 8 倍**，而它正是回灌时唯一可用的廉价预测。
（对照：完整式(3) 的保留项在同一档给出 8.47%，与实测 9.15% 只差 0.7 个百分点 —— 所以**走几何的
式子准、走维数比的捷径不准**；但 `predict_cancellation` 之所以用维数比，正是为了不把 `XᴴU`
这种样本级几何带进链路表。）所以：

> 调度器只能消费**实测**的 `I_res`，或用实测拟合过的预测；维数比只能用于规划。

回灌之前必须先做的事：用 MC 实测把保留项标定成一个关于 `(保护目标数, DD 重叠度)` 的经验式，
或者干脆让接收机在线上用 `C_h` 加一个标定因子自报。**标定因子至少要覆盖 3×–8× 这个区间**，
且必须随保护预算变化 —— 一个常数因子不够。这是下一版的第一件事。

**本节没有改动任何发布路径**：`cancellation.enable` 默认 `False`，`model.py` 未动。

---

## 八、过程中踩到并修掉的三个陷阱

三条都属于「看起来像模型错、其实是环境/度量错」，值得留档：

1. **`EPS = 1e-12` 比噪声功率还大 26 倍。** 默认电台 `n0 = 3.83e-14` W，于是
   `max(sigma2, EPS)` 把方差悄悄换成了大 26 倍的值，式(4) 的预测整体膨胀 26 倍（实测 365
   vs 正确的 14 个噪声单位）。`model.py` 的 `denominator_guard` 文档里写过**一模一样**的
   陷阱。现在改成 `_positive_sigma()`：拒绝非正方差，而不是夹取。单测
   `test_positive_sigma_refuses_a_clamped_variance` 会在这个前提失效时报警。
2. **`orthonormalise` 的秩判定必须尺度不变。** 切空间列是有限差分方向，其范数与中心列不同，
   差距随 `step` 变化：默认 `step=0.05` 时实测列范数比只有 **3.6 倍**，但改小 `step` 或直接
   传入未归一化的差分列会把它放大到任意倍数。用**相对最大特征值**的阈值做秩判定时，这个差距
   会污染秩：实测同一组 420 列，旧规则在容差 1e-12 / 1e-9 / 1e-6 下给出 **219 / 186 / 148**
   三个不同秩，而新规则（先按列归一化、再对相关矩阵用绝对阈值）稳定给 **169**。
   **这里的缺陷不是「秩虚高」，而是「秩由容差决定」** —— 一个随容差漂移 71 的秩无法作为
   保护代价的输入。单测 `test_orthonormalise_rank_ignores_column_scaling` 把尺度不变性钉死
   （它用 1e3 / 1e-3 的人为缩放，而不是依赖默认 `step` 的自然差距）。
3. **H1/H0 用两次独立构观 ⇒ 检测口径整体失效。** 详见第六节第 4 条；这是三者中后果最严重的
   一个，因为它不报错、不 NaN，只是安静地把每个臂的 P_D 压到地板上。

---

## 九、门禁与复现

四道门禁**在改动 `cancellation.py` 之后重跑**（`PY=E:/anaconda/3_11_python/python.exe`），全绿：

| 门禁 | 命令 | 结果 |
| --- | --- | --- |
| 1 发布身份 | `tools/check_release_identity.py --check` | CLEAN，96 冻结键 / 0 违规，归档交叉 110 键 |
| 2 重构口径逐位 | `tools/parity_check.py --baseline --strict` | CLEAN，0 差异 |
| 2b 发布口径逐位 | `tools/parity_check.py --release-baseline --strict` | CLEAN，**1653 格 / 0 差异**（py3.11.0 / numpy 2.2.6） |
| 3 契约引用 | `tools/check_contract_refs.py` | CLEAN，32 条引用全部解析（theory 15 / model 6 / algorithm 11） |
| 4 单测 | `pytest tests/ -q` | **196 passed**, 6 subtests passed |

门禁 2b 单独跑约 1.5 分钟；不要和门禁 1/2 串在一个 shell 里，会撞上工具默认超时。

复现本版结果：

```bash
PY=E:/anaconda/3_11_python/python.exe

# 主表：两种接收机规则各 80 trial（各约 3.5 分钟）
$PY tools/run_tp_uic_v1.py --trials 80 --receiver-rule worst  --quiet --out results_tp_uic_v1_paired
$PY tools/run_tp_uic_v1.py --trials 80 --receiver-rule median --quiet --out results_tp_uic_v1_paired_median

# 规约成第五节的表
$PY tools/summarise_tpuic.py results_tp_uic_v1_paired/arms_mc.csv

# 扫描（n_cpi / tangent_order / interference_tangent_order / max_protected_targets）
$PY tools/run_tp_uic_v1.py --trials 80 --sweep-trials 12 --sweeps --out results_tp_uic_v1_sweeps

# 单测
$PY -m pytest tests/test_cancellation_tp_uic.py -q
```

> `--max-protected-targets 0` 表示**保护全部目标**，不是「关闭保护」。它是扫参里唯一取 0 却
> 最贵的取值（`protect_dim` 与取 10 时相同），在 `max_protected_targets` 扫描里表现为非单调。
> 这是刻意保留的消融臂，不是笔误。

---

## 十、下一版要做的（按优先级）

1. **标定保留项**，把 `predict_cancellation` 从维数比换成实测拟合（标定因子须随保护预算变化，
   至少覆盖第七节实测的 3×–8× 区间），然后才谈回灌 `model.py`。在此之前任何「κ 由算法给出」
   的发布数字都不可靠。
2. **给回灌加门控**：`cancellation.mode = "predict"` 时 `model.py` 才读新接口，默认仍走常量，
   并重跑 2b 门禁确认默认路径逐位不变。
3. **DD 分辨率问题需要一个正面回答**。要么承认这个工作点上保护昂贵、把 TP-UIC 定位成
   「保护代价可计算」而不是「保护很便宜」；要么换波形参数（更小的 `Δf` 或更大的 `N·L`）
   让场景在 DD 域可分辨，并在新工作点上重跑本文全部数字。**这是方法层面的岔路，需要你定。**
4. **CFAR 支撑门控已经实现但未充分检验**：阶段二的候选集当前由接收机自报的残余水平定阈
   （`-ln(P_FA)·(n0 + I_res_pred/K)`），没有用真值。MC 上 `P_FA` 的收敛和支撑选择正确率还没
   单独统计过。注意这个门限与第五节的 `P_D/P_FA` **不是同一个东西**：前者是结构预测水平，后者
   来自每臂自己的经验 H0 分位数。两者的区别已写进 `run_tp_uic_v1.py` 的模块 docstring。
5. **P_D 口径需要一次决策（第六节第 4 条）**。现在有三个选项，互相排斥，需要你定：
   - **(a) 换场景**：跑单目标变体（`Q = 1`），把「该目标不在」这个 H0 变成良定义。最干净，
     代价是本版所有多目标数字要重跑。
   - **(b) 抬高被测回波**：对被测目标单独提高 RCS 或拉近距离，让完美对消后的 SNR 从 +2.7 dB
     升到 10 dB 以上。改动最小，但会让人质疑「是不是挑了个好检测的目标」。
   - **(c) 换检测器**：从「不知道回波方向的子空间能量检测器」改成对最强可信路径的相干匹配
     滤波。能拿回现在被 `rank` 惩罚吃掉的 ~15 dB，但需要额外的路径选择逻辑，且会削弱
     「接收机不知道哪个照明机占主导」这个前提的真实性。
   > 在选定之前，**P_D 不进主表**；`H1/H0` 只作方向性核对。
6. **`interference_tangent_order > 0` 时保留项的先验错配（第三节实测警示）**。
   `residual_accounting` 对 X 的每一列都给 `R_h = prior_variance`，但切空间列的真值系数恒为零，
   于是保留项被抬 ~25 倍、预测失效。修法是只对中心列给先验（需要把「哪些列是中心列」传进
   `residual_accounting`）。默认 `order = 0` 时不受影响，所以优先级低于前五项。
7. **只做了线性核**。PA 非线性、IQ 失衡、相位噪声都还在 `waveform_impairments` 里作为
   INR 项存在；等线性模型确实到顶再接小型非线性 residual canceller（AOP-LMS 那类）。

---

*本文件记录的是已落地的实现与已实测的数字。文中「发布默认 40 dB 落在实测 37.1–41.7 dB 之间」
是两个独立口径的对照，不构成对发布数字的替换。第五节两张表的 `C_IC` / `eta_surv` 列在 H1/H0
配对修复前后逐位不变，可直接与历史记录比对；检测列只在配对修复之后才有效。*
